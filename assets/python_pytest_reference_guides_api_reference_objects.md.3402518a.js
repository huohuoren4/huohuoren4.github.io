import{_ as e,o,c as t,X as s}from"./chunks/framework.b5656a4e.js";const m=JSON.parse('{"title":"Objects","description":"","frontmatter":{},"headers":[],"relativePath":"python/pytest/reference_guides/api_reference/objects.md","filePath":"python/pytest/reference_guides/api_reference/objects.md","lastUpdated":1692620003000}'),a={name:"python/pytest/reference_guides/api_reference/objects.md"},n=s('<h1 id="objects" tabindex="-1">Objects <a class="header-anchor" href="#objects" aria-label="Permalink to &quot;Objects&quot;">​</a></h1><p>Objects accessible from <a href="/python/pytest/reference_guides/fixture_reference#fixtures-reference">fixtures</a> or hooks or importable from <code>pytest</code>.</p><h2 id="callinfo" tabindex="-1">CallInfo <a class="header-anchor" href="#callinfo" aria-label="Permalink to &quot;CallInfo&quot;">​</a></h2><ul><li><h3 id="final-class-callinfo" tabindex="-1"><code>final class</code> <strong>CallInfo</strong> <a class="header-anchor" href="#final-class-callinfo" aria-label="Permalink to &quot;`final class` **CallInfo**&quot;">​</a></h3><p>Result/Exception info of a function invocation.</p><ul><li><p><strong>excinfo</strong>: <code>Optional[ExceptionInfo[BaseException]]</code></p><p>The captured exception of the call, if it raised.</p></li><li><p><strong>start</strong>: <code>float</code></p><p>The system time when the call started, in seconds since the epoch.</p></li><li><p><strong>stop</strong>: <code>float</code></p><p>The system time when the call ended, in seconds since the epoch.</p></li><li><p><strong>duration</strong>: <code>float</code></p><p>The call duration, in seconds.</p></li><li><p><strong>when</strong>: <code>Literal[&#39;collect&#39;, &#39;setup&#39;, &#39;call&#39;, &#39;teardown&#39;]</code></p><p>The context of invocation: “collect”, “setup”, “call” or “teardown”.</p></li><li><p><code>property</code> <strong>result</strong>: <code>TResult</code></p><p>The return value of the call, if it didn’t raise.</p><p>Can only be accessed if excinfo is None.</p></li><li><p><code>classmethod</code> <strong>from_call</strong>(<code>func, when, reraise=None</code>)</p><p>Call func, wrapping the result in a CallInfo.</p><p><em>Parameters</em>:</p><ul><li><p><code>func (Callable[[], TResult])</code> – The function to call. Called without arguments.</p></li><li><p><code>when (Literal[&#39;collect&#39;, &#39;setup&#39;, &#39;call&#39;, &#39;teardown&#39;])</code> – The phase in which the function is called.</p></li><li><p><code>reraise (Optional[Union[Type[BaseException], Tuple[Type[BaseException], ...]]])</code> – Exception or - exceptions that shall propagate if raised by the function, instead of being wrapped in the CallInfo.</p></li></ul></li></ul></li></ul><h2 id="collectreport" tabindex="-1">CollectReport <a class="header-anchor" href="#collectreport" aria-label="Permalink to &quot;CollectReport&quot;">​</a></h2><ul><li><h3 id="final-class-collectreport" tabindex="-1"><code>final class</code> <strong>CollectReport</strong> <a class="header-anchor" href="#final-class-collectreport" aria-label="Permalink to &quot;`final class` **CollectReport**&quot;">​</a></h3><ul><li><em>Bases</em>: <code>BaseReport</code></li></ul><p>Collection report object.</p><p>Reports can contain arbitrary extra attributes.</p><ul><li><p><strong>nodeid</strong>: <code>str</code></p><p>Normalized collection nodeid.</p></li><li><p><strong>outcome</strong>: <code>Literal[&#39;passed&#39;, &#39;failed&#39;, &#39;skipped&#39;]</code></p><p>Test outcome, always one of “passed”, “failed”, “skipped”.</p></li><li><p><strong>longrepr</strong>: <code>Union[None, ExceptionInfo[BaseException], Tuple[str, int, str], str, TerminalRepr]</code></p><p>None or a failure representation.</p></li><li><p><strong>result</strong></p><p>The collected items and collection nodes.</p></li><li><p><strong>sections</strong>: <code>List[Tuple[str, str]]</code></p><p>Tuples of str <code>(heading, content)</code> with extra information for the test report. Used by pytest to add text captured from <code>stdout</code>, <code>stderr</code>, and intercepted logging events. May be used by other plugins to add arbitrary information to reports.</p></li><li><p><code>property</code> <strong>caplog</strong>: <code>str</code></p><p>Return captured log lines, if log capturing is enabled.</p><p><em>New in version 3.5.</em></p></li><li><p><code>property</code> <strong>capstderr</strong>: <code>str</code></p><p>Return captured text from stderr, if capturing is enabled.</p><p><em>New in version 3.0.</em></p></li><li><p><code>property</code> <strong>capstdout</strong>: <code>str</code></p><p>Return captured text from stdout, if capturing is enabled.</p><p><em>New in version 3.0.</em></p></li><li><p><code>property</code> <strong>count_towards_summary</strong>: <code>bool</code></p><p>Experimental Whether this report should be counted towards the totals shown at the end of the test session: “1 passed, 1 failure, etc”.</p><div class="tip custom-block"><p class="custom-block-title">Note</p><p>This function is considered experimental, so beware that it is subject to changes even in patch releases.</p></div></li><li><p><code>property</code> <strong>failed</strong>: <code>bool</code></p><p>Whether the outcome is failed.</p></li><li><p><code>property</code> <strong>fspath</strong>: <code>str</code></p><p>The path portion of the reported node, as a string.</p></li><li><p><code>property</code> <strong>head_line</strong>: <code>Optional[str]</code></p><p>Experimental The head line shown with longrepr output for this report, more commonly during traceback representation during failures:</p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#FFCB6B;">________</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">Test.foo</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">________</span></span></code></pre></div><p>In the example above, the head_line is “Test.foo”.</p><div class="tip custom-block"><p class="custom-block-title">Note</p><p>This function is considered experimental, so beware that it is subject to changes even in patch releases.</p></div></li><li><p><code>property</code> <strong>longreprtext</strong>: <code>str</code></p><p>Read-only property that returns the full string representation of <code>longrepr</code>.</p><p><em>New in version 3.0.</em></p></li><li><p><code>property</code> <strong>passed</strong>: <code>bool</code></p><p>Whether the outcome is passed.</p></li><li><p><code>property</code> <strong>skipped</strong>: <code>bool</code></p><p>Whether the outcome is skipped.</p></li></ul></li></ul><h2 id="config" tabindex="-1">Config <a class="header-anchor" href="#config" aria-label="Permalink to &quot;Config&quot;">​</a></h2><ul><li><h3 id="final-class-config" tabindex="-1"><code>final class</code> <strong>Config</strong> <a class="header-anchor" href="#final-class-config" aria-label="Permalink to &quot;`final class` **Config**&quot;">​</a></h3><p>Access to configuration values, pluginmanager and plugin hooks.</p><ul><li><p><strong>Parameters</strong>:</p><ul><li><p><code>pluginmanager (PytestPluginManager)</code> – A pytest <code>PluginManager</code>.</p></li><li><p><code>invocation_params (InvocationParams)</code> – Object containing parameters regarding the <code>pytest.main()</code> invocation.</p></li></ul></li><li><p><code>final class</code> <strong>InvocationParams</strong>(<code>*, args, plugins, dir</code>)</p><p>Holds parameters passed during <code>pytest.main()</code>.</p><p>The object attributes are read-only.</p><p><em>New in version 5.1.</em></p><div class="tip custom-block"><p class="custom-block-title">Note</p><p>Note that the environment variable <code>PYTEST_ADDOPTS</code> and the <code>addopts</code> ini option are handled by pytest, not being included in the <code>args</code> attribute.</p><p>Plugins accessing <code>InvocationParams</code> must be aware of that.</p></div></li><li><p><strong>args</strong>: <code>Tuple[str, ...]</code></p><p>The command-line arguments as passed to <code>pytest.main()</code>.</p></li><li><p><strong>plugins</strong>: <code>Optional[Sequence[Union[str, object]]]</code></p><p>Extra plugins, might be <code>None</code>.</p></li><li><p><strong>dir</strong>: <code>Path</code></p><p>The directory from which <code>pytest.main()</code> was invoked.</p></li><li><p><code>class</code> <strong>ArgsSource</strong>(<code>value</code>)</p><p>Indicates the source of the test arguments.</p><p><em>New in version 7.2.</em></p><ul><li><p><strong>ARGS</strong> = 1</p><p>Command line arguments.</p></li><li><p><strong>INCOVATION_DIR</strong> = 2</p><p>Invocation directory.</p></li><li><p><strong>TESTPATHS</strong> = 3</p><p>‘testpaths’ configuration value.</p></li></ul></li><li><p><strong>option</strong></p><p>Access to command line option as attributes.</p><ul><li><em>Type</em>: <code>argparse.Namespace</code></li></ul></li><li><p><strong>invocation_params</strong></p><p>The parameters with which pytest was invoked.</p><p><em>Type</em>: <code>InvocationParams</code></p></li><li><p><strong>pluginmanager</strong></p><p>The plugin manager handles plugin registration and hook invocation.</p><p><em>Type</em>: <code>PytestPluginManager</code></p></li><li><p><strong>stash</strong></p><p>A place where plugins can store information on the config for their own use.</p><p><em>Type</em>: <code>Stash</code></p></li><li><p><code>property</code> <strong>rootpath</strong>: <code>Path</code></p><p>The path to the <code>rootdir</code>.</p><p><em>Type</em>: <code>pathlib.Path</code></p><p><em>New in version 6.1.</em></p></li><li><p><code>property</code> <strong>inipath</strong>: <code>Optional[Path]</code></p><p>The path to the configfile.</p><p><em>Type</em>: <code>Optional[pathlib.Path]</code></p><p><em>New in version 6.1.</em></p></li><li><p><strong>add_cleanup</strong>(<code>func</code>)</p><p>Add a function to be called when the config object gets out of use (usually coinciding with <code>pytest_unconfigure</code>).</p></li><li><p><code>classmethod</code> <strong>fromdictargs</strong>(<code>option_dict, args</code>)</p><p>Constructor usable for subprocesses.</p></li><li><p><strong>issue_config_time_warning</strong>(<code>warning, stacklevel</code>)</p><p>Issue and handle a warning during the “configure” stage.</p><p>During <code>pytest_configure</code> we can’t capture warnings using the <code>catch_warnings_for_item</code> function because it is not possible to have hook wrappers around <code>pytest_configure</code>.</p><p>This function is mainly intended for plugins that need to issue warnings during <code>pytest_configure</code> (or similar stages).</p><p><em>Parameters</em>:</p><ul><li><p><code>warning (Warning)</code> – The <code>warning</code> instance.</p></li><li><p><code>stacklevel (int)</code> – <code>stacklevel</code> forwarded to <code>warnings.warn</code>.</p></li></ul></li><li><p><strong>addinivalue_line</strong>(<code>name, line</code>)</p><p>Add a line to an ini-file option. The option must have been declared but might not yet be set in which case the line becomes the first line in its value.</p></li><li><p><strong>getini</strong>(<code>name</code>)</p><p>Return configuration value from an ini file.</p><p>If the specified name hasn’t been registered through a prior <code>parser.addini</code> call (usually from a plugin), a ValueError is raised.</p></li><li><p><strong>getoption</strong>(<code>name, default=&lt;NOTSET&gt;, skip=False</code>)</p><p>Return command line option value.</p><p><em>Parameters</em>:</p><ul><li><p><code>name (str)</code> – Name of the option. You may also specify the literal <code>--OPT</code> option instead of the “dest” option name.</p></li><li><p><code>default</code> – Default value if no option of that name exists.</p></li><li><p><code>skip (bool)</code> – If True, raise pytest.skip if option does not exists or has a None value.</p></li></ul></li><li><p><strong>getvalue</strong>(<code>name, path=None</code>)</p><p><em>Deprecated</em>, use <code>getoption()</code> instead.</p></li><li><p><strong>getvalueorskip</strong>(<code>name, path=None</code>)</p><p><em>Deprecated</em>, use <code>getoption(skip=True)</code> instead.</p></li></ul></li></ul><h2 id="exceptioninfo" tabindex="-1">ExceptionInfo <a class="header-anchor" href="#exceptioninfo" aria-label="Permalink to &quot;ExceptionInfo&quot;">​</a></h2><ul><li><h3 id="final-class-exceptioninfo" tabindex="-1"><code>final class</code> <strong>ExceptionInfo</strong> <a class="header-anchor" href="#final-class-exceptioninfo" aria-label="Permalink to &quot;`final class` **ExceptionInfo**&quot;">​</a></h3><p>Wraps <code>sys.exc_info()</code> objects and offers help for navigating the traceback.</p><ul><li><p><code>classmethod</code> <strong>from_exception</strong>(<code>exception, exprinfo=None</code>)</p><p>Return an ExceptionInfo for an existing exception.</p><p>The exception must have a non-<code>None</code> <code>__traceback__</code> attribute, otherwise this function fails with an assertion error. This means that the exception must have been raised, or added a traceback with the <code>with_traceback()</code> method.</p><p><em>Parameters</em>:</p><ul><li><code>exprinfo (Optional[str])</code> – A text string helping to determine if we should strip <code>AssertionError</code> from the output. Defaults to the exception <code>message/__str__()</code>.</li></ul><p><em>New in version 7.4.</em></p></li><li><p><code>classmethod</code> <strong>from_exc_info</strong>(<code>exc_info, exprinfo=None</code>)</p><p>Like <code>from_exception()</code>, but using old-style exc_info tuple.</p></li><li><p><code>classmethod</code> <strong>from_current</strong>(<code>exprinfo=None</code>)</p><p>Return an <code>ExceptionInfo</code> matching the current traceback.</p><div class="warning custom-block"><p class="custom-block-title">Warning</p><p>Experimental API</p></div><p><em>Parameters</em>:</p><ul><li><code>exprinf (Optional[str])</code> – A text string helping to determine if we should strip - <code>AssertionError</code> from the output. Defaults to the exception <code>message/__str__()</code>.</li></ul></li><li><p><code>classmethod</code> <strong>for_later</strong>()</p><p>Return an unfilled ExceptionInfo.</p></li><li><p><strong>fill_unfilled</strong>(<code>exc_info</code>)</p><p>Fill an unfilled ExceptionInfo created with <code>for_later()</code>.</p></li><li><p><code>property</code> <strong>type</strong>: <code>Type[E]</code></p><p>The exception class.</p></li><li><p><code>property</code> <strong>value</strong>: <code>E</code></p><p>The exception value.</p></li><li><p><code>property</code> <strong>tb</strong>: <code>TracebackType</code></p><p>The exception raw traceback.</p></li><li><p><code>property</code> <strong>typename</strong>: <code>str</code></p><p>The type name of the exception.</p></li><li><p><code>property</code> <strong>traceback</strong>: <code>Traceback</code></p><p>The traceback.</p></li><li><p><strong>exconly</strong>(<code>tryshort=False</code>)</p><p>Return the exception as a string.</p><p>When ‘tryshort’ resolves to True, and the exception is an AssertionError, only the actual exception part of the exception representation is returned (so ‘AssertionError: ‘ is removed from the beginning).</p></li><li><p><strong>errisinstance</strong>(<code>exc</code>)</p><p>Return True if the exception is an instance of exc.</p><p>Consider using <code>isinstance(excinfo.value, exc)</code> instead.</p></li><li><p><strong>getrepr</strong>(<code>showlocals=False, style=&#39;long&#39;, abspath=False, tbfilter=True, funcargs=False, - truncate_locals=True, chain=True</code>)</p><p>Return <code>str()</code>able representation of this exception info.</p><p><em>Parameters</em>:</p><ul><li><p><code>showlocals (bool)</code> – Show locals per traceback entry. Ignored if <code>style==&quot;native&quot;</code>.</p></li><li><p><code>style (str)</code> – long|short|line|no|native|value traceback style.</p></li><li><p><code>abspath (bool)</code> – If paths should be changed to absolute or left unchanged.</p></li><li><p><code>tbfilter (Union[bool, Callable[[ExceptionInfo[BaseException]], Traceback]])</code> – A filter for traceback entries.</p><ul><li><p>If false, don’t hide any entries.</p></li><li><p>If true, hide internal entries and entries that contain a local variable <code>__tracebackhide__ = True</code>.</p></li><li><p>If a callable, delegates the filtering to the callable.</p></li></ul><p>Ignored if <code>style</code> is <code>&quot;native&quot;</code>.</p></li><li><p><code>funcargs (bool)</code> – Show fixtures (“funcargs” for legacy purposes) per traceback entry.</p></li><li><p><code>truncate_locals (bool)</code> – With <code>showlocals==True</code>, make sure locals can be safely represented as strings.</p></li><li><p><code>chain (bool)</code> – If chained exceptions in Python 3 should be shown.</p></li></ul><p><em>Changed in version 3.9</em>: Added the <code>chain</code> parameter.</p></li><li><p><strong>match</strong>(<code>regexp</code>)</p><p>Check whether the regular expression <code>regexp</code> matches the string representation of the exception using <code>re.search()</code>.</p><p>If it matches <code>True</code> is returned, otherwise an <code>AssertionError</code> is raised.</p></li></ul></li></ul><h2 id="exitcode" tabindex="-1">ExitCode <a class="header-anchor" href="#exitcode" aria-label="Permalink to &quot;ExitCode&quot;">​</a></h2><ul><li><h3 id="final-class-exitcode-value" tabindex="-1"><code>final class</code> <strong>ExitCode</strong>(<code>value</code>) <a class="header-anchor" href="#final-class-exitcode-value" aria-label="Permalink to &quot;`final class` **ExitCode**(`value`)&quot;">​</a></h3><p>Encodes the valid exit codes by pytest.</p><p>Currently users and plugins may supply other exit codes as well.</p><p><em>New in version 5.0.</em></p><ul><li><p><strong>OK</strong> = 0</p><p>Tests passed.</p></li><li><p><strong>TESTS_FAILED</strong> = 1</p><p>Tests failed.</p></li><li><p><strong>INTERRUPTED</strong> = 2</p><p>pytest was interrupted.</p></li><li><p><strong>INTERNAL_ERROR</strong> = 3</p><p>An internal error got in the way.</p></li><li><p><strong>USAGE_ERROR</strong> = 4</p><p>pytest was misused.</p></li><li><p><strong>NO_TESTS_COLLECTED</strong> = 5</p><p>pytest couldn’t find tests.</p></li></ul></li></ul><h2 id="fixturedef" tabindex="-1">FixtureDef <a class="header-anchor" href="#fixturedef" aria-label="Permalink to &quot;FixtureDef&quot;">​</a></h2><ul><li><h3 id="final-class-fixturedef" tabindex="-1"><code>final class</code> <strong>FixtureDef</strong> <a class="header-anchor" href="#final-class-fixturedef" aria-label="Permalink to &quot;`final class` **FixtureDef**&quot;">​</a></h3><ul><li><em>Bases</em>: <code>Generic[FixtureValue]</code></li></ul><p>A container for a fixture definition.</p><p><em>Note</em>: At this time, only explicitly documented fields and methods are considered public stable API.</p><ul><li><p><code>property</code> <strong>scope</strong>: <code>Literal[&#39;session&#39;, &#39;package&#39;, &#39;module&#39;, &#39;class&#39;, &#39;function&#39;]</code></p><p>Scope string, one of <code>“function”</code>, <code>“class”</code>,<code> “module”</code>, <code>“package”</code>, <code>“session”</code>.</p></li></ul></li></ul><h2 id="markdecorator" tabindex="-1">MarkDecorator <a class="header-anchor" href="#markdecorator" aria-label="Permalink to &quot;MarkDecorator&quot;">​</a></h2><ul><li><h3 id="class-markdecorator" tabindex="-1"><code>class</code> <strong>MarkDecorator</strong> <a class="header-anchor" href="#class-markdecorator" aria-label="Permalink to &quot;`class` **MarkDecorator**&quot;">​</a></h3><p>A decorator for applying a mark on test functions and classes.</p><p><code>MarkDecorators</code> are created with <code>pytest.mark</code>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">mark1 </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> pytest</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">mark</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">NAME</span><span style="color:#A6ACCD;">              </span><span style="color:#676E95;font-style:italic;"># Simple MarkDecorator</span></span>\n<span class="line"><span style="color:#A6ACCD;">mark2 </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> pytest</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">mark</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">NAME</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">name1</span><span style="color:#89DDFF;">=</span><span style="color:#82AAFF;">value</span><span style="color:#89DDFF;">)</span><span style="color:#A6ACCD;"> </span><span style="color:#676E95;font-style:italic;"># Parametrized MarkDecorator</span></span></code></pre></div><p>and can then be applied as decorators to test functions:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">mark2</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">test_function</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">pass</span></span></code></pre></div><p>When a <code>MarkDecorator</code> is called, it does the following:</p><ol><li><p>If called with a single class as its only positional argument and no additional keyword arguments, it attaches the mark to the class so it gets applied automatically to all test cases found in that class.</p></li><li><p>If called with a single function as its only positional argument and no additional keyword arguments, it attaches the mark to the function, containing all the arguments already stored internally in the <code>MarkDecorator</code>.</p></li><li><p>When called in any other case, it returns a new <code>MarkDecorator</code> instance with the original <code>MarkDecorator’s</code> content updated with the arguments passed to this call.</p></li></ol><p><em>Note</em>: The rules above prevent a <code>MarkDecorator</code> from storing only a single function or class reference as its positional argument with no additional keyword or positional arguments. You can work around this by using <code>with_args()</code>.</p><ul><li><p><code>property</code> <strong>name</strong>: <code>str</code></p><p>Alias for <code>mark.name</code>.</p></li><li><p><code>property</code> <strong>args</strong>: <code>Tuple[Any, ...]</code></p><p>Alias for <code>mark.args</code>.</p></li><li><p><code>property</code> <strong>kwargs</strong>: <code>Mapping[str, Any]</code></p><p>Alias for <code>mark.kwargs</code>.</p></li><li><p><strong>with_args</strong>(<code>*args, **kwargs</code>)</p><p>Return a <code>MarkDecorator</code> with extra arguments added.</p><p>Unlike calling the MarkDecorator, <code>with_args()</code> can be used even if the sole argument is a <code>callable/class</code>.</p></li></ul></li></ul><h2 id="markgenerator" tabindex="-1">MarkGenerator <a class="header-anchor" href="#markgenerator" aria-label="Permalink to &quot;MarkGenerator&quot;">​</a></h2><ul><li><h3 id="final-class-markgenerator" tabindex="-1"><code>final class</code> <strong>MarkGenerator</strong> <a class="header-anchor" href="#final-class-markgenerator" aria-label="Permalink to &quot;`final class` **MarkGenerator**&quot;">​</a></h3><p>Factory for <code>MarkDecorator</code> objects - exposed as a <code>pytest.mark</code> singleton instance.</p><p><em>Example</em>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;font-style:italic;">import</span><span style="color:#A6ACCD;"> pytest</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">pytest</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">mark</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">slowtest</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">test_function</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">   </span><span style="color:#89DDFF;font-style:italic;">pass</span></span></code></pre></div><p>applies a ‘slowtest’ <code>Mark</code> on <code>test_function</code>.</p></li></ul><h2 id="mark" tabindex="-1">Mark <a class="header-anchor" href="#mark" aria-label="Permalink to &quot;Mark&quot;">​</a></h2><ul><li><h3 id="final-class-mark" tabindex="-1"><code>final class</code> <strong>Mark</strong> <a class="header-anchor" href="#final-class-mark" aria-label="Permalink to &quot;`final class` **Mark**&quot;">​</a></h3><p>A pytest mark.</p><ul><li><p><strong>name</strong>: <code>str</code></p><p>Name of the mark.</p></li><li><p><strong>args</strong>: <code>Tuple[Any, ...]</code></p><p>Positional arguments of the mark decorator.</p></li><li><p><strong>kwargs</strong>: <code>Mapping[str, Any]</code></p><p>Keyword arguments of the mark decorator.</p></li><li><p><strong>combined_with</strong>(<code>other</code>)</p><p>Return a new Mark which is a combination of this Mark and another Mark.</p><p>Combines by appending args and merging kwargs.</p><p><em>Parameters</em>:</p><ul><li><code>other (Mark)</code> – The mark to combine with.</li></ul><p><em>Return type</em>: <code>Mark</code></p></li></ul></li></ul><h2 id="metafunc" tabindex="-1">Metafunc <a class="header-anchor" href="#metafunc" aria-label="Permalink to &quot;Metafunc&quot;">​</a></h2><ul><li><h3 id="final-class-metafunc" tabindex="-1"><code>final class</code> <strong>Metafunc</strong> <a class="header-anchor" href="#final-class-metafunc" aria-label="Permalink to &quot;`final class` **Metafunc**&quot;">​</a></h3><p>Objects passed to the <code>pytest_generate_tests</code> hook.</p><p>They help to inspect a test function and to generate tests according to test configuration or values specified in the class or module where a test function is defined.</p><ul><li><p><strong>definition</strong></p><p>Access to the underlying <code>_pytest.python.FunctionDefinition</code>.</p></li><li><p><strong>config</strong></p><p>Access to the <code>pytest.Config</code> object for the test session.</p></li><li><p><strong>module</strong></p><p>The module object where the test function is defined in.</p></li><li><p><strong>function</strong></p><p>Underlying Python test function.</p></li><li><p><strong>fixturenames</strong></p><p>Set of fixture names required by the test function.</p></li><li><p><strong>cls</strong></p><p>Class object where the test function is defined in or <code>None</code>.</p></li><li><p><strong>parametrize</strong>(<code>argnames, argvalues, indirect=False, ids=None, scope=None, *, _param_mark=None</code>)</p><p>Add new invocations to the underlying test function using the list of argvalues for the given argnames. Parametrization is performed during the collection phase. If you need to setup expensive resources see about setting indirect to do it rather than at test setup time.</p><p>Can be called multiple times, in which case each call parametrizes all previous parametrizations, e.g.</p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#FFCB6B;">unparametrized:</span><span style="color:#A6ACCD;">         </span><span style="color:#C3E88D;">t</span></span>\n<span class="line"><span style="color:#FFCB6B;">parametrize</span><span style="color:#A6ACCD;"> [</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">x</span><span style="color:#89DDFF;">&quot;</span><span style="color:#A6ACCD;">, </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">y</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">]:</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">t[x],</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">t[y]</span></span>\n<span class="line"><span style="color:#FFCB6B;">parametrize</span><span style="color:#A6ACCD;"> [1, </span><span style="color:#F78C6C;">2</span><span style="color:#C3E88D;">]:</span><span style="color:#A6ACCD;">     </span><span style="color:#C3E88D;">t[x-1],</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">t[x-2],</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">t[y-1],</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">t[y-2]</span></span></code></pre></div><p><em>Parameters</em>:</p><ul><li><p><code>argnames (Union[str, Sequence[str]])</code> – A comma-separated string denoting one or more argument names, or a list/tuple of argument strings.</p></li><li><p><code>argvalues (Iterable[Union[ParameterSet, Sequence[object], object]])</code> – The list of argvalues determines how often a test is invoked with different argument values. If only one argname was specified argvalues is a list of values. If N argnames were specified, argvalues must be a list of N-tuples, where each tuple-element specifies a value for its respective argname.</p></li><li><p><code>indirect (Union[bool, Sequence[str]])</code> – A list of arguments’ names (subset of argnames) or a boolean. If True the list contains all names from the argnames. Each argvalue corresponding to an argname in this list will be passed as request.param to its respective argname fixture function so that it can perform more expensive setups during the setup phase of a test rather than at collection time.</p></li><li><p><code>ids (Optional[Union[Iterable[Optional[object]], Callable[[Any], Optional[object]]]])</code> – Sequence of (or generator for) ids for <code>argvalues</code>, or a callable to return part of the id for each argvalue. With sequences (and generators like <code>itertools.count()</code>) the returned ids should be of type <code>string</code>, <code>int</code>, <code>float</code>, <code>bool</code>, or <code>None</code>. They are mapped to the corresponding index in <code>argvalues</code>. <code>None</code> means to use the auto-generated id. If it is a callable it will be called for each entry in <code>argvalues</code>, and the return value is used as part of the auto-generated id for the whole set (where parts are joined with dashes (“-“)). This is useful to provide more specific ids for certain items, e.g. dates. Returning <code>None</code> will use an auto-generated id. If no ids are provided they will be generated automatically from the argvalues.</p></li><li><p><code>scope (Optional[Literal[&#39;session&#39;, &#39;package&#39;, &#39;module&#39;, &#39;class&#39;, &#39;function&#39;]])</code> – If specified it denotes the scope of the parameters. The scope is used for grouping tests by parameter instances. It will also override any fixture-function defined scope, allowing to set a dynamic scope using test context or configuration.</p></li></ul></li></ul></li></ul><h2 id="parser" tabindex="-1">Parser <a class="header-anchor" href="#parser" aria-label="Permalink to &quot;Parser&quot;">​</a></h2><ul><li><h3 id="final-class-parser" tabindex="-1"><code>final class</code> <strong>Parser</strong> <a class="header-anchor" href="#final-class-parser" aria-label="Permalink to &quot;`final class` **Parser**&quot;">​</a></h3><p><code>Parser</code> for command line arguments and <code>ini</code>-file values.</p><ul><li><p><strong>Variables</strong>:</p><ul><li><code>extra_info</code> – Dict of generic param -&gt; value to display in case there’s an error processing the command line arguments.</li></ul></li><li><p><strong>getgroup</strong>(<code>name, description=&#39;&#39;, after=None</code>)</p><p>Get (or create) a named option Group.</p><p><em>Parameters</em>:</p><ul><li><p><code>name (str)</code> – Name of the option group.</p></li><li><p><code>description (str)</code> – Long description for –help output.</p></li><li><p><code>after (Optional[str])</code> – Name of another group, used for ordering –help output.</p></li></ul><p><em>Returns</em>: The option group.</p><p><em>Return type</em>: <code>OptionGroup</code></p><p>The returned group object has an <code>addoption</code> method with the same signature as <code>parser.addoption</code> but will be shown in the respective group in the output of <code>pytest --help</code>.</p></li><li><p><strong>addoption</strong>(<code>*opts, **attrs</code>)</p><p>Register a command line option.</p><p><em>Parameters</em>:</p><ul><li><p><code>opts (str)</code> – Option names, can be short or long options.</p></li><li><p><code>attrs (Any)</code> – Same attributes as the argparse library’s <code>add_argument()</code> function accepts.</p></li></ul><p>After command line parsing, options are available on the pytest config object via <code>config.option.NAME</code> where <code>NAME</code> is usually set by passing a <code>dest</code> attribute, for example <code>addoption(&quot;--long&quot;, dest=&quot;NAME&quot;, ...)</code>.</p></li><li><p><strong>parse_known_args</strong>(<code>args, namespace=None</code>)</p><p>Parse the known arguments at this point.</p><p><em>Returns</em>: An argparse namespace object.</p><p><em>Return type</em>: <code>Namespace</code></p></li><li><p><strong>parse_known_and_unknown_args</strong>(<code>args, namespace=None</code>)</p><p>Parse the known arguments at this point, and also return the remaining unknown arguments.</p><p><em>Returns</em>: A tuple containing an argparse namespace object for the known arguments, and a list of the unknown arguments.</p><p><em>Return type</em>: <code>Tuple[Namespace, List[str]]</code></p></li><li><p><strong>addini</strong>(<code>name, help, type=None, default=None</code>)</p><p>Register an ini-file option.</p><p><em>Parameters</em>:</p><ul><li><p><code>name (str)</code> – Name of the ini-variable.</p></li><li><p><code>type (Optional[Literal[&#39;string&#39;, &#39;paths&#39;, &#39;pathlist&#39;, &#39;args&#39;, &#39;linelist&#39;, &#39;bool&#39;]])</code> – Type of the variable. Can be:</p><ul><li><p><code>string</code>: a string</p></li><li><p><code>bool</code>: a boolean</p></li><li><p><code>args</code>: a list of strings, separated as in a shell</p></li><li><p><code>linelist</code>: a list of strings, separated by line breaks</p></li><li><p><code>paths</code>: a list of <code>pathlib.Path</code>, separated as in a shell</p></li><li><p><code>pathlist</code>: a list of <code>py.path</code>, separated as in a shell</p></li></ul><p><em>New in version 7.0</em>: The <code>paths</code> variable type.</p><p>Defaults to <code>string</code> if <code>None</code> or not passed.</p></li><li><p><code>default (Optional[Any])</code> – Default value if no ini-file option exists but is queried.</p></li></ul><p>The value of ini-variables can be retrieved via a call to <code>config.getini(name)</code>.</p></li></ul></li></ul><h2 id="optiongroup" tabindex="-1">OptionGroup <a class="header-anchor" href="#optiongroup" aria-label="Permalink to &quot;OptionGroup&quot;">​</a></h2><ul><li><h3 id="class-optiongroup" tabindex="-1"><code>class</code> <strong>OptionGroup</strong> <a class="header-anchor" href="#class-optiongroup" aria-label="Permalink to &quot;`class` **OptionGroup**&quot;">​</a></h3><p>A group of options shown in its own section.</p><ul><li><p><strong>addoption</strong>(<code>*opts, **attrs</code>)</p><p>Add an option to this group.</p><p>If a shortened version of a long option is specified, it will be suppressed in the help. <code>addoption(&#39;--twowords&#39;, &#39;--two-words&#39;)</code> results in help showing <code>--two-words</code> only, but <code>--twowords</code> gets accepted and the automatic destination is in <code>args.twowords</code>.</p><p><em>Parameters</em>:</p><ul><li><p><code>opts (str)</code> – Option names, can be short or long options.</p></li><li><p><code>attrs (Any)</code> – Same attributes as the argparse library’s <code>add_argument()</code> function accepts.</p></li></ul></li></ul></li></ul><h2 id="pytestpluginmanager" tabindex="-1">PytestPluginManager <a class="header-anchor" href="#pytestpluginmanager" aria-label="Permalink to &quot;PytestPluginManager&quot;">​</a></h2><ul><li><h3 id="final-class-pytestpluginmanager" tabindex="-1"><code>final class</code> <strong>PytestPluginManager</strong> <a class="header-anchor" href="#final-class-pytestpluginmanager" aria-label="Permalink to &quot;`final class` **PytestPluginManager**&quot;">​</a></h3><ul><li><p><em>Bases</em>: <code>PluginManager</code></p></li><li><p>A <code>pluggy.PluginManager</code> with additional pytest-specific functionality:</p><ul><li><p>Loading plugins from the command line, <code>PYTEST_PLUGINS</code> env variable and <code>pytest_plugins</code> global variables found in plugins being loaded.</p></li><li><p><code>conftest.py</code> loading during start-up.</p></li></ul></li><li><p><strong>parse_hookimpl_opts</strong>(<code>plugin, name</code>)</p></li><li><p><strong>parse_hookspec_opts</strong>(<code>module_or_class, name</code>)</p></li><li><p><strong>register</strong>(<code>plugin, name=None</code>)</p><p>Register a plugin and return its name.</p><p>If a name is not specified, a name is generated using <code>get_canonical_name()</code>.</p><p>If the name is blocked from registering, returns <code>None</code>.</p><p>If the plugin is already registered, raises a <code>ValueError</code>.</p></li><li><p><strong>getplugin</strong>(<code>name</code>)</p></li><li><p><strong>hasplugin</strong>(<code>name</code>)</p><p>Return whether a plugin with the given name is registered.</p></li><li><p><strong>import_plugin</strong>(<code>modname, consider_entry_points=False</code>)</p><p>Import a plugin with <code>modname</code>.</p><p>If <code>consider_entry_points</code> is <code>True</code>, entry point names are also considered to find a plugin.</p></li><li><p><strong>add_hookcall_monitoring</strong>(<code>before, after</code>)</p><p>Add before/after tracing functions for all hooks.</p><p>Returns an undo function which, when called, removes the added tracers.</p><p><code>before(hook_name, hook_impls, kwargs)</code> will be called ahead of all hook calls and receive a - hookcaller instance, a list of HookImpl instances and the keyword arguments for the hook call.</p><p><code>after(outcome, hook_name, hook_impls, kwargs)</code> receives the same arguments as <code>before</code> but also a - <code>_Result</code> object which represents the result of the overall hook call.</p></li><li><p><strong>add_hookspecs</strong>(<code>module_or_class</code>)</p><p>Add new hook specifications defined in the given <code>module_or_class</code>.</p><p>Functions are recognized as hook specifications if they have been decorated with a matching <code>HookspecMarker</code>.</p></li><li><p><strong>check_pending</strong>()</p><p>Verify that all hooks which have not been verified against a hook specification are optional, otherwise raise <code>PluginValidationError</code>.</p></li><li><p><strong>enable_tracing</strong>()</p><p>Enable tracing of hook calls.</p><p>Returns an undo function which, when called, removes the added tracing.</p></li><li><p><strong>get_canonical_name</strong>(<code>plugin</code>)</p><p>Return a canonical name for a plugin object.</p><p>Note that a plugin may be registered under a different name specified by the caller of <code>register(plugin, name)</code>. To obtain the name of n registered plugin use <code>get_name(plugin)</code> instead.</p></li><li><p><strong>get_hookcallers</strong>(<code>plugin</code>)</p><p>Get all hook callers for the specified plugin.</p></li><li><p><strong>get_name</strong>(<code>plugin</code>)</p><p>Return the name the plugin is registered under, or <code>None</code> if is isn’t.</p></li><li><p><strong>get_plugin</strong>(<code>name</code>)</p><p>Return the plugin registered under the given name, if any.</p></li><li><p><strong>get_plugins</strong>()</p><p>Return a set of all registered plugin objects.</p></li><li><p><strong>has_plugin</strong>(<code>name</code>)</p><p>Return whether a plugin with the given name is registered.</p></li><li><p><strong>is_blocked</strong>(<code>name</code>)</p><p>Return whether the given plugin name is blocked.</p></li><li><p><strong>is_registered</strong>(<code>plugin</code>)</p><p>Return whether the plugin is already registered.</p></li><li><p><strong>list_name_plugin</strong>()</p><p>Return a list of (name, plugin) pairs for all registered plugins.</p></li><li><p><strong>list_plugin_distinfo</strong>()</p><p>Return a list of (plugin, distinfo) pairs for all setuptools-registered plugins.</p></li><li><p><strong>load_setuptools_entrypoints</strong>(<code>group, name=None</code>)</p><p>Load modules from querying the specified setuptools <code>group</code>.</p><p><em>Parameters</em>:</p><ul><li><p><code>group (str)</code> – Entry point group to load plugins.</p></li><li><p><code>name (str)</code> – If given, loads only plugins with the given <code>name</code>.</p></li></ul><p><em>Return type</em>: <code>int</code></p><p><em>Returns</em>: The number of plugins loaded by this call.</p></li><li><p><strong>set_blocked</strong>(<code>name</code>)</p><p>Block registrations of the given name, unregister if already registered.</p></li><li><p><strong>subset_hook_caller</strong>(<code>name, remove_plugins</code>)</p><p>Return a proxy <code>_hooks._HookCaller</code> instance for the named method which manages calls to all registered plugins except the ones from <code>remove_plugins</code>.</p></li><li><p><strong>unregister</strong>(<code>plugin=None, name=None</code>)</p><p>Unregister a plugin and all of its hook implementations.</p><p>The plugin can be specified either by the plugin object or the plugin name. If both are specified, they must agree.</p></li></ul></li></ul><h2 id="testreport" tabindex="-1">TestReport <a class="header-anchor" href="#testreport" aria-label="Permalink to &quot;TestReport&quot;">​</a></h2><ul><li><h3 id="final-class-testreport" tabindex="-1"><code>final class</code> <strong>TestReport</strong> <a class="header-anchor" href="#final-class-testreport" aria-label="Permalink to &quot;`final class` **TestReport**&quot;">​</a></h3><ul><li><em>Bases</em>: <code>BaseReport</code></li></ul><p>Basic test report object (also used for setup and teardown calls if they fail).</p><p>Reports can contain arbitrary extra attributes.</p><ul><li><p><strong>nodeid</strong>: <code>str</code></p><p>Normalized collection nodeid.</p></li><li><p><strong>location</strong>: <code>Tuple[str, Optional[int], str]</code></p><p>A <code>(filesystempath, lineno, domaininfo)</code> tuple indicating the actual location of a test item - it might be different from the collected one e.g. if a method is inherited from a different module. The filesystempath may be relative to <code>config.rootdir</code>. The line number is 0-based.</p></li><li><p><strong>keywords</strong>: <code>Mapping[str, Any]</code></p><p>A name -&gt; value dictionary containing all keywords and markers associated with a test invocation.</p></li><li><p><strong>outcome</strong>: <code>Literal[&#39;passed&#39;, &#39;failed&#39;, &#39;skipped&#39;]</code></p><p>Test outcome, always one of “passed”, “failed”, “skipped”.</p></li><li><p><strong>longrepr</strong>: <code>Union[None, ExceptionInfo[BaseException], Tuple[str, int, str], str, TerminalRepr]</code></p><p>None or a failure representation.</p></li><li><p><strong>when</strong>: <code>Optional[str]</code></p><p>One of ‘setup’, ‘call’, ‘teardown’ to indicate runtest phase.</p></li><li><p><strong>user_properties</strong></p><p>User properties is a list of tuples (name, value) that holds user defined properties of the test.</p></li><li><p><strong>sections</strong>: <code>List[Tuple[str, str]]</code></p><p>Tuples of str <code>(heading, content)</code> with extra information for the test report. Used by pytest to add text captured from <code>stdout</code>, <code>stderr</code>, and intercepted logging events. May be used by other plugins to add arbitrary information to reports.</p></li><li><p><strong>duration</strong>: <code>float</code></p><p>Time it took to run just the test.</p></li><li><p><strong>start</strong>: <code>float</code></p><p>The system time when the call started, in seconds since the epoch.</p></li><li><p><strong>stop</strong>: <code>float</code></p><p>The system time when the call ended, in seconds since the epoch.</p></li><li><p><code>classmethod</code> <strong>from_item_and_call</strong>(<code>item, call</code>)</p><p>Create and fill a TestReport with standard item and call info.</p><p><em>Parameters</em>:</p><ul><li><p><code>item (Item)</code> – The item.</p></li><li><p><code>call (CallInfo[None])</code> – The call info.</p></li></ul></li><li><p><code>property</code> <strong>caplog</strong>: <code>str</code></p><p>Return captured log lines, if log capturing is enabled.</p><p><em>New in version 3.5.</em></p></li><li><p><code>property</code> <strong>capstderr</strong>: <code>str</code></p><p>Return captured text from <code>stderr</code>, if capturing is enabled.</p><p><em>New in version 3.0.</em></p></li><li><p><code>property</code> <strong>capstdout</strong>: <code>str</code></p><p>Return captured text from <code>stdout</code>, if capturing is enabled.</p><p><em>New in version 3.0.</em></p></li><li><p><code>property</code> <strong>count_towards_summary</strong>: <code>bool</code></p><p>Experimental Whether this report should be counted towards the totals shown at the end of the test session: “1 passed, 1 failure, etc”.</p><div class="tip custom-block"><p class="custom-block-title">Note</p><p>This function is considered experimental, so beware that it is subject to changes even in patch releases.</p></div></li><li><p><code>property</code> <strong>failed</strong>: <code>bool</code></p><p>Whether the outcome is failed.</p></li><li><p><code>property</code> <strong>fspath</strong>: <code>str</code></p><p>The path portion of the reported node, as a string.</p></li><li><p><code>property</code> <strong>head_line</strong>: <code>Optional[str]</code></p><p>Experimental The head line shown with longrepr output for this report, more commonly during traceback representation during failures:</p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#FFCB6B;">________</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">Test.foo</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">________</span></span></code></pre></div><p>In the example above, the head_line is “Test.foo”.</p><div class="tip custom-block"><p class="custom-block-title">Note</p><p>This function is considered experimental, so beware that it is subject to changes even in patch releases.</p></div></li><li><p><code>property</code> <strong>longreprtext</strong>: <code>str</code></p><p>Read-only property that returns the full string representation of <code>longrepr</code>.</p><p><em>New in version 3.0.</em></p></li><li><p><code>property</code> <strong>passed</strong>: <code>bool</code></p><p>Whether the outcome is passed.</p></li><li><p><code>property</code> <strong>skipped</strong>: <code>bool</code></p><p>Whether the outcome is skipped.</p></li></ul></li></ul><h2 id="testshortlogreport" tabindex="-1">TestShortLogReport <a class="header-anchor" href="#testshortlogreport" aria-label="Permalink to &quot;TestShortLogReport&quot;">​</a></h2><ul><li><h3 id="class-testshortlogreport" tabindex="-1"><code>class</code> <strong>TestShortLogReport</strong> <a class="header-anchor" href="#class-testshortlogreport" aria-label="Permalink to &quot;`class` **TestShortLogReport**&quot;">​</a></h3><p>Used to store the test status result category, shortletter and verbose word. For example <code>&quot;rerun&quot;, &quot;R&quot;, (&quot;RERUN&quot;, {&quot;yellow&quot;: True})</code>.</p><ul><li><p><strong>Variables</strong>:</p><ul><li><p><code>category</code> – The class of result, for example <code>“passed”</code>, <code>“skipped”</code>, <code>“error”</code>, or the empty string.</p></li><li><p><code>letter</code> – The short letter shown as testing progresses, for example <code>&quot;.&quot;</code>, <code>&quot;s&quot;</code>, <code>&quot;E&quot;</code>, or the empty string.</p></li><li><p><code>word</code> – Verbose word is shown as testing progresses in verbose mode, for example <code>&quot;PASSED&quot;</code>, <code>&quot;SKIPPED&quot;</code>, <code>&quot;ERROR&quot;</code>, or the empty string.</p></li></ul></li><li><p><strong>category</strong>: <code>str</code></p><p>Alias for field number 0</p></li><li><p><strong>letter</strong>: <code>str</code></p><p>Alias for field number 1</p></li><li><p><strong>word</strong>: <code>Union[str, Tuple[str, Mapping[str, bool]]]</code></p><p>Alias for field number 2</p></li></ul></li></ul><h2 id="result" tabindex="-1">_Result <a class="header-anchor" href="#result" aria-label="Permalink to &quot;_Result&quot;">​</a></h2><p>Result object used within <a href="/python/pytest/how_to_guides/hook_func#hook-wrappers-executing-around-other-hooks">hook wrappers</a>, see <a href="https://pluggy.readthedocs.io/en/stable/api_reference.html#pluggy._callers._Result" target="_blank" rel="noreferrer">_Result in the pluggy documentation</a> for more information.</p><h2 id="stash" tabindex="-1">Stash <a class="header-anchor" href="#stash" aria-label="Permalink to &quot;Stash&quot;">​</a></h2><ul><li><h3 id="class-stash" tabindex="-1"><code>class</code> <strong>Stash</strong> <a class="header-anchor" href="#class-stash" aria-label="Permalink to &quot;`class` **Stash**&quot;">​</a></h3><p><code>Stash</code> is a type-safe heterogeneous mutable mapping that allows keys and value types to be defined separately from where it (the <code>Stash</code>) is created.</p><p>Usually you will be given an object which has a <code>Stash</code>, for example <code>Config</code> or a <code>Node</code>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">stash</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> Stash </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> some_object</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">stash</span></span></code></pre></div><p>If a module or plugin wants to store data in this <code>Stash</code>, it creates <code>StashKeys</code> for its keys (at the module level):</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#676E95;font-style:italic;"># At the top-level of the module</span></span>\n<span class="line"><span style="color:#A6ACCD;">some_str_key </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> StashKey</span><span style="color:#89DDFF;">[</span><span style="color:#FFCB6B;">str</span><span style="color:#89DDFF;">]()</span></span>\n<span class="line"><span style="color:#A6ACCD;">some_bool_key </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> StashKey</span><span style="color:#89DDFF;">[</span><span style="color:#FFCB6B;">bool</span><span style="color:#89DDFF;">]()</span></span></code></pre></div><p>To store information:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#676E95;font-style:italic;"># Value type must match the key.</span></span>\n<span class="line"><span style="color:#A6ACCD;">stash</span><span style="color:#89DDFF;">[</span><span style="color:#A6ACCD;">some_str_key</span><span style="color:#89DDFF;">]</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">value</span><span style="color:#89DDFF;">&quot;</span></span>\n<span class="line"><span style="color:#A6ACCD;">stash</span><span style="color:#89DDFF;">[</span><span style="color:#A6ACCD;">some_bool_key</span><span style="color:#89DDFF;">]</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">True</span></span></code></pre></div><p>To retrieve the information:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#676E95;font-style:italic;"># The static type of some_str is str.</span></span>\n<span class="line"><span style="color:#A6ACCD;">some_str </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> stash</span><span style="color:#89DDFF;">[</span><span style="color:#A6ACCD;">some_str_key</span><span style="color:#89DDFF;">]</span></span>\n<span class="line"><span style="color:#676E95;font-style:italic;"># The static type of some_bool is bool.</span></span>\n<span class="line"><span style="color:#A6ACCD;">some_bool </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> stash</span><span style="color:#89DDFF;">[</span><span style="color:#A6ACCD;">some_bool_key</span><span style="color:#89DDFF;">]</span></span></code></pre></div><ul><li><p><strong><strong>setitem</strong></strong>(<code>key, value</code>)</p><p>Set a value for key.</p></li><li><p><strong><strong>getitem</strong></strong>(<code>key</code>)</p><p>Get the value for key.</p><p>Raises <code>KeyError</code> if the key wasn’t set before.</p></li><li><p><strong>get</strong>(<code>key, default</code>)</p><p>Get the value for key, or return default if the key wasn’t set before.</p></li><li><p><strong>setdefault</strong>(<code>key, default</code>)</p><p>Return the value of key if already set, otherwise set the value of key to default and return - default.</p></li><li><p><strong><strong>delitem</strong></strong>(<code>key</code>)</p><p>Delete the value for key.</p><p>Raises <code>KeyError</code> if the key wasn’t set before.</p></li><li><p><strong><strong>contains</strong></strong>(<code>key</code>)</p><p>Return whether key was set.</p></li><li><p><strong><strong>len</strong></strong>()</p><p>Return how many items exist in the stash.</p></li></ul></li><li><h3 id="class-stashkey" tabindex="-1"><code>class</code> <strong>StashKey</strong> <a class="header-anchor" href="#class-stashkey" aria-label="Permalink to &quot;`class` **StashKey**&quot;">​</a></h3><ul><li><em>Bases</em>: <code>Generic[T]</code></li></ul><p><code>StashKey</code> is an object used as a key to a <code>Stash</code>.</p><p>A <code>StashKey</code> is associated with the type <code>T</code> of the value of the key.</p><p>A <code>StashKey</code> is unique and cannot conflict with another key.</p></li></ul>',36),r=[n];function l(i,p,c,d,u,h){return o(),t("div",null,r)}const f=e(a,[["render",l]]);export{m as __pageData,f as default};
