import{_ as e,o as s,c as a,Q as n}from"./chunks/framework.01af844e.js";const E=JSON.parse('{"title":"The Request Context","description":"","frontmatter":{},"headers":[],"relativePath":"python/flask/user_guide/request_context.md","filePath":"python/flask/user_guide/request_context.md","lastUpdated":1693328004000}'),t={name:"python/flask/user_guide/request_context.md"},o=n(`<h1 id="the-request-context" tabindex="-1">The Request Context <a class="header-anchor" href="#the-request-context" aria-label="Permalink to &quot;The Request Context {#the-request-context}&quot;">​</a></h1><p>The request context keeps track of the request-level data during a request. Rather than passing the request object to each function that runs during a request, the <code>request</code> and <code>session</code> proxies are accessed instead.</p><p>This is similar to <a href="/python/flask/user_guide/app_context#the-application-context">The Application Context</a>, which keeps track of the application-level data independent of a request. A corresponding application context is pushed when a request context is pushed.</p><h2 id="purpose-of-the-context" tabindex="-1">Purpose of the Context <a class="header-anchor" href="#purpose-of-the-context" aria-label="Permalink to &quot;Purpose of the Context {#purpose-of-the-context}&quot;">​</a></h2><p>When the <code>Flask</code> application handles a request, it creates a <code>Request</code> object based on the environment it received from the WSGI server. Because a worker (thread, process, or coroutine depending on the server) handles only one request at a time, the request data can be considered global to that worker during that request. Flask uses the term <code>context local</code> for this.</p><p>Flask automatically pushes a request context when handling a request. View functions, error handlers, and other functions that run during a request will have access to the <code>request</code> proxy, which points to the request object for the current request.</p><h2 id="lifetime-of-the-context" tabindex="-1">Lifetime of the Context <a class="header-anchor" href="#lifetime-of-the-context" aria-label="Permalink to &quot;Lifetime of the Context {#lifetime-of-the-context}&quot;">​</a></h2><p>When a Flask application begins handling a request, it pushes a request context, which also pushes an <a href="/python/flask/user_guide/app_context#the-application-context">app context</a>. When the request ends it pops the request context then the application context.</p><p>The context is unique to each thread (or other worker type). <code>request</code> cannot be passed to another thread, the other thread has a different context space and will not know about the request the parent thread was pointing to.</p><p>Context locals are implemented using Python’s <code>contextvars</code> and Werkzeug’s <code>LocalProxy</code>. Python manages the lifetime of context vars automatically, and local proxy wraps that low-level interface to make the data easier to work with.</p><h2 id="manually-push-a-context" tabindex="-1">Manually Push a Context <a class="header-anchor" href="#manually-push-a-context" aria-label="Permalink to &quot;Manually Push a Context {#manually-push-a-context}&quot;">​</a></h2><p>If you try to access <code>request</code>, or anything that uses it, outside a request context, you’ll get this error message:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#e1e4e8;">RuntimeError: Working outside of request context.</span></span>
<span class="line"><span style="color:#e1e4e8;"></span></span>
<span class="line"><span style="color:#e1e4e8;">This typically means that you attempted to use functionality that</span></span>
<span class="line"><span style="color:#e1e4e8;">needed an active HTTP request. Consult the documentation on testing</span></span>
<span class="line"><span style="color:#e1e4e8;">for information about how to avoid this problem.</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">RuntimeError: Working outside of request context.</span></span>
<span class="line"><span style="color:#24292e;"></span></span>
<span class="line"><span style="color:#24292e;">This typically means that you attempted to use functionality that</span></span>
<span class="line"><span style="color:#24292e;">needed an active HTTP request. Consult the documentation on testing</span></span>
<span class="line"><span style="color:#24292e;">for information about how to avoid this problem.</span></span></code></pre></div><p>This should typically only happen when testing code that expects an active request. One option is to use the <code>test client</code> to simulate a full request. Or you can use <code>test_request_context()</code> in a <code>with</code> block, and everything that runs in the block will have access to <code>request</code>, populated with your test data.</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#F97583;">def</span><span style="color:#E1E4E8;"> </span><span style="color:#B392F0;">generate_report</span><span style="color:#E1E4E8;">(year):</span></span>
<span class="line"><span style="color:#E1E4E8;">    </span><span style="color:#79B8FF;">format</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> request.args.get(</span><span style="color:#9ECBFF;">&quot;format&quot;</span><span style="color:#E1E4E8;">)</span></span>
<span class="line"><span style="color:#E1E4E8;">    </span><span style="color:#79B8FF;">...</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F97583;">with</span><span style="color:#E1E4E8;"> app.test_request_context(</span></span>
<span class="line"><span style="color:#E1E4E8;">    </span><span style="color:#9ECBFF;">&quot;/make_report/2017&quot;</span><span style="color:#E1E4E8;">, </span><span style="color:#FFAB70;">query_string</span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;">{</span><span style="color:#9ECBFF;">&quot;format&quot;</span><span style="color:#E1E4E8;">: </span><span style="color:#9ECBFF;">&quot;short&quot;</span><span style="color:#E1E4E8;">}</span></span>
<span class="line"><span style="color:#E1E4E8;">):</span></span>
<span class="line"><span style="color:#E1E4E8;">    generate_report()</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#D73A49;">def</span><span style="color:#24292E;"> </span><span style="color:#6F42C1;">generate_report</span><span style="color:#24292E;">(year):</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#005CC5;">format</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> request.args.get(</span><span style="color:#032F62;">&quot;format&quot;</span><span style="color:#24292E;">)</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#005CC5;">...</span></span>
<span class="line"></span>
<span class="line"><span style="color:#D73A49;">with</span><span style="color:#24292E;"> app.test_request_context(</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#032F62;">&quot;/make_report/2017&quot;</span><span style="color:#24292E;">, </span><span style="color:#E36209;">query_string</span><span style="color:#D73A49;">=</span><span style="color:#24292E;">{</span><span style="color:#032F62;">&quot;format&quot;</span><span style="color:#24292E;">: </span><span style="color:#032F62;">&quot;short&quot;</span><span style="color:#24292E;">}</span></span>
<span class="line"><span style="color:#24292E;">):</span></span>
<span class="line"><span style="color:#24292E;">    generate_report()</span></span></code></pre></div><p>If you see that error somewhere else in your code not related to testing, it most likely indicates that you should move that code into a view function.</p><p>For information on how to use the request context from the interactive Python shell, see <a href="/python/flask/user_guide/shell#working-with-the-shell">Working with the Shell</a>.</p><h2 id="how-the-context-works" tabindex="-1">How the Context Works <a class="header-anchor" href="#how-the-context-works" aria-label="Permalink to &quot;How the Context Works {#how-the-context-works}&quot;">​</a></h2><p>The <code>Flask.wsgi_app()</code> method is called to handle each request. It manages the contexts during the request. Internally, the request and application contexts work like stacks. When contexts are pushed, the proxies that depend on them are available and point at information from the top item.</p><p>When the request starts, a <code>RequestContext</code> is created and pushed, which creates and pushes an <code>AppContext</code> first if a context for that application is not already the top context. While these contexts are pushed, the <code>current_app</code>, <code>g</code>, <code>request</code>, and <code>session</code> proxies are available to the original thread handling the request.</p><p>Other contexts may be pushed to change the proxies during a request. While this is not a common pattern, it can be used in advanced applications to, for example, do internal redirects or chain different applications together.</p><p>After the request is dispatched and a response is generated and sent, the request context is popped, which then pops the application context. Immediately before they are popped, the <code>teardown_request()</code> and <code>teardown_appcontext()</code> functions are executed. These execute even if an unhandled exception occurred during dispatch.</p><h2 id="callbacks-and-errors" tabindex="-1">Callbacks and Errors <a class="header-anchor" href="#callbacks-and-errors" aria-label="Permalink to &quot;Callbacks and Errors {#callbacks-and-errors}&quot;">​</a></h2><p>Flask dispatches a request in multiple stages which can affect the request, response, and how errors are handled. The contexts are active during all of these stages.</p><p>A <code>Blueprint</code> can add handlers for these events that are specific to the blueprint. The handlers for a blueprint will run if the blueprint owns the route that matches the request.</p><ol><li><p>Before each request, <code>before_request()</code> functions are called. If one of these functions return a value, the other functions are skipped. The return value is treated as the response and the view function is not called.</p></li><li><p>If the <code>before_request()</code> functions did not return a response, the view function for the matched route is called and returns a response.</p></li><li><p>The return value of the view is converted into an actual response object and passed to the <code>after_request()</code> functions. Each function returns a modified or new response object.</p></li><li><p>After the response is returned, the contexts are popped, which calls the <code>teardown_request()</code> and <code>teardown_appcontext()</code> functions. These functions are called even if an unhandled exception was raised at any point above.</p></li></ol><p>If an exception is raised before the teardown functions, Flask tries to match it with an <code>errorhandler()</code> function to handle the exception and return a response. If no error handler is found, or the handler itself raises an exception, Flask returns a generic <code>500 Internal Server Error</code> response. The teardown functions are still called, and are passed the exception object.</p><p>If debug mode is enabled, unhandled exceptions are not converted to a <code>500</code> response and instead are propagated to the WSGI server. This allows the development server to present the interactive debugger with the traceback.</p><h3 id="teardown-callbacks" tabindex="-1">Teardown Callbacks <a class="header-anchor" href="#teardown-callbacks" aria-label="Permalink to &quot;Teardown Callbacks {#teardown-callbacks}&quot;">​</a></h3><p>The teardown callbacks are independent of the request dispatch, and are instead called by the contexts when they are popped. The functions are called even if there is an unhandled exception during dispatch, and for manually pushed contexts. This means there is no guarantee that any other parts of the request dispatch have run first. Be sure to write these functions in a way that does not depend on other callbacks and will not fail.</p><p>During testing, it can be useful to defer popping the contexts after the request ends, so that their data can be accessed in the test function. Use the <code>test_client()</code> as a <code>with</code> block to preserve the contexts until the <code>with</code> block exits.</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#F97583;">from</span><span style="color:#E1E4E8;"> flask </span><span style="color:#F97583;">import</span><span style="color:#E1E4E8;"> Flask, request</span></span>
<span class="line"></span>
<span class="line"><span style="color:#E1E4E8;">app </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> Flask(</span><span style="color:#79B8FF;">__name__</span><span style="color:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="color:#B392F0;">@app.route</span><span style="color:#E1E4E8;">(</span><span style="color:#9ECBFF;">&#39;/&#39;</span><span style="color:#E1E4E8;">)</span></span>
<span class="line"><span style="color:#F97583;">def</span><span style="color:#E1E4E8;"> </span><span style="color:#B392F0;">hello</span><span style="color:#E1E4E8;">():</span></span>
<span class="line"><span style="color:#E1E4E8;">    </span><span style="color:#79B8FF;">print</span><span style="color:#E1E4E8;">(</span><span style="color:#9ECBFF;">&#39;during view&#39;</span><span style="color:#E1E4E8;">)</span></span>
<span class="line"><span style="color:#E1E4E8;">    </span><span style="color:#F97583;">return</span><span style="color:#E1E4E8;"> </span><span style="color:#9ECBFF;">&#39;Hello, World!&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#B392F0;">@app.teardown_request</span></span>
<span class="line"><span style="color:#F97583;">def</span><span style="color:#E1E4E8;"> </span><span style="color:#B392F0;">show_teardown</span><span style="color:#E1E4E8;">(exception):</span></span>
<span class="line"><span style="color:#E1E4E8;">    </span><span style="color:#79B8FF;">print</span><span style="color:#E1E4E8;">(</span><span style="color:#9ECBFF;">&#39;after with block&#39;</span><span style="color:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F97583;">with</span><span style="color:#E1E4E8;"> app.test_request_context():</span></span>
<span class="line"><span style="color:#E1E4E8;">    </span><span style="color:#79B8FF;">print</span><span style="color:#E1E4E8;">(</span><span style="color:#9ECBFF;">&#39;during with block&#39;</span><span style="color:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;"># teardown functions are called after the context with block exits</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F97583;">with</span><span style="color:#E1E4E8;"> app.test_client() </span><span style="color:#F97583;">as</span><span style="color:#E1E4E8;"> client:</span></span>
<span class="line"><span style="color:#E1E4E8;">    client.get(</span><span style="color:#9ECBFF;">&#39;/&#39;</span><span style="color:#E1E4E8;">)</span></span>
<span class="line"><span style="color:#E1E4E8;">    </span><span style="color:#6A737D;"># the contexts are not popped even though the request ended</span></span>
<span class="line"><span style="color:#E1E4E8;">    </span><span style="color:#79B8FF;">print</span><span style="color:#E1E4E8;">(request.path)</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;"># the contexts are popped and teardown functions are called after</span></span>
<span class="line"><span style="color:#6A737D;"># the client with block exits</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#D73A49;">from</span><span style="color:#24292E;"> flask </span><span style="color:#D73A49;">import</span><span style="color:#24292E;"> Flask, request</span></span>
<span class="line"></span>
<span class="line"><span style="color:#24292E;">app </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> Flask(</span><span style="color:#005CC5;">__name__</span><span style="color:#24292E;">)</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6F42C1;">@app.route</span><span style="color:#24292E;">(</span><span style="color:#032F62;">&#39;/&#39;</span><span style="color:#24292E;">)</span></span>
<span class="line"><span style="color:#D73A49;">def</span><span style="color:#24292E;"> </span><span style="color:#6F42C1;">hello</span><span style="color:#24292E;">():</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#005CC5;">print</span><span style="color:#24292E;">(</span><span style="color:#032F62;">&#39;during view&#39;</span><span style="color:#24292E;">)</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#D73A49;">return</span><span style="color:#24292E;"> </span><span style="color:#032F62;">&#39;Hello, World!&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6F42C1;">@app.teardown_request</span></span>
<span class="line"><span style="color:#D73A49;">def</span><span style="color:#24292E;"> </span><span style="color:#6F42C1;">show_teardown</span><span style="color:#24292E;">(exception):</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#005CC5;">print</span><span style="color:#24292E;">(</span><span style="color:#032F62;">&#39;after with block&#39;</span><span style="color:#24292E;">)</span></span>
<span class="line"></span>
<span class="line"><span style="color:#D73A49;">with</span><span style="color:#24292E;"> app.test_request_context():</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#005CC5;">print</span><span style="color:#24292E;">(</span><span style="color:#032F62;">&#39;during with block&#39;</span><span style="color:#24292E;">)</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;"># teardown functions are called after the context with block exits</span></span>
<span class="line"></span>
<span class="line"><span style="color:#D73A49;">with</span><span style="color:#24292E;"> app.test_client() </span><span style="color:#D73A49;">as</span><span style="color:#24292E;"> client:</span></span>
<span class="line"><span style="color:#24292E;">    client.get(</span><span style="color:#032F62;">&#39;/&#39;</span><span style="color:#24292E;">)</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#6A737D;"># the contexts are not popped even though the request ended</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#005CC5;">print</span><span style="color:#24292E;">(request.path)</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;"># the contexts are popped and teardown functions are called after</span></span>
<span class="line"><span style="color:#6A737D;"># the client with block exits</span></span></code></pre></div><h3 id="signals" tabindex="-1">Signals <a class="header-anchor" href="#signals" aria-label="Permalink to &quot;Signals {#signals}&quot;">​</a></h3><p>The following signals are sent:</p><ol><li><p><code>request_started</code> is sent before the <code>before_request()</code> functions are called.</p></li><li><p><code>request_finished</code> is sent after the <code>after_request()</code> functions are called.</p></li><li><p><code>got_request_exception</code> is sent when an exception begins to be handled, but before an <code>errorhandler()</code> is looked up or called.</p></li><li><p><code>request_tearing_down</code> is sent after the <code>teardown_request()</code> functions are called.</p></li></ol><h2 id="notes-on-proxies" tabindex="-1">Notes On Proxies <a class="header-anchor" href="#notes-on-proxies" aria-label="Permalink to &quot;Notes On Proxies {#notes-on-proxies}&quot;">​</a></h2><p>Some of the objects provided by Flask are proxies to other objects. The proxies are accessed in the same way for each worker thread, but point to the unique object bound to each worker behind the scenes as described on this page.</p><p>Most of the time you don’t have to care about that, but there are some exceptions where it is good to know that this object is actually a proxy:</p><ul><li><p>The proxy objects cannot fake their type as the actual object types. If you want to perform instance checks, you have to do that on the object being proxied.</p></li><li><p>The reference to the proxied object is needed in some situations, such as sending <a href="/python/flask/user_guide/signal#signals">Signals</a> or passing data to a background thread.</p></li></ul><p>If you need to access the underlying object that is proxied, use the · method:</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#E1E4E8;">app </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> current_app._get_current_object()</span></span>
<span class="line"><span style="color:#E1E4E8;">my_signal.send(app)</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">app </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> current_app._get_current_object()</span></span>
<span class="line"><span style="color:#24292E;">my_signal.send(app)</span></span></code></pre></div>`,41),p=[o];function l(r,c,i,h,d,u){return s(),a("div",null,p)}const f=e(t,[["render",l]]);export{E as __pageData,f as default};
