import{_ as e,o,c as a,X as t}from"./chunks/framework.b5656a4e.js";const y=JSON.parse('{"title":"API","description":"","frontmatter":{},"headers":[],"relativePath":"python/flask/api_reference/app_obj.md","filePath":"python/flask/api_reference/app_obj.md","lastUpdated":1693404008000}'),s={name:"python/flask/api_reference/app_obj.md"},n=t('<h1 id="api" tabindex="-1">API <a class="header-anchor" href="#api" aria-label="Permalink to &quot;API {#api}&quot;">​</a></h1><p>This part of the documentation covers all the interfaces of Flask. For parts where Flask depends on external libraries, we document the most important right here and provide links to the canonical documentation.</p><h2 id="application-object" tabindex="-1">Application Object <a class="header-anchor" href="#application-object" aria-label="Permalink to &quot;Application Object {#application-object}&quot;">​</a></h2><ul><li><h3 id="class-flask-flask-import-name-static-url-path-none-static-folder-static-static-host-none-host-matching-false-subdomain-matching-false-template-folder-templates-instance-path-none-instance-relative-config-false-root-path-none" tabindex="-1"><code>class</code> flask.Flask(<code>import_name, static_url_path=None, static_folder=&#39;static&#39;, static_host=None, host_matching=False, subdomain_matching=False, template_folder=&#39;templates&#39;, instance_path=None, instance_relative_config=False, root_path=None</code>) <a class="header-anchor" href="#class-flask-flask-import-name-static-url-path-none-static-folder-static-static-host-none-host-matching-false-subdomain-matching-false-template-folder-templates-instance-path-none-instance-relative-config-false-root-path-none" aria-label="Permalink to &quot;`class` flask.Flask(`import_name, static_url_path=None, static_folder=&#39;static&#39;, static_host=None, host_matching=False, subdomain_matching=False, template_folder=&#39;templates&#39;, instance_path=None, instance_relative_config=False, root_path=None`)&quot;">​</a></h3><p>The flask object implements a WSGI application and acts as the central object. It is passed the name of the module or package of the application. Once it is created it will act as a central registry for the view functions, the URL rules, template configuration and much more.</p><p>The name of the package is used to resolve resources from inside the package or the folder the module is contained in depending on if the package parameter resolves to an actual python package (a folder with an <code>__init__.py</code> file inside) or a standard module (just a <code>.py</code> file).</p><p>For more information about resource loading, see <code>open_resource()</code>.</p><p>Usually you create a Flask instance in your main module or in the <code>__init__.py</code> file of your package like this:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;font-style:italic;">from</span><span style="color:#A6ACCD;"> flask </span><span style="color:#89DDFF;font-style:italic;">import</span><span style="color:#A6ACCD;"> Flask</span></span>\n<span class="line"><span style="color:#A6ACCD;">app </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">Flask</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;">__name__</span><span style="color:#89DDFF;">)</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">About the First Parameter</p><p>The idea of the first parameter is to give Flask an idea of what belongs to your application. This name is used to find resources on the filesystem, can be used by extensions to improve debugging information and a lot more.</p><p>So it’s important what you provide there. If you are using a single module, <code>__name__</code> is always the correct value. If you however are using a package, it’s usually recommended to hardcode the name of your package there.</p><p>For example if your application is defined in <code>yourapplication/app.py</code> you should create it with one of the two versions below:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">app </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">Flask</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">yourapplication</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#A6ACCD;">app </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">Flask</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;">__name__</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">split</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">.</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">)[</span><span style="color:#F78C6C;">0</span><span style="color:#89DDFF;">])</span></span></code></pre></div><p>Why is that? The application will work even with <code>__name__</code>, thanks to how resources are looked up. However it will make debugging more painful. Certain extensions can make assumptions based on the import name of your application. For example the Flask-SQLAlchemy extension will look for the code in your application that triggered an SQL query in debug mode. If the import name is not properly set up, that debugging information is lost. (For example it would only pick up SQL queries in <code>yourapplication.app</code> and not <code>yourapplication.views.frontend</code>)</p></div><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 1.0</em>: The <code>host_matching</code> and <code>static_host</code> parameters were added.</p><p><em>New in version 1.0</em>: The <code>subdomain_matching</code> parameter was added. Subdomain matching needs to be enabled manually now. Setting <code>SERVER_NAME</code> does not implicitly enable it.</p><p><em>New in version 0.11</em>: The root_path parameter was added.</p><p><em>New in version 0.8</em>: The <code>instance_path</code> and <code>instance_relative_config</code> parameters were added.</p><p><em>New in version 0.7</em>: The <code>static_url_path</code>, <code>static_folder</code>, and <code>template_folder</code> parameters were added.</p></details><ul><li><p><em>Parameters</em>:</p><ul><li><p><code>import_name (str)</code> – the name of the application package</p></li><li><p><code>static_url_path (str | None)</code> – can be used to specify a different path for the static files on the web. Defaults to the name of the <code>static_folder</code> folder.</p></li><li><p><code>static_folder (str | os.PathLike | None)</code> – The folder with static files that is served at <code>static_url_path</code>. Relative to the application <code>root_path</code> or an absolute path. Defaults to <code>&#39;static&#39;</code>.</p></li><li><p><code>static_host (str | None)</code> – the host to use when adding the static route. Defaults to None. Required when using <code>host_matching=True</code> with a <code>static_folder</code> configured.</p></li><li><p><code>host_matching (bool)</code> – set <code>url_map.host_matching</code> attribute. Defaults to <code>False</code>.</p></li><li><p><code>subdomain_matching (bool)</code> – consider the subdomain relative to <code>SERVER_NAME</code> when matching routes. Defaults to <code>False</code>.</p></li><li><p><code>template_folder (str | os.PathLike | None)</code> – the folder that contains the templates that should be used by the application. Defaults to <code>&#39;templates&#39;</code> folder in the root path of the application.</p></li><li><p><code>instance_path (str | None)</code> – An alternative instance path for the application. By default the folder <code>&#39;instance&#39;</code> next to the package or module is assumed to be the instance path.</p></li><li><p><code>instance_relative_config (bool)</code> – if set to <code>True</code> relative filenames for loading the config are assumed to be relative to the instance path instead of the application root.</p></li><li><p><code>root_path (str | None)</code> – The path to the root of the application files. This should only be set manually when it can’t be detected automatically, such as for namespace packages.</p></li></ul></li><li><h4 id="aborter" tabindex="-1">aborter <a class="header-anchor" href="#aborter" aria-label="Permalink to &quot;aborter&quot;">​</a></h4><p>An instance of <code>aborter_class</code> created by <code>make_aborter()</code>. This is called by <code>flask.abort()</code> to raise HTTP errors, and can be called directly as well.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.2</em>: Moved from <code>flask.abort</code>, which calls this object.</p></details></li><li><h4 id="aborter-class" tabindex="-1">aborter_class <a class="header-anchor" href="#aborter-class" aria-label="Permalink to &quot;aborter_class&quot;">​</a></h4><p>alias of <code>Aborter</code></p></li><li><h4 id="add-template-filter-f-name-none" tabindex="-1">add_template_filter(<code>f, name=None</code>) <a class="header-anchor" href="#add-template-filter-f-name-none" aria-label="Permalink to &quot;add_template_filter(`f, name=None`)&quot;">​</a></h4><p>Register a custom template filter. Works exactly like the <code>template_filter()</code> decorator.</p><p><em>Parameters</em>:</p><ul><li><p><code>name (str | None)</code> – the optional name of the filter, otherwise the function name will be used.</p></li><li><p><code>f (Callable[[...], Any])</code> –</p></li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h4 id="add-template-global-f-name-none" tabindex="-1">add_template_global(<code>f, name=None</code>) <a class="header-anchor" href="#add-template-global-f-name-none" aria-label="Permalink to &quot;add_template_global(`f, name=None`)&quot;">​</a></h4><p>Register a custom template global function. Works exactly like the <code>template_global()</code> decorator.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.10.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>name (str | None)</code> – the optional name of the global function, otherwise the function name will be used.</p></li><li><p><code>f (Callable[[...], Any])</code> –</p></li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h4 id="add-template-test-f-name-none" tabindex="-1">add_template_test(<code>f, name=None</code>) <a class="header-anchor" href="#add-template-test-f-name-none" aria-label="Permalink to &quot;add_template_test(`f, name=None`)&quot;">​</a></h4><p>Register a custom template test. Works exactly like the <code>template_test()</code> decorator.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.10.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>name (str | None)</code> – the optional name of the test, otherwise the function name will be used.</p></li><li><p><code>f (Callable[[...], bool])</code> –</p></li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h4 id="add-url-rule-rule-endpoint-none-view-func-none-provide-automatic-options-none-options" tabindex="-1">add_url_rule(<code>rule, endpoint=None, view_func=None, provide_automatic_options=None, **options</code>) <a class="header-anchor" href="#add-url-rule-rule-endpoint-none-view-func-none-provide-automatic-options-none-options" aria-label="Permalink to &quot;add_url_rule(`rule, endpoint=None, view_func=None, provide_automatic_options=None, **options`)&quot;">​</a></h4><p>Register a rule for routing incoming requests and building URLs. The <code>route()</code> decorator is a shortcut to call this with the <code>view_func</code> argument. These are equivalent:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">route</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">/</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">index</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">    ...</span></span></code></pre></div><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">index</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">    ...</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#A6ACCD;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">add_url_rule</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">/</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#A6ACCD;font-style:italic;">view_func</span><span style="color:#89DDFF;">=</span><span style="color:#82AAFF;">index</span><span style="color:#89DDFF;">)</span></span></code></pre></div><p>See <a href="/python/flask/api_reference/url_route#url-route-registrations">URL Route Registrations</a>.</p><p>The endpoint name for the route defaults to the name of the view function if the <code>endpoint</code> parameter isn’t passed. An error will be raised if a function has already been registered for the endpoint.</p><p>The <code>methods</code> parameter defaults to <code>[&quot;GET&quot;]</code>. <code>HEAD</code> is always added automatically, and <code>OPTIONS</code> is added automatically by default.</p><p><code>view_func</code> does not necessarily need to be passed, but if the rule should participate in routing an endpoint name must be associated with a view function at some point with the <code>endpoint()</code> decorator.</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">add_url_rule</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">/</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#A6ACCD;font-style:italic;">endpoint</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">index</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">endpoint</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">index</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">index</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">    ...</span></span></code></pre></div><p>If <code>view_func</code> has a <code>required_methods</code> attribute, those methods are added to the passed and automatic methods. If it has a <code>provide_automatic_methods</code> attribute, it is used as the default if the parameter is not passed.</p><p><em>Parameters</em>:</p><ul><li><p><code>rule (str)</code> – The URL rule string.</p></li><li><p><code>endpoint (str | None)</code> – The endpoint name to associate with the rule and view function. Used when routing and building URLs. Defaults to <code>view_func.__name__</code>.</p></li><li><p><code>view_func (ft.RouteCallable | None)</code> – The view function to associate with the endpoint name.</p></li><li><p><code>provide_automatic_options (bool | None)</code> – Add the <code>OPTIONS</code> method and respond to <code>OPTIONS</code> requests automatically.</p></li><li><p><code>options (t.Any)</code> – Extra options passed to the <code>Rule</code> object.</p></li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h4 id="after-request-f" tabindex="-1">after_request(<code>f</code>) <a class="header-anchor" href="#after-request-f" aria-label="Permalink to &quot;after_request(`f`)&quot;">​</a></h4><p>Register a function to run after each request to this object.</p><p>The function is called with the response object, and must return a response object. This allows the functions to modify or replace the response before it is sent.</p><p>If a function raises an exception, any remaining <code>after_request</code> functions will not be called. Therefore, this should not be used for actions that must execute, such as to close resources. Use <code>teardown_request()</code> for that.</p><p>This is available on both app and blueprint objects. When used on an app, this executes after every request. When used on a blueprint, this executes after every request that the blueprint handles. To register with a blueprint and execute after every request, use <code>Blueprint.after_app_request()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_after_request)</code> –</li></ul><p><em>Return type</em>: <code>T_after_request</code></p></li><li><h4 id="after-request-funcs-dict-ft-apporblueprintkey-list-ft-afterrequestcallable" tabindex="-1">after_request_funcs: <code>dict[ft.AppOrBlueprintKey, list[ft.AfterRequestCallable]]</code> <a class="header-anchor" href="#after-request-funcs-dict-ft-apporblueprintkey-list-ft-afterrequestcallable" aria-label="Permalink to &quot;after_request_funcs: `dict[ft.AppOrBlueprintKey, list[ft.AfterRequestCallable]]`&quot;">​</a></h4><p>A data structure of functions to call at the end of each request, in the format <code>{scope: [functions]}</code>. The <code>scope</code> key is the name of a blueprint the functions are active for, or <code>None</code> for all requests.</p><p>To register a function, use the <code>after_request()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li><li><h4 id="app-context" tabindex="-1">app_context() <a class="header-anchor" href="#app-context" aria-label="Permalink to &quot;app_context()&quot;">​</a></h4><p>Create an <code>AppContext</code>. Use as a <code>with</code> block to push the context, which will make <code>current_app</code> point at this application.</p><p>An application context is automatically pushed by <code>RequestContext.push()</code> when handling a request, and when running a CLI command. Use this to manually create a context outside of these situations.</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;font-style:italic;">with</span><span style="color:#A6ACCD;"> app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">app_context</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#82AAFF;">init_db</span><span style="color:#89DDFF;">()</span></span></code></pre></div><p>See <a href="/python/flask/user_guide/app_context#the-application-context">The Application Context</a>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.9.</em></p></details><p><em>Return type</em>: <code>AppContext</code></p></li><li><h4 id="app-ctx-globals-class" tabindex="-1">app_ctx_globals_class <a class="header-anchor" href="#app-ctx-globals-class" aria-label="Permalink to &quot;app_ctx_globals_class&quot;">​</a></h4><p>alias of <code>_AppCtxGlobals</code></p></li><li><h4 id="async-to-sync-func" tabindex="-1">async_to_sync(<code>func</code>) <a class="header-anchor" href="#async-to-sync-func" aria-label="Permalink to &quot;async_to_sync(`func`)&quot;">​</a></h4><p>Return a sync function that will run the coroutine function.</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">result </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">async_to_sync</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">func</span><span style="color:#89DDFF;">)(*</span><span style="color:#82AAFF;">args</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#89DDFF;">**</span><span style="color:#82AAFF;">kwargs</span><span style="color:#89DDFF;">)</span></span></code></pre></div><p>Override this method to change how the app converts async code to be synchronously callable.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.0.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>func (Callable[[...], Coroutine])</code> –</li></ul><p><em>Return type</em>: <code>Callable[[…], Any]</code></p></li><li><h4 id="auto-find-instance-path" tabindex="-1">auto_find_instance_path() <a class="header-anchor" href="#auto-find-instance-path" aria-label="Permalink to &quot;auto_find_instance_path()&quot;">​</a></h4><p>Tries to locate the instance path if it was not provided to the constructor of the application class. It will basically calculate the path to a folder named <code>instance</code> next to your main file or the package.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.8.</em></p></details><p><em>Return type</em>: <code>str</code></p></li><li><h4 id="before-request-f" tabindex="-1">before_request(<code>f</code>) <a class="header-anchor" href="#before-request-f" aria-label="Permalink to &quot;before_request(`f`)&quot;">​</a></h4><p>Register a function to run before each request.</p><p>For example, this can be used to open a database connection, or to load the logged in user from the session.</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">before_request</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">load_user</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">if</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">user_id</span><span style="color:#89DDFF;">&quot;</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">in</span><span style="color:#A6ACCD;"> session</span><span style="color:#89DDFF;">:</span></span>\n<span class="line"><span style="color:#A6ACCD;">        g</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">user</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> db</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">session</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">get</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">session</span><span style="color:#89DDFF;">[</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">user_id</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">])</span></span></code></pre></div><p>The function will be called without any arguments. If it returns a non-<code>None</code> value, the value is handled as if it was the return value from the view, and further request handling is stopped.</p><p>This is available on both app and blueprint objects. When used on an app, this executes before every request. When used on a blueprint, this executes before every request that the blueprint handles. To register with a blueprint and execute before every request, use <code>Blueprint.before_app_request()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_before_request)</code> –</li></ul><p><em>Return type</em>: <code>T_before_request</code></p></li><li><h4 id="before-request-funcs-dict-ft-apporblueprintkey-list-ft-beforerequestcallable" tabindex="-1">before_request_funcs: <code>dict[ft.AppOrBlueprintKey, list[ft.BeforeRequestCallable]]</code> <a class="header-anchor" href="#before-request-funcs-dict-ft-apporblueprintkey-list-ft-beforerequestcallable" aria-label="Permalink to &quot;before_request_funcs: `dict[ft.AppOrBlueprintKey, list[ft.BeforeRequestCallable]]`&quot;">​</a></h4><p>A data structure of functions to call at the beginning of each request, in the format <code>{scope: [functions]}</code>. The <code>scope</code> key is the name of a blueprint the functions are active for, or <code>None</code> for all requests.</p><p>To register a function, use the <code>before_request()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li><li><h4 id="blueprints-dict-str-blueprint" tabindex="-1">blueprints: <code>dict[str, Blueprint]</code> <a class="header-anchor" href="#blueprints-dict-str-blueprint" aria-label="Permalink to &quot;blueprints: `dict[str, Blueprint]`&quot;">​</a></h4><p>Maps registered blueprint names to blueprint objects. The dict retains the order the blueprints were registered in. Blueprints can be registered multiple times, this dict does not track how often they were attached.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.7.</em></p></details></li><li><h4 id="cli" tabindex="-1">cli <a class="header-anchor" href="#cli" aria-label="Permalink to &quot;cli&quot;">​</a></h4><p>The Click command group for registering CLI commands for this object. The commands are available from the <code>flask</code> command once the application has been discovered and blueprints have been registered.</p></li><li><h4 id="config" tabindex="-1">config <a class="header-anchor" href="#config" aria-label="Permalink to &quot;config&quot;">​</a></h4><p>The configuration dictionary as <code>Config</code>. This behaves exactly like a regular dictionary but supports additional methods to load a config from files.</p></li><li><h4 id="config-class" tabindex="-1">config_class <a class="header-anchor" href="#config-class" aria-label="Permalink to &quot;config_class&quot;">​</a></h4><p>alias of <code>Config</code></p></li><li><h4 id="context-processor-f" tabindex="-1">context_processor(<code>f</code>) <a class="header-anchor" href="#context-processor-f" aria-label="Permalink to &quot;context_processor(`f`)&quot;">​</a></h4><p>Registers a template context processor function. These functions run before rendering a template. The keys of the returned dict are added as variables available in the template.</p><p>This is available on both app and blueprint objects. When used on an app, this is called for every rendered template. When used on a blueprint, this is called for templates rendered from the blueprint’s views. To register with a blueprint and affect every template, use <code>Blueprint.app_context_processor()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_template_context_processor)</code> –</li></ul><p><em>Return type</em>: <code>T_template_context_processor</code></p></li><li><h4 id="create-global-jinja-loader" tabindex="-1">create_global_jinja_loader() <a class="header-anchor" href="#create-global-jinja-loader" aria-label="Permalink to &quot;create_global_jinja_loader()&quot;">​</a></h4><p>Creates the loader for the Jinja2 environment. Can be used to override just the loader and keeping the rest unchanged. It’s discouraged to override this function. Instead one should override the <code>jinja_loader()</code> function instead.</p><p>The global loader dispatches between the loaders of the application and the individual blueprints.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.7.</em></p></details><p><em>Return type</em>: <code>DispatchingJinjaLoader</code></p></li><li><h4 id="create-jinja-environment" tabindex="-1">create_jinja_environment() <a class="header-anchor" href="#create-jinja-environment" aria-label="Permalink to &quot;create_jinja_environment()&quot;">​</a></h4><p>Create the Jinja environment based on <code>jinja_options</code> and the various Jinja-related methods of the app. Changing <code>jinja_options</code> after this will have no effect. Also adds Flask-related globals and filters to the environment.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 0.11</em>: <code>Environment.auto_reload</code> set in accordance with <code>TEMPLATES_AUTO_RELOAD</code> configuration option.</p><p><em>New in version 0.5.</em></p></details><p><em>Return type</em>: <code>Environment</code></p></li><li><h4 id="create-url-adapter-request" tabindex="-1">create_url_adapter(<code>request</code>) <a class="header-anchor" href="#create-url-adapter-request" aria-label="Permalink to &quot;create_url_adapter(`request`)&quot;">​</a></h4><p>Creates a URL adapter for the given request. The URL adapter is created at a point where the request context is not yet set up so the request is passed explicitly.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 1.0</em>: <code>SERVER_NAME</code> no longer implicitly enables subdomain matching. Use <code>subdomain_matching</code> instead.</p><p><em>Changed in version 0.9</em>: This can now also be called without a request object when the URL adapter is created for the application context.</p><p><em>New in version 0.6.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>request (Request | None)</code> –</li></ul><p><em>Return type</em>: <code>MapAdapter | None</code></p></li><li><h4 id="property-debug-bool" tabindex="-1"><code>property</code> debug: <code>bool</code> <a class="header-anchor" href="#property-debug-bool" aria-label="Permalink to &quot;`property` debug: `bool`&quot;">​</a></h4><p>Whether debug mode is enabled. When using <code>flask run</code> to start the development server, an interactive debugger will be shown for unhandled exceptions, and the server will be reloaded when code changes. This maps to the <code>DEBUG</code> config key. It may not behave as expected if set late.</p><p>Do not enable debug mode when deploying in production.</p><p><em>Default</em>: <code>False</code></p></li><li><h4 id="default-config-application-root-debug-none-explain-template-loading-false-max-content-length-none-max-cookie-size-4093-permanent-session-lifetime-datetime-timedelta-days-31-preferred-url-scheme-http-propagate-exceptions-none-secret-key-none-send-file-max-age-default-none-server-name-none-session-cookie-domain-none-session-cookie-httponly-true-session-cookie-name-session-session-cookie-path-none-session-cookie-samesite-none-session-cookie-secure-false-session-refresh-each-request-true-templates-auto-reload-none-testing-false-trap-bad-request-errors-none-trap-http-exceptions-false-use-x-sendfile-false" tabindex="-1">default_config = <code>{&#39;APPLICATION_ROOT&#39;: &#39;/&#39;, &#39;DEBUG&#39;: None, &#39;EXPLAIN_TEMPLATE_LOADING&#39;: False, &#39;MAX_CONTENT_LENGTH&#39;: None, &#39;MAX_COOKIE_SIZE&#39;: 4093, &#39;PERMANENT_SESSION_LIFETIME&#39;: datetime.timedelta(days=31), &#39;PREFERRED_URL_SCHEME&#39;: &#39;http&#39;, &#39;PROPAGATE_EXCEPTIONS&#39;: None, &#39;SECRET_KEY&#39;: None, &#39;SEND_FILE_MAX_AGE_DEFAULT&#39;: None, &#39;SERVER_NAME&#39;: None, &#39;SESSION_COOKIE_DOMAIN&#39;: None, &#39;SESSION_COOKIE_HTTPONLY&#39;: True, &#39;SESSION_COOKIE_NAME&#39;: &#39;session&#39;, &#39;SESSION_COOKIE_PATH&#39;: None, &#39;SESSION_COOKIE_SAMESITE&#39;: None, &#39;SESSION_COOKIE_SECURE&#39;: False, &#39;SESSION_REFRESH_EACH_REQUEST&#39;: True, &#39;TEMPLATES_AUTO_RELOAD&#39;: None, &#39;TESTING&#39;: False, &#39;TRAP_BAD_REQUEST_ERRORS&#39;: None, &#39;TRAP_HTTP_EXCEPTIONS&#39;: False, &#39;USE_X_SENDFILE&#39;: False}</code> <a class="header-anchor" href="#default-config-application-root-debug-none-explain-template-loading-false-max-content-length-none-max-cookie-size-4093-permanent-session-lifetime-datetime-timedelta-days-31-preferred-url-scheme-http-propagate-exceptions-none-secret-key-none-send-file-max-age-default-none-server-name-none-session-cookie-domain-none-session-cookie-httponly-true-session-cookie-name-session-session-cookie-path-none-session-cookie-samesite-none-session-cookie-secure-false-session-refresh-each-request-true-templates-auto-reload-none-testing-false-trap-bad-request-errors-none-trap-http-exceptions-false-use-x-sendfile-false" aria-label="Permalink to &quot;default_config = `{&#39;APPLICATION_ROOT&#39;: &#39;/&#39;, &#39;DEBUG&#39;: None, &#39;EXPLAIN_TEMPLATE_LOADING&#39;: False, &#39;MAX_CONTENT_LENGTH&#39;: None, &#39;MAX_COOKIE_SIZE&#39;: 4093, &#39;PERMANENT_SESSION_LIFETIME&#39;: datetime.timedelta(days=31), &#39;PREFERRED_URL_SCHEME&#39;: &#39;http&#39;, &#39;PROPAGATE_EXCEPTIONS&#39;: None, &#39;SECRET_KEY&#39;: None, &#39;SEND_FILE_MAX_AGE_DEFAULT&#39;: None, &#39;SERVER_NAME&#39;: None, &#39;SESSION_COOKIE_DOMAIN&#39;: None, &#39;SESSION_COOKIE_HTTPONLY&#39;: True, &#39;SESSION_COOKIE_NAME&#39;: &#39;session&#39;, &#39;SESSION_COOKIE_PATH&#39;: None, &#39;SESSION_COOKIE_SAMESITE&#39;: None, &#39;SESSION_COOKIE_SECURE&#39;: False, &#39;SESSION_REFRESH_EACH_REQUEST&#39;: True, &#39;TEMPLATES_AUTO_RELOAD&#39;: None, &#39;TESTING&#39;: False, &#39;TRAP_BAD_REQUEST_ERRORS&#39;: None, &#39;TRAP_HTTP_EXCEPTIONS&#39;: False, &#39;USE_X_SENDFILE&#39;: False}`&quot;">​</a></h4><p>Default configuration parameters.</p></li><li><h4 id="delete-rule-options" tabindex="-1">delete(<code>rule, **options</code>) <a class="header-anchor" href="#delete-rule-options" aria-label="Permalink to &quot;delete(`rule, **options`)&quot;">​</a></h4><p>Shortcut for <code>route()</code> with <code>methods=[&quot;DELETE&quot;]</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.0.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>rule (str)</code> –</p></li><li><p><code>options (Any)</code> –</p></li></ul><p><em>Return type</em>: <code>Callable[[T_route], T_route]</code></p></li><li><h4 id="dispatch-request" tabindex="-1">dispatch_request() <a class="header-anchor" href="#dispatch-request" aria-label="Permalink to &quot;dispatch_request()&quot;">​</a></h4><p>Does the request dispatching. Matches the URL and returns the return value of the view or error handler. This does not have to be a response object. In order to convert the return value to a proper response object, call <code>make_response()</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 0.7</em>: This no longer does the exception handling, this code was moved to the new <code>full_dispatch_request()</code>.</p></details><p><em>Return type</em>: <code>ft.ResponseReturnValue</code></p></li><li><h4 id="do-teardown-appcontext-exc-object-object" tabindex="-1">do_teardown_appcontext(<code>exc=&lt;object object&gt;</code>) <a class="header-anchor" href="#do-teardown-appcontext-exc-object-object" aria-label="Permalink to &quot;do_teardown_appcontext(`exc=&lt;object object&gt;`)&quot;">​</a></h4><p>Called right before the application context is popped.</p><p>When handling a request, the application context is popped after the request context. See <code>do_teardown_request()</code>.</p><p>This calls all functions decorated with <code>teardown_appcontext()</code>. Then the <code>appcontext_tearing_down</code> signal is sent.</p><p>This is called by <code>AppContext.pop()</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.9.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>exc (BaseException | None)</code> –</li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h4 id="do-teardown-request-exc-object-object" tabindex="-1">do_teardown_request(<code>exc=&lt;object object&gt;</code>) <a class="header-anchor" href="#do-teardown-request-exc-object-object" aria-label="Permalink to &quot;do_teardown_request(`exc=&lt;object object&gt;`)&quot;">​</a></h4><p>Called after the request is dispatched and the response is returned, right before the request context is popped.</p><p>This calls all functions decorated with <code>teardown_request()</code>, and <code>Blueprint.teardown_request()</code> if a blueprint handled the request. Finally, the <code>request_tearing_down</code> signal is sent.</p><p>This is called by <code>RequestContext.pop()</code>, which may be delayed during testing to maintain access to resources.</p><p><em>Parameters</em>:</p><ul><li><code>exc (BaseException | None)</code> – An unhandled exception raised while dispatching the request. Detected from the current exception information if not passed. Passed to each teardown function.</li></ul><p><em>Return type</em>: <code>None</code></p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 0.9</em>: Added the <code>exc</code> argument.</p></details></li><li><h4 id="endpoint-endpoint" tabindex="-1">endpoint(<code>endpoint</code>) <a class="header-anchor" href="#endpoint-endpoint" aria-label="Permalink to &quot;endpoint(`endpoint`)&quot;">​</a></h4><p>Decorate a view function to register it for the given endpoint. Used if a rule is added without a <code>view_func</code> with <code>add_url_rule()</code>.</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">add_url_rule</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">/ex</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#A6ACCD;font-style:italic;">endpoint</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">example</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">endpoint</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">example</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">example</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">    ...</span></span></code></pre></div><p><em>Parameters</em>:</p><ul><li><code>endpoint (str)</code> – The endpoint name to associate with the view function.</li></ul><p><em>Return type</em>: <code>Callable[[F], F]</code></p></li><li><h4 id="ensure-sync-func" tabindex="-1">ensure_sync(<code>func</code>) <a class="header-anchor" href="#ensure-sync-func" aria-label="Permalink to &quot;ensure_sync(`func`)&quot;">​</a></h4><p>Ensure that the function is synchronous for WSGI workers. Plain <code>def</code> functions are returned as-is. <code>async def</code> functions are wrapped to run and wait for the response.</p><p>Override this method to change how the app runs async views.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.0.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>func (Callable)</code> –</li></ul><p><em>Return type</em>: <code>Callable</code></p></li><li><h4 id="error-handler-spec-dict-ft-apporblueprintkey-dict-int-none-dict-type-exception-ft-errorhandlercallable" tabindex="-1">error_handler_spec: <code>dict[ft.AppOrBlueprintKey, dict[int | None, dict[type[Exception], ft.ErrorHandlerCallable]]]</code> <a class="header-anchor" href="#error-handler-spec-dict-ft-apporblueprintkey-dict-int-none-dict-type-exception-ft-errorhandlercallable" aria-label="Permalink to &quot;error_handler_spec: `dict[ft.AppOrBlueprintKey, dict[int | None, dict[type[Exception], ft.ErrorHandlerCallable]]]`&quot;">​</a></h4><p>A data structure of registered error handlers, in the format <code>{scope: {code: {class: handler}}}</code>. The <code>scope</code> key is the name of a blueprint the handlers are active for, or <code>None</code> for all requests. The <code>code</code> key is the HTTP status code for <code>HTTPException</code>, or <code>None</code> for other exceptions. The innermost dictionary maps exception classes to handler functions.</p><p>To register an error handler, use the <code>errorhandler()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li><li><h4 id="errorhandler-code-or-exception" tabindex="-1">errorhandler(<code>code_or_exception</code>) <a class="header-anchor" href="#errorhandler-code-or-exception" aria-label="Permalink to &quot;errorhandler(`code_or_exception`)&quot;">​</a></h4><p>Register a function to handle errors by code or exception class.</p><p>A decorator that is used to register a function given an error code. Example:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">errorhandler</span><span style="color:#89DDFF;">(</span><span style="color:#F78C6C;">404</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">page_not_found</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">error</span><span style="color:#89DDFF;">):</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">This page does not exist</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">404</span></span></code></pre></div><p>You can also register handlers for arbitrary exceptions:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">errorhandler</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">DatabaseError</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">special_exception_handler</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">error</span><span style="color:#89DDFF;">):</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">Database connection failed</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">500</span></span></code></pre></div><p>This is available on both app and blueprint objects. When used on an app, this can handle errors from every request. When used on a blueprint, this can handle errors from requests that the blueprint handles. To register with a blueprint and affect every request, use <code>Blueprint.app_errorhandler()</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.7</em>: Use <code>register_error_handler()</code> instead of modifying <code>error_handler_spec</code> directly, for application wide error handlers.</p><p><em>New in version 0.7</em>: One can now additionally also register custom exception types that do not necessarily have to be a subclass of the <code>HTTPException</code> class.</p></details><p><em>Parameters</em>:</p><ul><li><code>code_or_exception (type[Exception] | int)</code> – the code as integer for the handler, or an arbitrary exception</li></ul><p><em>Return type</em>: <code>Callable[[T_error_handler], T_error_handler]</code></p></li><li><h4 id="extensions-dict" tabindex="-1">extensions: <code>dict</code> <a class="header-anchor" href="#extensions-dict" aria-label="Permalink to &quot;extensions: `dict`&quot;">​</a></h4><p>a place where extensions can store application specific state. For example this is where an extension could store database engines and similar things.</p><p>The key must match the name of the extension module. For example in case of a “Flask-Foo” extension in <code>flask_foo</code>, the key would be <code>&#39;foo&#39;</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.7.</em></p></details></li><li><h4 id="full-dispatch-request" tabindex="-1">full_dispatch_request() <a class="header-anchor" href="#full-dispatch-request" aria-label="Permalink to &quot;full_dispatch_request()&quot;">​</a></h4><p>Dispatches the request and on top of that performs request pre and postprocessing as well as HTTP exception catching and error handling.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.7.</em></p></details><p><em>Return type</em>: <code>Response</code></p></li><li><h4 id="get-rule-options" tabindex="-1">get(<code>rule, **options</code>) <a class="header-anchor" href="#get-rule-options" aria-label="Permalink to &quot;get(`rule, **options`)&quot;">​</a></h4><p>Shortcut for <code>route()</code> with <code>methods=[&quot;GET&quot;]</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.0.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>rule (str)</code> –</p></li><li><p><code>options (Any)</code> –</p></li></ul><p><em>Return type</em>: <code>Callable[[T_route], T_route]</code></p></li><li><h4 id="get-send-file-max-age-filename" tabindex="-1">get_send_file_max_age(<code>filename</code>) <a class="header-anchor" href="#get-send-file-max-age-filename" aria-label="Permalink to &quot;get_send_file_max_age(`filename`)&quot;">​</a></h4><p>Used by <code>send_file()</code> to determine the <code>max_age</code> cache value for a given file path if it wasn’t passed.</p><p>By default, this returns <code>SEND_FILE_MAX_AGE_DEFAULT</code> from the configuration of <code>current_app</code>. This defaults to <code>None</code>, which tells the browser to use conditional requests instead of a timed cache, which is usually preferable.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 2.0</em>: The default configuration is <code>None</code> instead of 12 hours.</p><p>New in version 0.9.</p></details><p><em>Parameters</em>:</p><ul><li><code>filename (str | None)</code> –</li></ul><p><em>Return type</em>: <code>int | None</code></p></li><li><h4 id="property-got-first-request-bool" tabindex="-1"><code>property</code> got_first_request: <code>bool</code> <a class="header-anchor" href="#property-got-first-request-bool" aria-label="Permalink to &quot;`property` got_first_request: `bool`&quot;">​</a></h4><p>This attribute is set to <code>True</code> if the application started handling the first request.</p><p><em>Deprecated since version 2.3</em>: Will be removed in Flask 2.4.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.8.</em></p></details></li><li><h4 id="handle-exception-e" tabindex="-1">handle_exception(<code>e</code>) <a class="header-anchor" href="#handle-exception-e" aria-label="Permalink to &quot;handle_exception(`e`)&quot;">​</a></h4><p>Handle an exception that did not have an error handler associated with it, or that was raised from an error handler. This always causes a 500 <code>InternalServerError</code>.</p><p>Always sends the <code>got_request_exception</code> signal.</p><p>If <code>PROPAGATE_EXCEPTIONS</code> is <code>True</code>, such as in debug mode, the error will be re-raised so that the debugger can display it. Otherwise, the original exception is logged, and an <code>InternalServerError</code> is returned.</p><p>If an error handler is registered for <code>InternalServerError</code> or <code>500</code>, it will be used. For consistency, the handler will always receive the <code>InternalServerError</code>. The original unhandled exception is available as <code>e.original_exception</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 1.1.0</em>: Always passes the <code>InternalServerError</code> instance to the handler, setting <code>original_exception</code> to the unhandled error.</p><p><em>Changed in version 1.1.0</em>: <code>after_request</code> functions and other finalization is done even for the default 500 response when there is no handler.</p><p><em>New in version 0.3.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>e (Exception)</code> –</li></ul><p><em>Return type</em>: <code>Response</code></p></li><li><h4 id="handle-http-exception-e" tabindex="-1">handle_http_exception(<code>e</code>) <a class="header-anchor" href="#handle-http-exception-e" aria-label="Permalink to &quot;handle_http_exception(`e`)&quot;">​</a></h4><p>Handles an HTTP exception. By default this will invoke the registered error handlers and fall back to returning the exception as response.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 1.0.3</em>: <code>RoutingException</code>, used internally for actions such as slash redirects during routing, is not passed to error handlers.</p><p><em>Changed in version 1.0</em>: Exceptions are looked up by code and by MRO, so <code>HTTPException</code> subclasses can be handled with a catch-all handler for the base <code>HTTPException</code>.</p><p><em>New in version 0.3.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>e (HTTPException)</code> –</li></ul><p><em>Return type</em>: <code>HTTPException | ft.ResponseReturnValue</code></p></li><li><h4 id="handle-url-build-error-error-endpoint-values" tabindex="-1">handle_url_build_error(<code>error, endpoint, values</code>) <a class="header-anchor" href="#handle-url-build-error-error-endpoint-values" aria-label="Permalink to &quot;handle_url_build_error(`error, endpoint, values`)&quot;">​</a></h4><p>Called by <code>url_for()</code> if a <code>BuildError</code> was raised. If this returns a value, it will be returned by <code>url_for</code>, otherwise the error will be re-raised.</p><p>Each function in <code>url_build_error_handlers</code> is called with <code>error</code>, <code>endpoint</code> and <code>values</code>. If a function returns <code>None</code> or raises a <code>BuildError</code>, it is skipped. Otherwise, its return value is returned by <code>url_for</code>.</p><p><em>Parameters</em>:</p><ul><li><p><code>error (BuildError)</code> – The active <code>BuildError</code> being handled.</p></li><li><p><code>endpoint (str)</code> – The endpoint being built.</p></li><li><p><code>values (dict[str, Any])</code> – The keyword arguments passed to <code>url_for</code>.</p></li></ul><p><em>Return type</em>: <code>str</code></p></li><li><h4 id="handle-user-exception-e" tabindex="-1">handle_user_exception(<code>e</code>) <a class="header-anchor" href="#handle-user-exception-e" aria-label="Permalink to &quot;handle_user_exception(`e`)&quot;">​</a></h4><p>This method is called whenever an exception occurs that should be handled. A special case is <code>HTTPException</code> which is forwarded to the <code>handle_http_exception()</code> method. This function will either return a response value or reraise the exception with the same traceback.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 1.0</em>: Key errors raised from request data like <code>form</code> show the bad key in debug mode rather than a generic bad request message.</p><p><em>New in version 0.7.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>e (Exception)</code> –</li></ul><p><em>Return type</em>: <code>HTTPException | ft.ResponseReturnValue</code></p></li><li><h4 id="property-has-static-folder-bool" tabindex="-1"><code>property</code> has_static_folder: <code>bool</code> <a class="header-anchor" href="#property-has-static-folder-bool" aria-label="Permalink to &quot;`property` has_static_folder: `bool`&quot;">​</a></h4><p><code>True</code> if <code>static_folder</code> is set.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.5.</em></p></details></li><li><h4 id="import-name" tabindex="-1">import_name <a class="header-anchor" href="#import-name" aria-label="Permalink to &quot;import_name&quot;">​</a></h4><p>The name of the package or module that this object belongs to. Do not change this once it is set by the constructor.</p></li><li><h4 id="inject-url-defaults-endpoint-values" tabindex="-1">inject_url_defaults(<code>endpoint, values</code>) <a class="header-anchor" href="#inject-url-defaults-endpoint-values" aria-label="Permalink to &quot;inject_url_defaults(`endpoint, values`)&quot;">​</a></h4><p>Injects the URL defaults for the given endpoint directly into the values dictionary passed. This is used internally and automatically called on URL building.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.7.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>endpoint (str)</code> –</p></li><li><p><code>values (dict)</code> –</p></li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h4 id="instance-path" tabindex="-1">instance_path <a class="header-anchor" href="#instance-path" aria-label="Permalink to &quot;instance_path&quot;">​</a></h4><p>Holds the path to the instance folder.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.8.</em></p></details></li><li><h4 id="iter-blueprints" tabindex="-1">iter_blueprints() <a class="header-anchor" href="#iter-blueprints" aria-label="Permalink to &quot;iter_blueprints()&quot;">​</a></h4><p>Iterates over all blueprints by the order they were registered.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.11.</em></p></details><p><em>Return type</em>: <code>t.ValuesView[Blueprint]</code></p></li><li><h4 id="property-jinja-env-environment" tabindex="-1"><code>property</code> jinja_env: <code>Environment</code> <a class="header-anchor" href="#property-jinja-env-environment" aria-label="Permalink to &quot;`property` jinja_env: `Environment`&quot;">​</a></h4><p>The Jinja environment used to load templates.</p><p>The environment is created the first time this property is accessed. Changing <code>jinja_options</code> after that will have no effect.</p></li><li><h4 id="jinja-environment" tabindex="-1">jinja_environment <a class="header-anchor" href="#jinja-environment" aria-label="Permalink to &quot;jinja_environment&quot;">​</a></h4><p>alias of <code>Environment</code></p></li><li><h4 id="property-jinja-loader-filesystemloader-none" tabindex="-1"><code>property</code> jinja_loader: <code>FileSystemLoader | None</code> <a class="header-anchor" href="#property-jinja-loader-filesystemloader-none" aria-label="Permalink to &quot;`property` jinja_loader: `FileSystemLoader | None`&quot;">​</a></h4><p>The Jinja loader for this object’s templates. By default this is a class <code>jinja2.loaders.FileSystemLoader</code> to <code>template_folder</code> if it is set.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.5.</em></p></details></li><li><h4 id="jinja-options-dict" tabindex="-1">jinja_options: <code>dict = {}</code> <a class="header-anchor" href="#jinja-options-dict" aria-label="Permalink to &quot;jinja_options: `dict = {}`&quot;">​</a></h4><p>Options that are passed to the Jinja environment in <code>create_jinja_environment()</code>. Changing these options after the environment is created (accessing <code>jinja_env</code>) will have no effect.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 1.1.0</em>: This is a <code>dict</code> instead of an <code>ImmutableDict</code> to allow easier configuration.</p></details></li><li><h4 id="json-jsonprovider" tabindex="-1">json: <code>JSONProvider</code> <a class="header-anchor" href="#json-jsonprovider" aria-label="Permalink to &quot;json: `JSONProvider`&quot;">​</a></h4><p>Provides access to JSON methods. Functions in <code>flask.json</code> will call methods on this provider when the application context is active. Used for handling JSON requests and responses.</p><p>An instance of <code>json_provider_class</code>. Can be customized by changing that attribute on a subclass, or by assigning to this attribute afterwards.</p><p>The default, <code>DefaultJSONProvider</code>, uses Python’s built-in <code>json</code> library. A different provider can use a different JSON library.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.2.</em></p></details></li><li><h4 id="json-provider-class" tabindex="-1">json_provider_class <a class="header-anchor" href="#json-provider-class" aria-label="Permalink to &quot;json_provider_class&quot;">​</a></h4><p>alias of <code>DefaultJSONProvider</code></p></li><li><h4 id="log-exception-exc-info" tabindex="-1">log_exception(<code>exc_info</code>) <a class="header-anchor" href="#log-exception-exc-info" aria-label="Permalink to &quot;log_exception(`exc_info`)&quot;">​</a></h4><p>Logs an exception. This is called by <code>handle_exception()</code> if debugging is disabled and right before the handler is called. The default implementation logs the exception as error on the <code>logger</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.8.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>exc_info (tuple[type, BaseException, traceback] | tuple[None, None, None])</code> –</li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h4 id="property-logger-logger" tabindex="-1"><code>property</code> logger: <code>Logger</code> <a class="header-anchor" href="#property-logger-logger" aria-label="Permalink to &quot;`property` logger: `Logger`&quot;">​</a></h4><p>A standard Python <code>Logger</code> for the app, with the same name as <code>name</code>.</p><p>In debug mode, the logger’s <code>level</code> will be set to <code>DEBUG</code>.</p><p>If there are no handlers configured, a default handler will be added. See <code>Logging</code> for more information.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 1.1.0</em>: The logger takes the same name as <code>name</code> rather than hard-coding <code>&quot;flask.app&quot;</code>.</p><p><em>Changed in version 1.0.0</em>: Behavior was simplified. The logger is always named <code>&quot;flask.app&quot;</code>. The level is only set during configuration, it doesn’t check <code>app.debug</code> each time. Only one format is used, not different ones depending on <code>app.debug</code>. No handlers are removed, and a handler is only added if no handlers are already configured.</p><p><em>New in version 0.3.</em></p></details></li><li><h4 id="make-aborter" tabindex="-1">make_aborter() <a class="header-anchor" href="#make-aborter" aria-label="Permalink to &quot;make_aborter()&quot;">​</a></h4><p>Create the object to assign to <code>aborter</code>. That object is called by <code>flask.abort()</code> to raise HTTP errors, and can be called directly as well.</p><p>By default, this creates an instance of <code>aborter_class</code>, which defaults to <code>werkzeug.exceptions.Aborter</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.2.</em></p></details><p><em>Return type</em>: <code>Aborter</code></p></li><li><h4 id="make-config-instance-relative-false" tabindex="-1">make_config(<code>instance_relative=False</code>) <a class="header-anchor" href="#make-config-instance-relative-false" aria-label="Permalink to &quot;make_config(`instance_relative=False`)&quot;">​</a></h4><p>Used to create the config attribute by the Flask constructor. The <code>instance_relative</code> parameter is passed in from the constructor of Flask (there named <code>instance_relative_config</code>) and indicates if the config should be relative to the instance path or the root path of the application.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.8.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>instance_relative (bool)</code> –</li></ul><p><em>Return type</em>: <code>Config</code></p></li><li><h4 id="make-default-options-response" tabindex="-1">make_default_options_response() <a class="header-anchor" href="#make-default-options-response" aria-label="Permalink to &quot;make_default_options_response()&quot;">​</a></h4><p>This method is called to create the default <code>OPTIONS</code> response. This can be changed through subclassing to change the default behavior of <code>OPTIONS</code> responses.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.7.</em></p></details><p><em>Return type</em>: <code>Response</code></p></li><li><h4 id="make-response-rv" tabindex="-1">make_response(<code>rv</code>) <a class="header-anchor" href="#make-response-rv" aria-label="Permalink to &quot;make_response(`rv`)&quot;">​</a></h4><p>Convert the return value from a view function to an instance of <code>response_class</code>.</p><p><em>Parameters</em>:</p><ul><li><p><code>rv (ft.ResponseReturnValue)</code> – the return value from the view function. The view function must return a response. Returning <code>None</code>, or the view ending without returning, is not allowed. The following types are allowed for <code>view_rv</code>:</p><ul><li><p><code>str</code>: A response object is created with the string encoded to UTF-8 as the body.</p></li><li><p><code>bytes</code>: A response object is created with the bytes as the body.</p></li><li><p><code>dict</code>: A dictionary that will be jsonify’d before being returned.</p></li><li><p><code>list</code>: A list that will be jsonify’d before being returned.</p></li><li><p><code>generator</code> or <code>iterator</code>: A generator that returns <code>str</code> or <code>bytes</code> to be streamed as the response.</p></li><li><p><code>tuple</code>: Either <code>(body, status, headers)</code>, <code>(body, status)</code>, or <code>(body, headers)</code>, where <code>body</code> is any of the other types allowed here, <code>status</code> is a string or an integer, and <code>headers</code> is a dictionary or a list of <code>(key, value)</code> tuples. If <code>body</code> is a <code>response_class</code> instance, <code>status</code> overwrites the exiting value and <code>headers</code> are extended.</p></li><li><p><code>response_class</code>: The object is returned unchanged.</p></li><li><p>other <code>Response</code> class: The object is coerced to <code>response_class</code>.</p></li><li><p><code>callable()</code>: The function is called as a WSGI application. The result is used to create a response object.</p></li></ul></li></ul><p><em>Return type</em>: <code>Response</code></p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 2.2</em>: A generator will be converted to a streaming response. A list will be converted to a JSON response.</p><p><em>Changed in version 1.1</em>: A dict will be converted to a JSON response.</p><p><em>Changed in version 0.9</em>: Previously a tuple was interpreted as the arguments for the response object.</p></details></li><li><h4 id="make-shell-context" tabindex="-1">make_shell_context() <a class="header-anchor" href="#make-shell-context" aria-label="Permalink to &quot;make_shell_context()&quot;">​</a></h4><p>Returns the shell context for an interactive shell for this application. This runs all the registered shell context processors.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.11.</em></p></details><p><em>Return type</em>: <code>dict</code></p></li><li><h4 id="property-name-str" tabindex="-1"><code>property</code> name: <code>str</code> <a class="header-anchor" href="#property-name-str" aria-label="Permalink to &quot;`property` name: `str`&quot;">​</a></h4><p>The name of the application. This is usually the import name with the difference that it’s guessed from the run file if the import name is main. This name is used as a display name when Flask needs the name of the application. It can be set and overridden to change the value.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.8.</em></p></details></li><li><h4 id="open-instance-resource-resource-mode-rb" tabindex="-1">open_instance_resource(<code>resource, mode=&#39;rb&#39;</code>) <a class="header-anchor" href="#open-instance-resource-resource-mode-rb" aria-label="Permalink to &quot;open_instance_resource(`resource, mode=&#39;rb&#39;`)&quot;">​</a></h4><p>Opens a resource from the application’s instance folder (<code>instance_path</code>). Otherwise works like <code>open_resource()</code>. Instance resources can also be opened for writing.</p><p><em>Parameters</em>:</p><ul><li><p><code>resource (str)</code> – the name of the resource. To access resources within subfolders use forward slashes as separator.</p></li><li><p><code>mode (str)</code> – resource file opening mode, default is <code>‘rb’</code>.</p></li></ul><p><em>Return type</em>: <code>IO</code></p></li><li><h4 id="open-resource-resource-mode-rb" tabindex="-1">open_resource(<code>resource, mode=&#39;rb&#39;</code>) <a class="header-anchor" href="#open-resource-resource-mode-rb" aria-label="Permalink to &quot;open_resource(`resource, mode=&#39;rb&#39;`)&quot;">​</a></h4><p>Open a resource file relative to <code>root_path</code> for reading.</p><p>For example, if the file <code>schema.sql</code> is next to the file <code>app.py</code> where the <code>Flask</code> app is defined, it can be opened with:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;font-style:italic;">with</span><span style="color:#A6ACCD;"> app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">open_resource</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">schema.sql</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;font-style:italic;">as</span><span style="color:#A6ACCD;"> f</span><span style="color:#89DDFF;">:</span></span>\n<span class="line"><span style="color:#A6ACCD;">    conn</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">executescript</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">f</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">read</span><span style="color:#89DDFF;">())</span></span></code></pre></div><p><em>Parameters</em>:</p><ul><li><p><code>resource (str)</code> – Path to the resource relative to <code>root_path</code>.</p></li><li><p><code>mode (str)</code> – Open the file in this mode. Only reading is supported, valid values are <code>“r”</code> (or <code>“rt”</code>) and <code>“rb”</code>.</p></li></ul><p><em>Return type</em>: <code>IO</code></p></li><li><h4 id="patch-rule-options" tabindex="-1">patch(<code>rule, **options</code>) <a class="header-anchor" href="#patch-rule-options" aria-label="Permalink to &quot;patch(`rule, **options`)&quot;">​</a></h4><p>Shortcut for <code>route()</code> with <code>methods=[&quot;PATCH&quot;]</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.0.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>rule (str)</code> –</p></li><li><p><code>options (Any)</code> –</p></li></ul><p><em>Return type</em>: <code>Callable[[T_route], T_route]</code></p></li><li><h4 id="permanent-session-lifetime" tabindex="-1">permanent_session_lifetime <a class="header-anchor" href="#permanent-session-lifetime" aria-label="Permalink to &quot;permanent_session_lifetime&quot;">​</a></h4><p>A <code>timedelta</code> which is used to set the expiration date of a permanent session. The default is 31 days which makes a permanent session survive for roughly one month.</p><p>This attribute can also be configured from the config with the <code>PERMANENT_SESSION_LIFETIME</code> configuration key. Defaults to <code>timedelta(days=31)</code></p></li><li><h4 id="post-rule-options" tabindex="-1">post(<code>rule, **options</code>) <a class="header-anchor" href="#post-rule-options" aria-label="Permalink to &quot;post(`rule, **options`)&quot;">​</a></h4><p>Shortcut for <code>route()</code> with <code>methods=[&quot;POST&quot;]</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.0.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>rule (str)</code> –</p></li><li><p><code>options (Any)</code> –</p></li></ul><p><em>Return type</em>: <code>Callable[[T_route], T_route]</code></p></li><li><h4 id="preprocess-request" tabindex="-1">preprocess_request() <a class="header-anchor" href="#preprocess-request" aria-label="Permalink to &quot;preprocess_request()&quot;">​</a></h4><p>Called before the request is dispatched. Calls <code>url_value_preprocessors</code> registered with the app and the current blueprint (if <code>any</code>). Then calls <code>before_request_funcs</code> registered with the app and the blueprint.</p><p>If any <code>before_request()</code> handler returns a non-None value, the value is handled as if it was the return value from the view, and further request handling is stopped.</p><p><em>Return type</em>: <code>ft.ResponseReturnValue | None</code></p></li><li><h4 id="process-response-response" tabindex="-1">process_response(<code>response</code>) <a class="header-anchor" href="#process-response-response" aria-label="Permalink to &quot;process_response(`response`)&quot;">​</a></h4><p>Can be overridden in order to modify the response object before it’s sent to the WSGI server. By default this will call all the <code>after_request()</code> decorated functions.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 0.5</em>: As of Flask 0.5 the functions registered for after request execution are called in reverse order of registration.</p></details><p><em>Parameters</em>:</p><ul><li><code>response (Response)</code> – a <code>response_class</code> object.</li></ul><p><em>Returns</em>: a new response object or the same, has to be an instance of <code>response_class</code>.</p><p><em>Return type</em>: <code>Response</code></p></li><li><h4 id="put-rule-options" tabindex="-1">put(<code>rule, **options</code>) <a class="header-anchor" href="#put-rule-options" aria-label="Permalink to &quot;put(`rule, **options`)&quot;">​</a></h4><p>Shortcut for <code>route()</code> with <code>methods=[&quot;PUT&quot;]</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.0.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>rule (str)</code> –</p></li><li><p><code>options (Any)</code> –</p></li></ul><p><em>Return type</em>: <code>Callable[[T_route], T_route]</code></p></li><li><h4 id="redirect-location-code-302" tabindex="-1">redirect(<code>location, code=302</code>) <a class="header-anchor" href="#redirect-location-code-302" aria-label="Permalink to &quot;redirect(`location, code=302`)&quot;">​</a></h4><p>Create a redirect response object.</p><p>This is called by <code>flask.redirect()</code>, and can be called directly as well.</p><p><em>Parameters</em>:</p><ul><li><p><code>location (str)</code> – The URL to redirect to.</p></li><li><p><code>code (int)</code> – The status code for the redirect.</p></li></ul><p><em>Return type</em>: <code>Response</code></p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.2</em>: Moved from <code>flask.redirect</code>, which calls this method.</p></details></li><li><h4 id="register-blueprint-blueprint-options" tabindex="-1">register_blueprint(<code>blueprint, **options</code>) <a class="header-anchor" href="#register-blueprint-blueprint-options" aria-label="Permalink to &quot;register_blueprint(`blueprint, **options`)&quot;">​</a></h4><p>Register a <code>Blueprint</code> on the application. Keyword arguments passed to this method will override the defaults set on the blueprint.</p><p>Calls the blueprint’s <code>register()</code> method after recording the blueprint in the application’s <code>blueprints</code>.</p><p><em>Parameters</em>:</p><ul><li><p><code>blueprint (Blueprint)</code> – The blueprint to register.</p></li><li><p><code>url_prefix</code> – Blueprint routes will be prefixed with this.</p></li><li><p><code>subdomain</code> – Blueprint routes will match on this subdomain.</p></li><li><p><code>url_defaults</code> – Blueprint routes will use these default values for view arguments.</p></li><li><p><code>options (t.Any)</code> – Additional keyword arguments are passed to <code>BlueprintSetupState</code>. They can be accessed in <code>record()</code> callbacks.</p></li></ul><p><em>Return type</em>: <code>None</code></p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 2.0.1</em>: The <code>name</code> option can be used to change the (pre-dotted) name the blueprint is registered with. This allows the same blueprint to be registered multiple times with unique names for <code>url_for</code>.</p><p><em>New in version 0.7.</em></p></details></li><li><h4 id="register-error-handler-code-or-exception-f" tabindex="-1">register_error_handler(<code>code_or_exception, f</code>) <a class="header-anchor" href="#register-error-handler-code-or-exception-f" aria-label="Permalink to &quot;register_error_handler(`code_or_exception, f`)&quot;">​</a></h4><p>Alternative error attach function to the <code>errorhandler()</code> decorator that is more straightforward to use for non decorator usage.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.7.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>code_or_exception (type[Exception] | int)</code> –</p></li><li><p><code>f (ft.ErrorHandlerCallable)</code> –</p></li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h4 id="request-class" tabindex="-1">request_class <a class="header-anchor" href="#request-class" aria-label="Permalink to &quot;request_class&quot;">​</a></h4><p>alias of <code>Request</code></p></li><li><h4 id="request-context-environ" tabindex="-1">request_context(<code>environ</code>) <a class="header-anchor" href="#request-context-environ" aria-label="Permalink to &quot;request_context(`environ`)&quot;">​</a></h4><p>Create a <code>RequestContext</code> representing a WSGI environment. Use a <code>with</code> block to push the context, which will make <code>request</code> point at this request.</p><p>See <a href="/python/flask/user_guide/request_context#the-request-context">The Request Context</a>.</p><p>Typically you should not call this from your own code. A request context is automatically pushed by the <code>wsgi_app()</code> when handling a request. Use <code>test_request_context()</code> to create an environment and context instead of this method.</p><p><em>Parameters</em>:</p><ul><li><code>environ (dict)</code> – a <code>WSGI</code> environment</li></ul><p><em>Return type</em>: <code>RequestContext</code></p></li><li><h4 id="response-class" tabindex="-1">response_class <a class="header-anchor" href="#response-class" aria-label="Permalink to &quot;response_class&quot;">​</a></h4><p>alias of <code>Response</code></p></li><li><h4 id="root-path" tabindex="-1">root_path <a class="header-anchor" href="#root-path" aria-label="Permalink to &quot;root_path&quot;">​</a></h4><p>Absolute path to the package on the filesystem. Used to look up resources contained in the package.</p></li><li><h4 id="route-rule-options" tabindex="-1">route(<code>rule, **options</code>) <a class="header-anchor" href="#route-rule-options" aria-label="Permalink to &quot;route(`rule, **options`)&quot;">​</a></h4><p>Decorate a view function to register it with the given URL rule and options. Calls <code>add_url_rule()</code>, which has more details about the implementation.</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">route</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">/</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">index</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">Hello, World!</span><span style="color:#89DDFF;">&quot;</span></span></code></pre></div><p>See <a href="/python/flask/api_reference/url_route#url-route-registrations">URL Route Registrations</a>.</p><p>The endpoint name for the route defaults to the name of the view function if the <code>endpoint</code> parameter isn’t passed.</p><p>The <code>methods</code> parameter defaults to <code>[&quot;GET&quot;]</code>. <code>HEAD</code> and <code>OPTIONS</code> are added automatically.</p><p><em>Parameters</em>:</p><ul><li><p><code>rule (str)</code> – The URL rule string.</p></li><li><p><code>options (Any)</code> – Extra options passed to the <code>Rule</code> object.</p></li></ul><p><em>Return type</em>: <code>Callable[[T_route], T_route]</code></p></li><li><h4 id="run-host-none-port-none-debug-none-load-dotenv-true-options" tabindex="-1">run(<code>host=None, port=None, debug=None, load_dotenv=True, **options</code>) <a class="header-anchor" href="#run-host-none-port-none-debug-none-load-dotenv-true-options" aria-label="Permalink to &quot;run(`host=None, port=None, debug=None, load_dotenv=True, **options`)&quot;">​</a></h4><p>Runs the application on a local development server.</p><p>Do not use <code>run()</code> in a production setting. It is not intended to meet security and performance requirements for a production server. Instead, see <a href="/python/flask/user_guide/deploy#deploying-to-production">Deploying to Production</a> for WSGI server recommendations.</p><p>If the <code>debug</code> flag is set the server will automatically reload for code changes and show a debugger in case an exception happened.</p><p>If you want to run the application in debug mode, but disable the code execution on the interactive debugger, you can pass <code>use_evalex=False</code> as parameter. This will keep the debugger’s traceback screen active, but disable code execution.</p><p>It is not recommended to use this function for development with automatic reloading as this is badly supported. Instead you should be using the flask command line script’s <code>run</code> support.</p><div class="tip custom-block"><p class="custom-block-title">Keep in Mind</p><p>Flask will suppress any server error with a generic error page unless it is in debug mode. As such to enable just the interactive debugger without the code reloading, you have to invoke <code>run()</code> with <code>debug=True</code> and <code>use_reloader=False</code>. Setting <code>use_debugger</code> to <code>True</code> without being in debug mode won’t catch any exceptions because there won’t be any to catch.</p></div><p><em>Parameters</em>:</p><ul><li><p><code>host (str | None)</code> – the hostname to listen on. Set this to <code>&#39;0.0.0.0&#39;</code> to have the server available externally as well. Defaults to <code>&#39;127.0.0.1&#39;</code> or the host in the <code>SERVER_NAME</code> config variable if present.</p></li><li><p><code>port (int | None)</code> – the port of the webserver. Defaults to <code>5000</code> or the port defined in the <code>SERVER_NAME</code> config variable if present.</p></li><li><p><code>debug (bool | None)</code> – if given, enable or disable debug mode. See <code>debug</code>.</p></li><li><p><code>load_dotenv (bool)</code> – Load the nearest <code>.env</code> and <code>.flaskenv</code> files to set environment variables. Will also change the working directory to the directory containing the first file found.</p></li><li><p><code>options (Any)</code> – the options to be forwarded to the underlying Werkzeug server. See <code>werkzeug.serving.run_simple()</code> for more information.</p></li></ul><p><em>Return type</em>: <code>None</code></p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 1.0</em>: If installed, python-dotenv will be used to load environment variables from <code>.env</code> and <code>.flaskenv</code> files.</p><p>The <code>FLASK_DEBUG</code> environment variable will override <code>debug</code>.</p><p>Threaded mode is enabled by default.</p><p><em>Changed in version 0.10</em>: The default port is now picked from the <code>SERVER_NAME</code> variable.</p></details></li><li><h4 id="secret-key" tabindex="-1">secret_key <a class="header-anchor" href="#secret-key" aria-label="Permalink to &quot;secret_key&quot;">​</a></h4><p>If a secret key is set, cryptographic components can use this to sign cookies and other things. Set this to a complex random value when you want to use the secure cookie for instance.</p><p>This attribute can also be configured from the config with the <code>SECRET_KEY</code> configuration key. Defaults to <code>None</code>.</p></li><li><h4 id="select-jinja-autoescape-filename" tabindex="-1">select_jinja_autoescape(<code>filename</code>) <a class="header-anchor" href="#select-jinja-autoescape-filename" aria-label="Permalink to &quot;select_jinja_autoescape(`filename`)&quot;">​</a></h4><p>Returns <code>True</code> if autoescaping should be active for the given template name. If no template name is given, returns <code>True</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 2.2</em>: Autoescaping is now enabled by default for <code>.svg</code> files.</p><p><em>New in version 0.5.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>filename (str)</code> –</li></ul><p><em>Return type</em>: <code>bool</code></p></li><li><p>send_static_file(<code>filename</code>)</p><p>The view function used to serve files from <code>static_folder</code>. A route is automatically registered for this view at <code>static_url_path</code> if <code>static_folder</code> is set.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.5.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>filename (str)</code> –</li></ul><p><em>Return type</em>: <code>Response</code></p></li><li><h4 id="session-interface-sessioninterface-flask-sessions-securecookiesessioninterface-object" tabindex="-1">session_interface: <code>SessionInterface = &lt;flask.sessions.SecureCookieSessionInterface object&gt;</code> <a class="header-anchor" href="#session-interface-sessioninterface-flask-sessions-securecookiesessioninterface-object" aria-label="Permalink to &quot;session_interface: `SessionInterface = &lt;flask.sessions.SecureCookieSessionInterface object&gt;`&quot;">​</a></h4><p>the session interface to use. By default an instance of <code>SecureCookieSessionInterface</code> is used here.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.8.</em></p></details></li><li><h4 id="shell-context-processor-f" tabindex="-1">shell_context_processor(<code>f</code>) <a class="header-anchor" href="#shell-context-processor-f" aria-label="Permalink to &quot;shell_context_processor(`f`)&quot;">​</a></h4><p>Registers a shell context processor function.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.11.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>f (T_shell_context_processor)</code> –</li></ul><p><em>Return type</em>: <code>T_shell_context_processor</code></p></li><li><h4 id="shell-context-processors-list-ft-shellcontextprocessorcallable" tabindex="-1">shell_context_processors: <code>list[ft.ShellContextProcessorCallable]</code> <a class="header-anchor" href="#shell-context-processors-list-ft-shellcontextprocessorcallable" aria-label="Permalink to &quot;shell_context_processors: `list[ft.ShellContextProcessorCallable]`&quot;">​</a></h4><p>A list of shell context processor functions that should be run when a shell context is created.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.11.</em></p></details></li><li><h4 id="should-ignore-error-error" tabindex="-1">should_ignore_error(<code>error</code>) <a class="header-anchor" href="#should-ignore-error-error" aria-label="Permalink to &quot;should_ignore_error(`error`)&quot;">​</a></h4><p>This is called to figure out if an error should be ignored or not as far as the teardown system is concerned. If this function returns <code>True</code> then the teardown handlers will not be passed the error.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.10.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>error (BaseException | None)</code> –</li></ul><p><em>Return type</em>: <code>bool</code></p></li><li><h4 id="property-static-folder-str-none" tabindex="-1"><code>property</code> static_folder: <code>str | None</code> <a class="header-anchor" href="#property-static-folder-str-none" aria-label="Permalink to &quot;`property` static_folder: `str | None`&quot;">​</a></h4><p>The absolute path to the configured static folder. <code>None</code> if no static folder is set.</p></li><li><h4 id="property-static-url-path-str-none" tabindex="-1"><code>property</code> static_url_path: <code>str | None</code> <a class="header-anchor" href="#property-static-url-path-str-none" aria-label="Permalink to &quot;`property` static_url_path: `str | None`&quot;">​</a></h4><p>The URL prefix that the static route will be accessible from.</p><p>If it was not configured during init, it is derived from <code>static_folder</code>.</p></li><li><h4 id="teardown-appcontext-f" tabindex="-1">teardown_appcontext(<code>f</code>) <a class="header-anchor" href="#teardown-appcontext-f" aria-label="Permalink to &quot;teardown_appcontext(`f`)&quot;">​</a></h4><p>Registers a function to be called when the application context is popped. The application context is typically popped after the request context for each request, at the end of CLI commands, or after a manually pushed context ends.</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;font-style:italic;">with</span><span style="color:#A6ACCD;"> app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">app_context</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">    ...</span></span></code></pre></div><p>When the <code>with</code> block exits (or <code>ctx.pop()</code> is called), the teardown functions are called just before the app context is made inactive. Since a request context typically also manages an application context it would also be called when you pop a request context.</p><p>When a teardown function was called because of an unhandled exception it will be passed an error object. If an <code>errorhandler()</code> is registered, it will handle the exception and the teardown will not receive it.</p><p>Teardown functions must avoid raising exceptions. If they execute code that might fail they must surround that code with a <code>try/except</code> block and log any errors.</p><p>The return values of teardown functions are ignored.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.9.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>f (T_teardown)</code> –</li></ul><p><em>Return type</em>: <code>T_teardown</code></p><ul><li>teardown_appcontext_funcs: <code>list[ft.TeardownCallable]</code></li></ul><p>A list of functions that are called when the application context is destroyed. Since the application context is also torn down if the request ends this is the place to store code that disconnects from databases.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.9.</em></p></details></li><li><h4 id="teardown-request-f" tabindex="-1">teardown_request(<code>f</code>) <a class="header-anchor" href="#teardown-request-f" aria-label="Permalink to &quot;teardown_request(`f`)&quot;">​</a></h4><p>Register a function to be called when the request context is popped. Typically this happens at the end of each request, but contexts may be pushed manually as well during testing.</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;font-style:italic;">with</span><span style="color:#A6ACCD;"> app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">test_request_context</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">    ...</span></span></code></pre></div><p>When the <code>with</code> block exits (or <code>ctx.pop()</code> is called), the teardown functions are called just before the request context is made inactive.</p><p>When a teardown function was called because of an unhandled exception it will be passed an error object. If an <code>errorhandler()</code> is registered, it will handle the exception and the teardown will not receive it.</p><p>Teardown functions must avoid raising exceptions. If they execute code that might fail they must surround that code with a <code>try/except</code> block and log any errors.</p><p>The return values of teardown functions are ignored.</p><p>This is available on both app and blueprint objects. When used on an app, this executes after every request. When used on a blueprint, this executes after every request that the blueprint handles. To register with a blueprint and execute after every request, use <code>Blueprint.teardown_app_request()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_teardown)</code> –</li></ul><p><em>Return type</em>: <code>T_teardown</code></p></li><li><h4 id="teardown-request-funcs-dict-ft-apporblueprintkey-list-ft-teardowncallable" tabindex="-1">teardown_request_funcs: <code>dict[ft.AppOrBlueprintKey, list[ft.TeardownCallable]]</code> <a class="header-anchor" href="#teardown-request-funcs-dict-ft-apporblueprintkey-list-ft-teardowncallable" aria-label="Permalink to &quot;teardown_request_funcs: `dict[ft.AppOrBlueprintKey, list[ft.TeardownCallable]]`&quot;">​</a></h4><p>A data structure of functions to call at the end of each request even if an exception is raised, in the format <code>{scope: [functions]}</code>. The <code>scope</code> key is the name of a blueprint the functions are active for, or <code>None</code> for all requests.</p><p>To register a function, use the <code>teardown_request()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li><li><h4 id="template-context-processors-dict-ft-apporblueprintkey-list-ft-templatecontextprocessorcallable" tabindex="-1">template_context_processors: <code>dict[ft.AppOrBlueprintKey, list[ft.TemplateContextProcessorCallable]]</code> <a class="header-anchor" href="#template-context-processors-dict-ft-apporblueprintkey-list-ft-templatecontextprocessorcallable" aria-label="Permalink to &quot;template_context_processors: `dict[ft.AppOrBlueprintKey, list[ft.TemplateContextProcessorCallable]]`&quot;">​</a></h4><p>A data structure of functions to call to pass extra context values when rendering templates, in the format <code>{scope: [functions]}</code>. The <code>scope</code> key is the name of a blueprint the functions are active for, or <code>None</code> for all requests.</p><p>To register a function, use the <code>context_processor()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li><li><h4 id="template-filter-name-none" tabindex="-1">template_filter(<code>name=None</code>) <a class="header-anchor" href="#template-filter-name-none" aria-label="Permalink to &quot;template_filter(`name=None`)&quot;">​</a></h4><p>A decorator that is used to register custom template filter. You can specify a name for the filter, otherwise the function name will be used. Example:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">template_filter</span><span style="color:#89DDFF;">()</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">reverse</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">s</span><span style="color:#89DDFF;">):</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#A6ACCD;"> s</span><span style="color:#89DDFF;">[::-</span><span style="color:#F78C6C;">1</span><span style="color:#89DDFF;">]</span></span></code></pre></div><p><em>Parameters</em>:</p><ul><li><code>name (str | None)</code> – the optional name of the filter, otherwise the function name will be used.</li></ul><p><em>Return type</em>: <code>Callable[[T_template_filter], T_template_filter]</code></p></li><li><h4 id="template-folder" tabindex="-1">template_folder <a class="header-anchor" href="#template-folder" aria-label="Permalink to &quot;template_folder&quot;">​</a></h4><p>The path to the templates folder, relative to <code>root_path</code>, to add to the template loader. <code>None</code> if templates should not be added.</p></li><li><h4 id="template-global-name-none" tabindex="-1">template_global(<code>name=None</code>) <a class="header-anchor" href="#template-global-name-none" aria-label="Permalink to &quot;template_global(`name=None`)&quot;">​</a></h4><p>A decorator that is used to register a custom template global function. You can specify a name for the global function, otherwise the function name will be used. <em>Example</em>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">template_global</span><span style="color:#89DDFF;">()</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">double</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">n</span><span style="color:#89DDFF;">):</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">2</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">*</span><span style="color:#A6ACCD;"> n</span></span></code></pre></div><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.10.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>name (str | None)</code> – the optional name of the global function, otherwise the function name will be used.</li></ul><p><em>Return type</em>: <code>Callable[[T_template_global], T_template_global]</code></p></li><li><h4 id="template-test-name-none" tabindex="-1">template_test(<code>name=None</code>) <a class="header-anchor" href="#template-test-name-none" aria-label="Permalink to &quot;template_test(`name=None`)&quot;">​</a></h4><p>A decorator that is used to register custom template test. You can specify a name for the test, otherwise the function name will be used. Example:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">template_test</span><span style="color:#89DDFF;">()</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">is_prime</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">n</span><span style="color:#89DDFF;">):</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">if</span><span style="color:#A6ACCD;"> n </span><span style="color:#89DDFF;">==</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">2</span><span style="color:#89DDFF;">:</span></span>\n<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">True</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">for</span><span style="color:#A6ACCD;"> i </span><span style="color:#89DDFF;font-style:italic;">in</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">range</span><span style="color:#89DDFF;">(</span><span style="color:#F78C6C;">2</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#FFCB6B;">int</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">math</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">ceil</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">math</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">sqrt</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">n</span><span style="color:#89DDFF;">)))</span><span style="color:#82AAFF;"> </span><span style="color:#89DDFF;">+</span><span style="color:#82AAFF;"> </span><span style="color:#F78C6C;">1</span><span style="color:#89DDFF;">):</span></span>\n<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#89DDFF;font-style:italic;">if</span><span style="color:#A6ACCD;"> n </span><span style="color:#89DDFF;">%</span><span style="color:#A6ACCD;"> i </span><span style="color:#89DDFF;">==</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">0</span><span style="color:#89DDFF;">:</span></span>\n<span class="line"><span style="color:#A6ACCD;">            </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">False</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">True</span></span></code></pre></div><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.10.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>name (str | None)</code> – the optional name of the test, otherwise the function name will be used.</li></ul><p><em>Return type</em>: <code>Callable[[T_template_test], T_template_test]</code></p></li><li><h4 id="test-cli-runner-kwargs" tabindex="-1">test_cli_runner(<code>**kwargs</code>) <a class="header-anchor" href="#test-cli-runner-kwargs" aria-label="Permalink to &quot;test_cli_runner(`**kwargs`)&quot;">​</a></h4><p>Create a CLI runner for testing CLI commands. See <a href="/python/flask/user_guide/application#running-commands-with-the-cli-runner">Running Commands with the CLI Runner</a>.</p><p>Returns an instance of <code>test_cli_runner_class</code>, by default <code>FlaskCliRunner</code>. The Flask app object is passed as the first argument.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 1.0.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>kwargs (t.Any)</code> –</li></ul><p><em>Return type</em>: <code>FlaskCliRunner</code></p></li><li><h4 id="test-cli-runner-class-type-flaskclirunner-none-none" tabindex="-1">test_cli_runner_class: <code>type[FlaskCliRunner] | None = None</code> <a class="header-anchor" href="#test-cli-runner-class-type-flaskclirunner-none-none" aria-label="Permalink to &quot;test_cli_runner_class: `type[FlaskCliRunner] | None = None`&quot;">​</a></h4><p>The <code>CliRunner</code> subclass, by default <code>FlaskCliRunner</code> that is used by <code>test_cli_runner()</code>. Its <code>__init__</code> method should take a <code>Flask</code> app object as the first argument.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 1.0.</em></p></details></li><li><h4 id="test-client-use-cookies-true-kwargs" tabindex="-1">test_client(<code>use_cookies=True, **kwargs</code>) <a class="header-anchor" href="#test-client-use-cookies-true-kwargs" aria-label="Permalink to &quot;test_client(`use_cookies=True, **kwargs`)&quot;">​</a></h4><p>Creates a test client for this application. For information about unit testing head over to <a href="/python/flask/user_guide/application#testing-flask-applications">Testing Flask Applications</a>.</p><p>Note that if you are testing for assertions or exceptions in your application code, you must set <code>app.testing = True</code> in order for the exceptions to propagate to the test client. Otherwise, the exception will be handled by the application (not visible to the test client) and the only indication of an <code>AssertionError</code> or other exception will be a 500 status code response to the test client. See the <code>testing</code> attribute. For example:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">app</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">testing</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">True</span></span>\n<span class="line"><span style="color:#A6ACCD;">client </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">test_client</span><span style="color:#89DDFF;">()</span></span></code></pre></div><p>The test client can be used in a <code>with</code> block to defer the closing down of the context until the end of the <code>with</code> block. This is useful if you want to access the context locals for testing:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;font-style:italic;">with</span><span style="color:#A6ACCD;"> app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">test_client</span><span style="color:#89DDFF;">()</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;font-style:italic;">as</span><span style="color:#A6ACCD;"> c</span><span style="color:#89DDFF;">:</span></span>\n<span class="line"><span style="color:#A6ACCD;">    rv </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> c</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">get</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">/?vodka=42</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">assert</span><span style="color:#A6ACCD;"> request</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">args</span><span style="color:#89DDFF;">[</span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">vodka</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">]</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">==</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">42</span><span style="color:#89DDFF;">&#39;</span></span></code></pre></div><p>Additionally, you may pass optional keyword arguments that will then be passed to the application’s <code>test_client_class</code> constructor. For example:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;font-style:italic;">from</span><span style="color:#A6ACCD;"> flask</span><span style="color:#89DDFF;">.</span><span style="color:#A6ACCD;">testing </span><span style="color:#89DDFF;font-style:italic;">import</span><span style="color:#A6ACCD;"> FlaskClient</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#C792EA;">class</span><span style="color:#A6ACCD;"> </span><span style="color:#FFCB6B;">CustomClient</span><span style="color:#89DDFF;">(</span><span style="color:#FFCB6B;">FlaskClient</span><span style="color:#89DDFF;">):</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">__init__</span><span style="color:#89DDFF;">(</span><span style="color:#F07178;font-style:italic;">self</span><span style="color:#89DDFF;">,</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">*</span><span style="color:#A6ACCD;font-style:italic;">args</span><span style="color:#89DDFF;">,</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">**</span><span style="color:#A6ACCD;font-style:italic;">kwargs</span><span style="color:#89DDFF;">):</span></span>\n<span class="line"><span style="color:#A6ACCD;">        self</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">_authentication</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> kwargs</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">pop</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">authentication</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#FFCB6B;">super</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">CustomClient</span><span style="color:#89DDFF;">,</span><span style="color:#A6ACCD;">self</span><span style="color:#89DDFF;">).</span><span style="color:#82AAFF;">__init__</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;"> </span><span style="color:#89DDFF;">*</span><span style="color:#82AAFF;">args</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#89DDFF;">**</span><span style="color:#82AAFF;">kwargs</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#A6ACCD;">app</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">test_client_class</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> CustomClient</span></span>\n<span class="line"><span style="color:#A6ACCD;">client </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">test_client</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">authentication</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">Basic ....</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">)</span></span></code></pre></div><p>See <code>FlaskClient</code> for more information.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 0.11</em>: Added <code>**kwargs</code> to support passing additional keyword arguments to the constructor of <code>test_client_class</code>.</p><p><em>New in version 0.7</em>: The use_cookies parameter was added as well as the ability to override the client to be used by setting the <code>test_client_class</code> attribute.</p><p><em>Changed in version 0.4</em>: added support for <code>with</code> block usage for the client.</p></details><p><em>Parameters</em>:</p><ul><li><p><code>use_cookies (bool)</code> –</p></li><li><p><code>kwargs (t.Any)</code> –</p></li></ul><p><em>Return type</em>: <code>FlaskClient</code></p></li><li><h4 id="test-client-class-type-flaskclient-none-none" tabindex="-1">test_client_class: <code>type[FlaskClient] | None = None</code> <a class="header-anchor" href="#test-client-class-type-flaskclient-none-none" aria-label="Permalink to &quot;test_client_class: `type[FlaskClient] | None = None`&quot;">​</a></h4><p>The <code>test_client()</code> method creates an instance of this test client class. Defaults to <code>FlaskClient</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.7.</em></p></details></li><li><h4 id="test-request-context-args-kwargs" tabindex="-1">test_request_context(<code>*args, **kwargs</code>) <a class="header-anchor" href="#test-request-context-args-kwargs" aria-label="Permalink to &quot;test_request_context(`*args, **kwargs`)&quot;">​</a></h4><p>Create a <code>RequestContext</code> for a WSGI environment created from the given values. This is mostly useful during testing, where you may want to run a function that uses request data without dispatching a full request.</p><p>See <a href="/python/flask/user_guide/request_context#the-request-context">The Request Context</a>.</p><p>Use a <code>with</code> block to push the context, which will make <code>request</code> point at the request for the created environment.</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;font-style:italic;">with</span><span style="color:#A6ACCD;"> app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">test_request_context</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">...</span><span style="color:#89DDFF;">):</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#82AAFF;">generate_report</span><span style="color:#89DDFF;">()</span></span></code></pre></div><p>When using the shell, it may be easier to push and pop the context manually to avoid indentation.</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">ctx </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">test_request_context</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">...</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#A6ACCD;">ctx</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">push</span><span style="color:#89DDFF;">()</span></span>\n<span class="line"><span style="color:#A6ACCD;">...</span></span>\n<span class="line"><span style="color:#A6ACCD;">ctx</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">pop</span><span style="color:#89DDFF;">()</span></span></code></pre></div><p>Takes the same arguments as Werkzeug’s <code>EnvironBuilder</code>, with some defaults from the application. See the linked Werkzeug docs for most of the available arguments. Flask-specific behavior is listed here.</p><p><em>Parameters</em>:</p><ul><li><p><code>path</code> – URL path being requested.</p></li><li><p><code>base_url</code> – Base URL where the app is being served, which <code>path</code> is relative to. If not given, built from <code>PREFERRED_URL_SCHEME</code>, <code>subdomain</code>, <code>SERVER_NAME</code>, and <code>APPLICATION_ROOT</code>.</p></li><li><p><code>subdomain</code> – Subdomain name to append to <code>SERVER_NAME</code>.</p></li><li><p><code>url_scheme</code> – Scheme to use instead of <code>PREFERRED_URL_SCHEME</code>.</p></li><li><p><code>data</code> – The request body, either as a string or a dict of form keys and values.</p></li><li><p><code>json</code> – If given, this is serialized as JSON and passed as <code>data</code>. Also defaults <code>content_type</code> to <code>application/json</code>.</p></li><li><p><code>args (Any)</code> – other positional arguments passed to <code>EnvironBuilder</code>.</p></li><li><p><code>kwargs (Any)</code> – other keyword arguments passed to <code>EnvironBuilder</code>.</p></li></ul><p><em>Return type</em>: <code>RequestContext</code></p></li><li><h4 id="testing" tabindex="-1">testing <a class="header-anchor" href="#testing" aria-label="Permalink to &quot;testing&quot;">​</a></h4><p>The testing flag. Set this to <code>True</code> to enable the test mode of Flask extensions (and in the future probably also Flask itself). For example this might activate test helpers that have an additional runtime cost which should not be enabled by default.</p><p>If this is enabled and <code>PROPAGATE_EXCEPTIONS</code> is not changed from the default it’s implicitly enabled.</p><p>This attribute can also be configured from the config with the <code>TESTING</code> configuration key. Defaults to <code>False</code>.</p></li><li><h4 id="trap-http-exception-e" tabindex="-1">trap_http_exception(<code>e</code>) <a class="header-anchor" href="#trap-http-exception-e" aria-label="Permalink to &quot;trap_http_exception(`e`)&quot;">​</a></h4><p>Checks if an HTTP exception should be trapped or not. By default this will return <code>False</code> for all exceptions except for a bad request key error if <code>TRAP_BAD_REQUEST_ERRORS</code> is set to True. It also returns True if <code>TRAP_HTTP_EXCEPTIONS</code> is set to <code>True</code>.</p><p>This is called for all HTTP exceptions raised by a view function. If it returns <code>True</code> for any exception the error handler for this exception is not called and it shows up as regular exception in the traceback. This is helpful for debugging implicitly raised HTTP exceptions.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 1.0</em>: Bad request errors are not trapped by default in debug mode.</p><p><em>New in version 0.8.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>e (Exception)</code> –</li></ul><p><em>Return type</em>: <code>bool</code></p></li><li><h4 id="update-template-context-context" tabindex="-1">update_template_context(<code>context</code>) <a class="header-anchor" href="#update-template-context-context" aria-label="Permalink to &quot;update_template_context(`context`)&quot;">​</a></h4><p>Update the template context with some commonly used variables. This injects request, session, config and g into the template context as well as everything template context processors want to inject. Note that the as of Flask 0.6, the original values in the context will not be overridden if a context processor decides to return a value with the same key.</p><p><em>Parameters</em>:</p><ul><li><code>context (dict)</code> – the context as a dictionary that is updated in place to add extra variables.</li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h4 id="url-build-error-handlers-list-t-callable-exception-str-dict-str-t-any-str" tabindex="-1">url_build_error_handlers: <code>list[t.Callable[[Exception, str, dict[str, t.Any]], str]]</code> <a class="header-anchor" href="#url-build-error-handlers-list-t-callable-exception-str-dict-str-t-any-str" aria-label="Permalink to &quot;url_build_error_handlers: `list[t.Callable[[Exception, str, dict[str, t.Any]], str]]`&quot;">​</a></h4><p>A list of functions that are called by <code>handle_url_build_error()</code> when <code>url_for()</code> raises a <code>BuildError</code>. Each function is called with <code>error</code>, <code>endpoint</code> and <code>values</code>. If a function returns <code>None</code> or raises a <code>BuildError</code>, it is skipped. Otherwise, its return value is returned by <code>url_for</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.9.</em></p></details></li><li><h4 id="url-default-functions-dict-ft-apporblueprintkey-list-ft-urldefaultcallable" tabindex="-1">url_default_functions: <code>dict[ft.AppOrBlueprintKey, list[ft.URLDefaultCallable]]</code> <a class="header-anchor" href="#url-default-functions-dict-ft-apporblueprintkey-list-ft-urldefaultcallable" aria-label="Permalink to &quot;url_default_functions: `dict[ft.AppOrBlueprintKey, list[ft.URLDefaultCallable]]`&quot;">​</a></h4><p>A data structure of functions to call to modify the keyword arguments when generating URLs, in the format <code>{scope: [functions]}</code>. The <code>scope</code> key is the name of a blueprint the functions are active for, or <code>None</code> for all requests.</p><p>To register a function, use the <code>url_defaults()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li><li><h4 id="url-defaults-f" tabindex="-1">url_defaults(<code>f</code>) <a class="header-anchor" href="#url-defaults-f" aria-label="Permalink to &quot;url_defaults(`f`)&quot;">​</a></h4><p>Callback function for URL defaults for all view functions of the application. It’s called with the endpoint and values and should update the values passed in place.</p><p>This is available on both app and blueprint objects. When used on an app, this is called for every request. When used on a blueprint, this is called for requests that the blueprint handles. To register with a blueprint and affect every request, use <code>Blueprint.app_url_defaults()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_url_defaults)</code> –</li></ul><p><em>Return type</em>: <code>T_url_defaults</code></p></li><li><h4 id="url-for-endpoint-anchor-none-method-none-scheme-none-external-none-values" tabindex="-1">url_for(<code>endpoint, *, _anchor=None, _method=None, _scheme=None, _external=None, **values</code>) <a class="header-anchor" href="#url-for-endpoint-anchor-none-method-none-scheme-none-external-none-values" aria-label="Permalink to &quot;url_for(`endpoint, *, _anchor=None, _method=None, _scheme=None, _external=None, **values`)&quot;">​</a></h4><p>Generate a URL to the given endpoint with the given values.</p><p>This is called by <code>flask.url_for()</code>, and can be called directly as well.</p><p>An endpoint is the name of a URL rule, usually added with <code>@app.route()</code>, and usually the same name as the view function. A route defined in a <code>Blueprint</code> will prepend the blueprint’s name separated by a <code>.</code> to the endpoint.</p><p>In some cases, such as email messages, you want URLs to include the scheme and domain, like <code>https://example.com/hello</code>. When not in an active request, URLs will be external by default, but this requires setting <code>SERVER_NAME</code> so Flask knows what domain to use. <code>APPLICATION_ROOT</code> and <code>PREFERRED_URL_SCHEME</code> should also be configured as needed. This config is only used when not in an active request.</p><p>Functions can be decorated with <code>url_defaults()</code> to modify keyword arguments before the URL is built.</p><p>If building fails for some reason, such as an unknown endpoint or incorrect values, the app’s <code>handle_url_build_error()</code> method is called. If that returns a string, that is returned, otherwise a <code>BuildError</code> is raised.</p><p><em>Parameters</em>:</p><ul><li><p><code>endpoint (str)</code> – The endpoint name associated with the URL to generate. If this starts with a ., the current blueprint name (if any) will be used.</p></li><li><p><code>_anchor (str | None)</code> – If given, append this as <code>#anchor</code> to the URL.</p></li><li><p><code>_method (str | None)</code> – If given, generate the URL associated with this method for the endpoint.</p></li><li><p><code>_scheme (str | None)</code> – If given, the URL will have this scheme if it is external.</p></li><li><p><code>_external (bool | None) </code>– If given, prefer the URL to be internal (False) or require it to be external (True). External URLs include the scheme and domain. When not in an active request, URLs are external by default.</p></li><li><p><code>values (Any)</code> – Values to use for the variable parts of the URL rule. Unknown keys are appended as query string arguments, like <code>?a=b&amp;c=d</code>.</p></li></ul><p><em>Return type</em>: <code>str</code></p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.2</em>: Moved from <code>flask.url_for</code>, which calls this method.</p></details></li><li><h4 id="url-map" tabindex="-1">url_map <a class="header-anchor" href="#url-map" aria-label="Permalink to &quot;url_map&quot;">​</a></h4><p>The <code>Map</code> for this instance. You can use this to change the routing converters after the class was created but before any routes are connected. Example:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;font-style:italic;">from</span><span style="color:#A6ACCD;"> werkzeug</span><span style="color:#89DDFF;">.</span><span style="color:#A6ACCD;">routing </span><span style="color:#89DDFF;font-style:italic;">import</span><span style="color:#A6ACCD;"> BaseConverter</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#C792EA;">class</span><span style="color:#A6ACCD;"> </span><span style="color:#FFCB6B;">ListConverter</span><span style="color:#89DDFF;">(</span><span style="color:#FFCB6B;">BaseConverter</span><span style="color:#89DDFF;">):</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">to_python</span><span style="color:#89DDFF;">(</span><span style="color:#F07178;font-style:italic;">self</span><span style="color:#89DDFF;">,</span><span style="color:#A6ACCD;"> </span><span style="color:#A6ACCD;font-style:italic;">value</span><span style="color:#89DDFF;">):</span></span>\n<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#A6ACCD;"> value</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">split</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">,</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">to_url</span><span style="color:#89DDFF;">(</span><span style="color:#F07178;font-style:italic;">self</span><span style="color:#89DDFF;">,</span><span style="color:#A6ACCD;"> </span><span style="color:#A6ACCD;font-style:italic;">values</span><span style="color:#89DDFF;">):</span></span>\n<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">,</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">join</span><span style="color:#89DDFF;">(</span><span style="color:#FFCB6B;">super</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">ListConverter</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#A6ACCD;">self</span><span style="color:#89DDFF;">).</span><span style="color:#82AAFF;">to_url</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">value</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#82AAFF;">                        </span><span style="color:#89DDFF;font-style:italic;">for</span><span style="color:#82AAFF;"> value </span><span style="color:#89DDFF;font-style:italic;">in</span><span style="color:#82AAFF;"> values</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#A6ACCD;">app </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">Flask</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;">__name__</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#A6ACCD;">app</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">url_map</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">converters</span><span style="color:#89DDFF;">[</span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">list</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">]</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> ListConverter</span></span></code></pre></div></li><li><h4 id="url-map-class" tabindex="-1">url_map_class <a class="header-anchor" href="#url-map-class" aria-label="Permalink to &quot;url_map_class&quot;">​</a></h4><p>alias of <code>Map</code></p></li><li><h4 id="url-rule-class" tabindex="-1">url_rule_class <a class="header-anchor" href="#url-rule-class" aria-label="Permalink to &quot;url_rule_class&quot;">​</a></h4><p>alias of <code>Rule</code></p></li><li><h4 id="url-value-preprocessor-f" tabindex="-1">url_value_preprocessor(<code>f</code>) <a class="header-anchor" href="#url-value-preprocessor-f" aria-label="Permalink to &quot;url_value_preprocessor(`f`)&quot;">​</a></h4><p>Register a URL value preprocessor function for all view functions in the application. These functions will be called before the <code>before_request()</code> functions.</p><p>The function can modify the values captured from the matched url before they are passed to the view. For example, this can be used to pop a common language code value and place it in g rather than pass it to every view.</p><p>The function is passed the endpoint name and values dict. The return value is ignored.</p><p>This is available on both app and blueprint objects. When used on an app, this is called for every request. When used on a blueprint, this is called for requests that the blueprint handles. To register with a blueprint and affect every request, use <code>Blueprint.app_url_value_preprocessor()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_url_value_preprocessor)</code> –</li></ul><p><em>Return type</em>: <code>T_url_value_preprocessor</code></p></li><li><h4 id="url-value-preprocessors-dict-ft-apporblueprintkey-list-ft-urlvaluepreprocessorcallable" tabindex="-1">url_value_preprocessors: <code>dict[ft.AppOrBlueprintKey, list[ft.URLValuePreprocessorCallable]]</code> <a class="header-anchor" href="#url-value-preprocessors-dict-ft-apporblueprintkey-list-ft-urlvaluepreprocessorcallable" aria-label="Permalink to &quot;url_value_preprocessors: `dict[ft.AppOrBlueprintKey, list[ft.URLValuePreprocessorCallable]]`&quot;">​</a></h4><p>A data structure of functions to call to modify the keyword arguments passed to the view function, in the format <code>{scope: [functions]}</code>. The <code>scope</code> key is the name of a blueprint the functions are active for, or <code>None</code> for all requests.</p><p>To register a function, use the <code>url_value_preprocessor()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li><li><h4 id="view-functions-dict-str-t-callable" tabindex="-1">view_functions: <code>dict[str, t.Callable]</code> <a class="header-anchor" href="#view-functions-dict-str-t-callable" aria-label="Permalink to &quot;view_functions: `dict[str, t.Callable]`&quot;">​</a></h4><p>A dictionary mapping endpoint names to view functions.</p><p>To register a view function, use the <code>route()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li><li><h4 id="wsgi-app-environ-start-response" tabindex="-1">wsgi_app(<code>environ, start_response</code>) <a class="header-anchor" href="#wsgi-app-environ-start-response" aria-label="Permalink to &quot;wsgi_app(`environ, start_response`)&quot;">​</a></h4><p>The actual WSGI application. This is not implemented in <code>__call__()</code> so that middlewares can be applied without losing a reference to the app object. Instead of doing this:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">app </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">MyMiddleware</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">)</span></span></code></pre></div><p>It’s a better idea to do this instead:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">app</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">wsgi_app</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">MyMiddleware</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">wsgi_app</span><span style="color:#89DDFF;">)</span></span></code></pre></div><p>Then you still have the original application object around and can continue to call methods on it.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 0.7</em>: Teardown events for the request and app contexts are called even if an unhandled error occurs. Other events may not be called depending on when an error occurs during dispatch. See <a href="/python/flask/user_guide/request_context#callbacks-and-errors">Callbacks and Errors</a>.</p></details><p><em>Parameters</em>:</p><ul><li><p><code>environ (dict)</code> – A WSGI environment.</p></li><li><p><code>start_response (Callable)</code> – A callable accepting a status code, a list of headers, and an optional exception context to start the response.</p></li></ul><p><em>Return type</em>: <code>Any</code></p></li></ul></li></ul>',4),l=[n];function r(i,c,p,d,u,h){return o(),a("div",null,l)}const f=e(s,[["render",r]]);export{y as __pageData,f as default};
