import{_ as e,o as t,c as o,X as s}from"./chunks/framework.b5656a4e.js";const m=JSON.parse('{"title":"Fixtures","description":"","frontmatter":{},"headers":[],"relativePath":"python/pytest/reference_guides/api_reference/fixtures.md","filePath":"python/pytest/reference_guides/api_reference/fixtures.md","lastUpdated":1692807718000}'),a={name:"python/pytest/reference_guides/api_reference/fixtures.md"},n=s(`<h1 id="fixtures" tabindex="-1">Fixtures <a class="header-anchor" href="#fixtures" aria-label="Permalink to &quot;Fixtures {#fixtures}&quot;">​</a></h1><p><em>Tutorial</em>: <a href="/python/pytest/reference_guides/fixture_reference#fixtures-reference">Fixtures reference</a></p><p>Fixtures are requested by test functions or other fixtures by declaring them as argument names.</p><p>Example of a test requiring a fixture:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">test_output</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">capsys</span><span style="color:#89DDFF;">):</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#82AAFF;">print</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">hello</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>
<span class="line"><span style="color:#A6ACCD;">    out</span><span style="color:#89DDFF;">,</span><span style="color:#A6ACCD;"> err </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> capsys</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">readouterr</span><span style="color:#89DDFF;">()</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">assert</span><span style="color:#A6ACCD;"> out </span><span style="color:#89DDFF;">==</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">hello</span><span style="color:#A6ACCD;">\\n</span><span style="color:#89DDFF;">&quot;</span></span></code></pre></div><p>Example of a fixture requiring another fixture:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">pytest</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">fixture</span></span>
<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">db_session</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">tmp_path</span><span style="color:#89DDFF;">):</span></span>
<span class="line"><span style="color:#A6ACCD;">    fn </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> tmp_path </span><span style="color:#89DDFF;">/</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">db.file</span><span style="color:#89DDFF;">&quot;</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">connect</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">fn</span><span style="color:#89DDFF;">)</span></span></code></pre></div><p>For more details, consult the full <a href="/python/pytest/reference_guides/fixture_reference#fixtures-reference">fixtures docs</a>.</p><h2 id="pytest-fixture" tabindex="-1">@pytest.fixture <a class="header-anchor" href="#pytest-fixture" aria-label="Permalink to &quot;@pytest.fixture {#pytest-fixture}&quot;">​</a></h2><ul><li><h3 id="fixture-fixture-function-fixturefunction-scope-union-literal-session-package-module-class-function-callable-str-config-literal-session-package-module-class-function-function-params-optional-iterable-object-none-autouse-bool-false-ids-optional-union-sequence-optional-object-callable-any-optional-object-none-name-optional-str-none-→-fixturefunction" tabindex="-1">@fixture(<code>fixture_function: FixtureFunction, *, scope: Union[Literal[&#39;session&#39;, &#39;package&#39;, &#39;module&#39;, &#39;class&#39;, &#39;function&#39;], Callable[[str, Config], Literal[&#39;session&#39;, &#39;package&#39;, &#39;module&#39;, &#39;class&#39;, &#39;function&#39;]]] = &#39;function&#39;, params: Optional[Iterable[object]] = None, autouse: bool = False, ids: Optional[Union[Sequence[Optional[object]], Callable[[Any], Optional[object]]]] = None, name: Optional[str] = None</code>) → <code>FixtureFunction</code> <a class="header-anchor" href="#fixture-fixture-function-fixturefunction-scope-union-literal-session-package-module-class-function-callable-str-config-literal-session-package-module-class-function-function-params-optional-iterable-object-none-autouse-bool-false-ids-optional-union-sequence-optional-object-callable-any-optional-object-none-name-optional-str-none-→-fixturefunction" aria-label="Permalink to &quot;@fixture(\`fixture_function: FixtureFunction, *, scope: Union[Literal[&#39;session&#39;, &#39;package&#39;, &#39;module&#39;, &#39;class&#39;, &#39;function&#39;], Callable[[str, Config], Literal[&#39;session&#39;, &#39;package&#39;, &#39;module&#39;, &#39;class&#39;, &#39;function&#39;]]] = &#39;function&#39;, params: Optional[Iterable[object]] = None, autouse: bool = False, ids: Optional[Union[Sequence[Optional[object]], Callable[[Any], Optional[object]]]] = None, name: Optional[str] = None\`) → \`FixtureFunction\`&quot;">​</a></h3></li><li><h3 id="fixture-fixture-function-none-none-scope-union-literal-session-package-module-class-function-callable-str-config-literal-session-package-module-class-function-function-params-optional-iterable-object-none-autouse-bool-false-ids-optional-union-sequence-optional-object-callable-any-optional-object-none-name-optional-str-none-→-fixturefunctionmarker" tabindex="-1">@fixture(<code>fixture_function: None = None, *, scope: Union[Literal[&#39;session&#39;, &#39;package&#39;, &#39;module&#39;, &#39;class&#39;, &#39;function&#39;], Callable[[str, Config], Literal[&#39;session&#39;, &#39;package&#39;, &#39;module&#39;, &#39;class&#39;, &#39;function&#39;]]] = &#39;function&#39;, params: Optional[Iterable[object]] = None, autouse: bool = False, ids: Optional[Union[Sequence[Optional[object]], Callable[[Any], Optional[object]]]] = None, name: Optional[str] = None</code>) → <code>FixtureFunctionMarker</code> <a class="header-anchor" href="#fixture-fixture-function-none-none-scope-union-literal-session-package-module-class-function-callable-str-config-literal-session-package-module-class-function-function-params-optional-iterable-object-none-autouse-bool-false-ids-optional-union-sequence-optional-object-callable-any-optional-object-none-name-optional-str-none-→-fixturefunctionmarker" aria-label="Permalink to &quot;@fixture(\`fixture_function: None = None, *, scope: Union[Literal[&#39;session&#39;, &#39;package&#39;, &#39;module&#39;, &#39;class&#39;, &#39;function&#39;], Callable[[str, Config], Literal[&#39;session&#39;, &#39;package&#39;, &#39;module&#39;, &#39;class&#39;, &#39;function&#39;]]] = &#39;function&#39;, params: Optional[Iterable[object]] = None, autouse: bool = False, ids: Optional[Union[Sequence[Optional[object]], Callable[[Any], Optional[object]]]] = None, name: Optional[str] = None\`) → \`FixtureFunctionMarker\`&quot;">​</a></h3><p>Decorator to mark a fixture factory function.</p><p>This decorator can be used, with or without parameters, to define a fixture function.</p><p>The name of the fixture function can later be referenced to cause its invocation ahead of running tests: test modules or classes can use the <code>pytest.mark.usefixtures(fixturename)</code> marker.</p><p>Test functions can directly use fixture names as input arguments in which case the fixture instance returned from the fixture function will be injected.</p><p>Fixtures can provide their values to test functions using <code>return</code> or <code>yield</code> statements. When using <code>yield</code> the code block after the <code>yield</code> statement is executed as teardown code regardless of the test outcome, and must yield exactly once.</p><p><em>Parameters</em>:</p><ul><li><p><code>scope</code> – The scope for which this fixture is shared; one of <code>&quot;function&quot;</code> (default), <code>&quot;class&quot;</code>, <code>&quot;module&quot;</code>, <code>&quot;package&quot;</code> or <code>&quot;session&quot;</code>. This parameter may also be a callable which receives <code>(fixture_name, config)</code> as parameters, and must return a str with one of the values mentioned above. See <a href="/python/pytest/how_to_guides/fixture#dynamic-scope">Dynamic scope</a> in the docs for more information.</p></li><li><p><code>params</code> – An optional list of parameters which will cause multiple invocations of the fixture function and all of the tests using it. The current parameter is available in <code>request.param</code>.</p></li><li><p><code>autouse</code> – If <code>True</code>, the fixture func is activated for all tests that can see it. If <code>False</code> (the default), an explicit reference is needed to activate the fixture.</p></li><li><p><code>ids</code> – Sequence of ids each corresponding to the params so that they are part of the test id. If no ids are provided they will be generated automatically from the params.</p></li><li><p><code>name</code> – The name of the fixture. This defaults to the name of the decorated function. If a fixture is used in the same module in which it is defined, the function name of the fixture will be shadowed by the function arg that requests the fixture; one way to resolve this is to name the decorated function <code>fixture_&lt;fixturename&gt;</code> and then use <code>@pytest.fixture(name=&#39;&lt;fixturename&gt;&#39;)</code>.</p></li></ul></li></ul><h2 id="capfd" tabindex="-1">capfd <a class="header-anchor" href="#capfd" aria-label="Permalink to &quot;capfd {#capfd}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="/python/pytest/how_to_guides/output#how-to-capture-stdout-stderr-output">How to capture stdout/stderr output</a></p><ul><li><h3 id="capfd-1" tabindex="-1">capfd() <a class="header-anchor" href="#capfd-1" aria-label="Permalink to &quot;capfd()&quot;">​</a></h3><p>Enable text capturing of writes to file descriptors <code>1</code> and <code>2</code>.</p><p>The captured output is made available via <code>capfd.readouterr()</code> method calls, which return a <code>(out, err)</code> namedtuple. <code>out</code> and <code>err</code> will be <code>text</code> objects.</p><p>Returns an instance of <code>CaptureFixture[str]</code>.</p><p><em>Example</em>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">test_system_echo</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">capfd</span><span style="color:#89DDFF;">):</span></span>
<span class="line"><span style="color:#A6ACCD;">    os</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">system</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">echo &quot;hello&quot;</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">)</span></span>
<span class="line"><span style="color:#A6ACCD;">    captured </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> capfd</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">readouterr</span><span style="color:#89DDFF;">()</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">assert</span><span style="color:#A6ACCD;"> captured</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">out</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">==</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">hello</span><span style="color:#A6ACCD;">\\n</span><span style="color:#89DDFF;">&quot;</span></span></code></pre></div></li></ul><h2 id="capfdbinary" tabindex="-1">capfdbinary <a class="header-anchor" href="#capfdbinary" aria-label="Permalink to &quot;capfdbinary {#capfdbinary}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="/python/pytest/how_to_guides/output#how-to-capture-stdout-stderr-output">How to capture stdout/stderr output</a></p><ul><li><h3 id="capfdbinary-1" tabindex="-1">capfdbinary() <a class="header-anchor" href="#capfdbinary-1" aria-label="Permalink to &quot;capfdbinary()&quot;">​</a></h3><p>Enable bytes capturing of writes to file descriptors <code>1</code> and <code>2</code>.</p><p>The captured output is made available via <code>capfd.readouterr()</code> method calls, which return a <code>(out, err)</code> namedtuple. <code>out</code> and <code>err</code> will be <code>byte</code> objects.</p><p>Returns an instance of <code>CaptureFixture[bytes]</code>.</p><p><em>Example</em>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">test_system_echo</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">capfdbinary</span><span style="color:#89DDFF;">):</span></span>
<span class="line"><span style="color:#A6ACCD;">    os</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">system</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">echo &quot;hello&quot;</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">)</span></span>
<span class="line"><span style="color:#A6ACCD;">    captured </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> capfdbinary</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">readouterr</span><span style="color:#89DDFF;">()</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">assert</span><span style="color:#A6ACCD;"> captured</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">out</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">==</span><span style="color:#A6ACCD;"> </span><span style="color:#C792EA;">b</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">hello</span><span style="color:#A6ACCD;">\\n</span><span style="color:#89DDFF;">&quot;</span></span></code></pre></div></li></ul><h2 id="caplog" tabindex="-1">caplog <a class="header-anchor" href="#caplog" aria-label="Permalink to &quot;caplog {#caplog}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="/python/pytest/how_to_guides/logging#how-to-manage-logging">How to manage logging</a></p><ul><li><h3 id="caplog-1" tabindex="-1">caplog() <a class="header-anchor" href="#caplog-1" aria-label="Permalink to &quot;caplog()&quot;">​</a></h3><p>Access and control log capturing.</p><p>Captured logs are available through the following properties/methods:</p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">*</span><span style="color:#A6ACCD;"> caplog.messages        -</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;"> list of format-interpolated log messages</span></span>
<span class="line"><span style="color:#89DDFF;">*</span><span style="color:#A6ACCD;"> caplog.text            -</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;"> string containing formatted log output</span></span>
<span class="line"><span style="color:#89DDFF;">*</span><span style="color:#A6ACCD;"> caplog.records         -</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;"> list of logging.LogRecord instances</span></span>
<span class="line"><span style="color:#89DDFF;">*</span><span style="color:#A6ACCD;"> caplog.record_tuples   -</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;"> list of </span><span style="color:#89DDFF;">(</span><span style="color:#FFCB6B;">logger_name,</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">level,</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">message</span><span style="color:#89DDFF;">)</span><span style="color:#A6ACCD;"> tuples</span></span>
<span class="line"><span style="color:#89DDFF;">*</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">caplog.clear</span><span style="color:#89DDFF;">()</span><span style="color:#A6ACCD;">         -&gt; clear captured records and formatted log output string</span></span></code></pre></div><p>Returns a <code>pytest.LogCaptureFixture</code> instance.</p></li><li><h3 id="final-class-logcapturefixture" tabindex="-1"><code>final class</code> LogCaptureFixture <a class="header-anchor" href="#final-class-logcapturefixture" aria-label="Permalink to &quot;\`final class\` LogCaptureFixture&quot;">​</a></h3><p>Provides access and control of log capturing.</p><ul><li><p><code>property</code> <strong>handler</strong>: <code>LogCaptureHandler</code></p><p>Get the logging handler used by the fixture.</p></li><li><p><strong>get_records</strong>(<code>when</code>)</p><p>Get the logging records for one of the possible test phases.</p><p><em>Parameters</em>:</p><ul><li><code>when (Literal[&#39;setup&#39;, &#39;call&#39;, &#39;teardown&#39;])</code> – Which test phase to obtain the records from. Valid values are: “setup”, “call” and “teardown”.</li></ul><p><em>Returns</em>: The list of captured records at the given stage.</p><p><em>Return type</em>: <code>List[LogRecord]</code></p><p><em>New in version 3.4.</em></p></li><li><p><code>property</code> <strong>text</strong>: <code>str</code></p><p>The formatted log text.</p></li><li><p><code>property</code> <strong>records</strong>: <code>List[LogRecord]</code></p><p>The list of log records.</p></li><li><p><code>property</code> <strong>record_tuples</strong>: <code>List[Tuple[str, int, str]]</code></p><p>A list of a stripped down version of log records intended for use in assertion comparison.</p><p>The format of the tuple is: <code>(logger_name, log_level, message)</code></p></li><li><p><code>property</code> <strong>messages</strong>: <code>List[str]</code></p><p>A list of format-interpolated log messages.</p><p>Unlike ‘records’, which contains the format string and parameters for interpolation, log messages in this list are all interpolated.</p><p>Unlike ‘text’, which contains the output from the handler, log messages in this list are unadorned with levels, timestamps, etc, making exact comparisons more reliable.</p><p>Note that traceback or stack info (from <code>logging.exception()</code> or the <code>exc_info</code> or <code>stack_info</code> arguments to the logging functions) is not included, as this is added by the formatter in the handler.</p><p><em>New in version 3.7.</em></p></li><li><p><strong>clear</strong>()</p><p>Reset the list of log records and the captured log text.</p></li><li><p><strong>set_level</strong>(<code>level, logger=None</code>)</p><p>Set the threshold level of a logger for the duration of a test.</p><p>Logging messages which are less severe than this level will not be captured.</p><p><em>Changed in version 3.4</em>: The levels of the loggers changed by this function will be restored to their initial values at the end of the test.</p><p>Will enable the requested logging level if it was disabled via <code>logging.disable()</code>.</p><p><em>Parameters</em>:</p><ul><li><p><code>level (Union[int, str])</code> – The level.</p></li><li><p><code>logger (Optional[str])</code> – The logger to update. If not given, the root logger.</p></li></ul></li><li><p><code>with</code> <strong>at_level</strong>(<code>level, logger=None</code>)</p><p>Context manager that sets the level for capturing of logs. After the end of the ‘with’ statement the level is restored to its original value.</p><p>Will enable the requested logging level if it was disabled via <code>logging.disable()</code>.</p><p><em>Parameters</em>:</p><ul><li><p><code>level (Union[int, str])</code> – The level.</p></li><li><p><code>logger (Optional[str])</code> – The logger to update. If not given, the root logger.</p></li></ul></li></ul></li></ul><h2 id="capsys" tabindex="-1">capsys <a class="header-anchor" href="#capsys" aria-label="Permalink to &quot;capsys {#capsys}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="/python/pytest/how_to_guides/output#how-to-capture-stdout-stderr-output">How to capture stdout/stderr output</a></p><ul><li><h3 id="capsys-1" tabindex="-1">capsys() <a class="header-anchor" href="#capsys-1" aria-label="Permalink to &quot;capsys()&quot;">​</a></h3><p>Enable text capturing of writes to <code>sys.stdout</code> and <code>sys.stderr</code>.</p><p>The captured output is made available via <code>capsys.readouterr()</code> method calls, which return a <code>(out, err)</code> namedtuple. <code>out</code> and <code>err</code> will be <code>text</code> objects.</p><p>Returns an instance of <code>CaptureFixture[str]</code>.</p><p><em>Example</em>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">test_output</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">capsys</span><span style="color:#89DDFF;">):</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#82AAFF;">print</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">hello</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>
<span class="line"><span style="color:#A6ACCD;">    captured </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> capsys</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">readouterr</span><span style="color:#89DDFF;">()</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">assert</span><span style="color:#A6ACCD;"> captured</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">out</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">==</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">hello</span><span style="color:#A6ACCD;">\\n</span><span style="color:#89DDFF;">&quot;</span></span></code></pre></div></li><li><h3 id="class-capturefixture" tabindex="-1"><code>class</code> CaptureFixture <a class="header-anchor" href="#class-capturefixture" aria-label="Permalink to &quot;\`class\` CaptureFixture&quot;">​</a></h3><p>Object returned by the <code>capsys</code>, <code>capsysbinary</code>, <code>capfd</code> and <code>capfdbinary</code> fixtures.</p><ul><li><p><strong>readouterr</strong>()</p><p>Read and return the captured output so far, resetting the internal buffer.</p><p><em>Returns</em>: The captured content as a namedtuple with <code>out</code> and <code>err</code> string attributes.</p><p><em>Return type</em>: <code>CaptureResult</code></p></li><li><p><code>with</code> <strong>disabled</strong>()</p><p>Temporarily disable capturing while inside the <code>with</code> block.</p></li></ul></li></ul><h2 id="capsysbinary" tabindex="-1">capsysbinary <a class="header-anchor" href="#capsysbinary" aria-label="Permalink to &quot;capsysbinary {#capsysbinary}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="/python/pytest/how_to_guides/output#how-to-capture-stdout-stderr-output">How to capture stdout/stderr output</a></p><ul><li><h3 id="capsysbinary-1" tabindex="-1">capsysbinary() <a class="header-anchor" href="#capsysbinary-1" aria-label="Permalink to &quot;capsysbinary()&quot;">​</a></h3><p>Enable bytes capturing of writes to <code>sys.stdout</code> and <code>sys.stderr</code>.</p><p>The captured output is made available via <code>capsysbinary.readouterr()</code> method calls, which return a <code>(out, err)</code> namedtuple. <code>out</code> and <code>err</code> will be <code>bytes</code> objects.</p><p><em>Returns</em> an instance of <code>CaptureFixture[bytes]</code>.</p><p><em>Example</em>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">test_output</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">capsysbinary</span><span style="color:#89DDFF;">):</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#82AAFF;">print</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">hello</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>
<span class="line"><span style="color:#A6ACCD;">    captured </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> capsysbinary</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">readouterr</span><span style="color:#89DDFF;">()</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">assert</span><span style="color:#A6ACCD;"> captured</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">out</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">==</span><span style="color:#A6ACCD;"> </span><span style="color:#C792EA;">b</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">hello</span><span style="color:#A6ACCD;">\\n</span><span style="color:#89DDFF;">&quot;</span></span></code></pre></div></li></ul><h2 id="config-cache" tabindex="-1">config.cache <a class="header-anchor" href="#config-cache" aria-label="Permalink to &quot;config.cache {#config-cache}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="/python/pytest/how_to_guides/re_run#how-to-re-run-failed-tests-and-maintain-state-between-test-runs">How to re-run failed tests and maintain state between test runs</a></p><p>The <code>config.cache</code> object allows other plugins and fixtures to store and retrieve values across test runs. To access it from fixtures request <code>pytestconfig</code> into your fixture and get it with <code>pytestconfig.cache</code>.</p><p>Under the hood, the cache plugin uses the simple <code>dumps/loads</code> API of the json stdlib module.</p><p><code>config.cache</code> is an instance of <code>pytest.Cache</code>:</p><ul><li><h3 id="final-class-cache" tabindex="-1"><code>final class</code> Cache <a class="header-anchor" href="#final-class-cache" aria-label="Permalink to &quot;\`final class\` Cache&quot;">​</a></h3><p>Instance of the <code>cache</code> fixture.</p><ul><li><p><strong>mkdir</strong>(<code>name</code>)</p><p>Return a directory path object with the given name.</p><p>If the directory does not yet exist, it will be created. You can use it to manage files to e.g. store/retrieve database dumps across test sessions.</p><p><em>New in version 7.0.</em></p><p><em>Parameters</em>:</p><ul><li><code>name (str)</code> – Must be a string not containing a <code>/</code> separator. Make sure the name contains your plugin or application identifiers to prevent clashes with other cache users.</li></ul></li><li><p><strong>get</strong>(<code>key, default</code>)</p><p>Return the cached value for the given key.</p><p>If no value was yet cached or the value cannot be read, the specified default is returned.</p><p><em>Parameters</em>:</p><ul><li><p><code>key (str)</code> – Must be a <code>/</code> separated value. Usually the first name is the name of your plugin or your application.</p></li><li><p><code>default</code> – The value to return in case of a cache-miss or invalid cache value.</p></li></ul></li><li><p><strong>set</strong>(<code>key, value</code>)</p><p>Save value for the given key.</p><p><em>Parameters</em>:</p><ul><li><p><code>key (str)</code> – Must be a / separated value. Usually the first name is the name of your plugin or your application.</p></li><li><p><code>value (object)</code> – Must be of any combination of basic python types, including nested types like lists of dictionaries.</p></li></ul></li></ul></li></ul><h2 id="doctest-namespace" tabindex="-1">doctest_namespace <a class="header-anchor" href="#doctest-namespace" aria-label="Permalink to &quot;doctest_namespace {#doctest-namespace}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="/python/pytest/how_to_guides/doctest#how-to-run-doctests">How to run doctests</a></p><ul><li><h3 id="doctest-namespace-1" tabindex="-1">doctest_namespace() <a class="header-anchor" href="#doctest-namespace-1" aria-label="Permalink to &quot;doctest_namespace()&quot;">​</a></h3><p>Fixture that returns a <code>dict</code> that will be injected into the namespace of doctests.</p><p>Usually this fixture is used in conjunction with another <code>autouse</code> fixture:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">pytest</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">fixture</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">autouse</span><span style="color:#89DDFF;">=True)</span></span>
<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">add_np</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">doctest_namespace</span><span style="color:#89DDFF;">):</span></span>
<span class="line"><span style="color:#A6ACCD;">    doctest_namespace</span><span style="color:#89DDFF;">[</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">np</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">]</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> numpy</span></span></code></pre></div><p>For more details: <a href="/python/pytest/how_to_guides/doctest#doctest-namespace-fixture">‘doctest_namespace’ fixture</a>.</p></li></ul><h2 id="monkeypatch" tabindex="-1">monkeypatch <a class="header-anchor" href="#monkeypatch" aria-label="Permalink to &quot;monkeypatch {#monkeypatch}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="/python/pytest/how_to_guides/mock#how-to-monkeypatch-mock-modules-and-environments">How to monkeypatch/mock modules and environments</a></p><ul><li><h3 id="monkeypatch-1" tabindex="-1">monkeypatch() <a class="header-anchor" href="#monkeypatch-1" aria-label="Permalink to &quot;monkeypatch()&quot;">​</a></h3><p>A convenient fixture for monkey-patching.</p><p>The fixture provides these methods to modify objects, dictionaries, or <code>os.environ</code>:</p><ul><li><p><code>monkeypatch.setattr(obj, name, value, raising=True)</code></p></li><li><p><code>monkeypatch.delattr(obj, name, raising=True)</code></p></li><li><p><code>monkeypatch.setitem(mapping, name, value)</code></p></li><li><p><code>monkeypatch.delitem(obj, name, raising=True)</code></p></li><li><p><code>monkeypatch.setenv(name, value, prepend=None)</code></p></li><li><p><code>monkeypatch.delenv(name, raising=True)</code></p></li><li><p><code>monkeypatch.syspath_prepend(path)</code></p></li><li><p><code>monkeypatch.chdir(path)</code></p></li><li><p><code>monkeypatch.context()</code></p></li></ul><p>All modifications will be undone after the requesting test function or fixture has finished. The <code>raising</code> parameter determines if a <code>KeyError</code> or <code>AttributeError</code> will be raised if the set/deletion operation does not have the specified target.</p><p>To undo modifications done by the fixture in a contained scope, use <code>context()</code>.</p><p>Returns a <code>MonkeyPatch</code> instance.</p></li><li><h3 id="final-class-monkeypatch" tabindex="-1"><code>final class</code> MonkeyPatch <a class="header-anchor" href="#final-class-monkeypatch" aria-label="Permalink to &quot;\`final class\` MonkeyPatch&quot;">​</a></h3><p>Helper to conveniently monkeypatch attributes/items/environment variables/syspath.</p><p>Returned by the <code>monkeypatch</code> fixture.</p><p><em>Changed in version 6.2</em>: Can now also be used directly as <code>pytest.MonkeyPatch()</code>, for when the fixture is not available. In this case, use <code>with MonkeyPatch.context() as mp</code>: or remember to call <code>undo()</code> explicitly.</p><ul><li><p><code>classmethod with</code> <strong>context</strong>()</p><p>Context manager that returns a new <code>MonkeyPatch</code> object which undoes any patching done inside the <code>with</code> block upon exit.</p><p><em>Example</em>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;font-style:italic;">import</span><span style="color:#A6ACCD;"> functools</span></span>
<span class="line"></span>
<span class="line"></span>
<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">test_partial</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">monkeypatch</span><span style="color:#89DDFF;">):</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">with</span><span style="color:#A6ACCD;"> monkeypatch</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">context</span><span style="color:#89DDFF;">()</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;font-style:italic;">as</span><span style="color:#A6ACCD;"> m</span><span style="color:#89DDFF;">:</span></span>
<span class="line"><span style="color:#A6ACCD;">        m</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">setattr</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">functools</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">partial</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#F78C6C;">3</span><span style="color:#89DDFF;">)</span></span></code></pre></div><p>Useful in situations where it is desired to undo some patches before the test ends, such as mocking <code>stdlib</code> functions that might break pytest itself if mocked (for examples of this see <a href="https://github.com/pytest-dev/pytest/issues/3290" target="_blank" rel="noreferrer">issue #3290</a>).</p></li><li><p><strong>setattr</strong>(<code>target: str, name: object, value: ~_pytest.monkeypatch.Notset = &lt;notset&gt;, raising: bool = True</code>) → <code>None</code></p></li><li><p><strong>setattr</strong>(<code>target: object, name: str, value: object, raising: bool = True</code>) → <code>None</code></p><p>Set attribute value on target, memorizing the old value.</p><p><em>For example</em>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;font-style:italic;">import</span><span style="color:#A6ACCD;"> os</span></span>
<span class="line"></span>
<span class="line"><span style="color:#A6ACCD;">monkeypatch</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">setattr</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">os</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">getcwd</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#C792EA;">lambda</span><span style="color:#89DDFF;">:</span><span style="color:#82AAFF;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">/</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span></code></pre></div><p>The code above replaces the <code>os.getcwd()</code> function by a <code>lambda</code> which always returns <code>&quot;/&quot;</code>.</p><p>For convenience, you can specify a string as <code>target</code> which will be interpreted as a dotted import path, with the last part being the attribute name:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">monkeypatch</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">setattr</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">os.getcwd</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#C792EA;">lambda</span><span style="color:#89DDFF;">:</span><span style="color:#82AAFF;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">/</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span></code></pre></div><p>Raises <code>AttributeError</code> if the attribute does not exist, unless <code>raising</code> is set to False.</p><p><em>Where to patch</em></p><p><code>monkeypatch.setattr</code> works by (temporarily) changing the object that a name points to with another one. There can be many names pointing to any individual object, so for patching to work you must ensure that you patch the name used by the system under test.</p><p>See the section <a href="https://docs.python.org/3/library/unittest.mock.html#where-to-patch" target="_blank" rel="noreferrer">Where to patch</a> in the <code>unittest.mock</code> docs for a complete explanation, which is meant for <code>unittest.mock.patch()</code> but applies to <code>monkeypatch.setattr</code> as well.</p></li><li><p><strong>delattr</strong>(<code>target, name=&lt;notset&gt;, raising=True</code>)</p><p>Delete attribute <code>name</code> from <code>target</code>.</p><p>If no <code>name</code> is specified and <code>target</code> is a string it will be interpreted as a dotted import path with the last part being the attribute name.</p><p>Raises AttributeError it the attribute does not exist, unless <code>raising</code> is set to False.</p></li><li><p><strong>setitem</strong>(<code>dic, name, value</code>)</p><p>Set dictionary entry <code>name</code> to value.</p></li><li><p><strong>delitem</strong>(<code>dic, name, raising=True</code>)</p><p>Delete <code>name</code> from dict.</p><p>Raises <code>KeyError</code> if it doesn’t exist, unless <code>raising</code> is set to False.</p></li><li><p><strong>setenv</strong>(<code>name, value, prepend=None</code>)</p><p>Set environment variable <code>name</code> to <code>value</code>.</p><p>If <code>prepend</code> is a character, read the current environment variable value and prepend the <code>value</code> adjoined with the <code>prepend</code> character.</p></li><li><p><strong>delenv</strong>(<code>name, raising=True</code>)</p><p>Delete <code>name</code> from the environment.</p><p>Raises <code>KeyError</code> if it does not exist, unless <code>raising</code> is set to False.</p></li><li><p><strong>syspath_prepend</strong>(<code>path</code>)</p><p>Prepend <code>path</code> to <code>sys.path</code> list of import locations.</p></li><li><p><strong>chdir</strong>(<code>path</code>)</p><p>Change the current working directory to the specified path.</p><p><em>Parameters</em>:</p><ul><li><code>path (Union[str, PathLike[str]])</code> – The path to change into.</li></ul></li><li><p><strong>undo</strong>()</p><p>Undo previous changes.</p><p>This call consumes the undo stack. Calling it a second time has no effect unless you do more monkeypatching after the undo call.</p><p>There is generally no need to call <code>undo()</code>, since it is called automatically during tear-down.</p><div class="tip custom-block"><p class="custom-block-title">Note</p><p>The same <code>monkeypatch</code> fixture is used across a single test function invocation. If <code>monkeypatch</code> is used both by the test function itself and one of the test fixtures, calling <code>undo()</code> will undo all of the changes made in both functions.</p><p>Prefer to use <code>context()</code> instead.</p></div></li></ul></li></ul><h2 id="pytestconfig" tabindex="-1">pytestconfig <a class="header-anchor" href="#pytestconfig" aria-label="Permalink to &quot;pytestconfig {#pytestconfig}&quot;">​</a></h2><ul><li><h3 id="pytestconfig-1" tabindex="-1">pytestconfig() <a class="header-anchor" href="#pytestconfig-1" aria-label="Permalink to &quot;pytestconfig()&quot;">​</a></h3><p>Session-scoped fixture that returns the session’s <code>pytest.Config</code> object.</p><p><em>Example</em>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">test_foo</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">pytestconfig</span><span style="color:#89DDFF;">):</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">if</span><span style="color:#A6ACCD;"> pytestconfig</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">getoption</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">verbose</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">0</span><span style="color:#89DDFF;">:</span></span>
<span class="line"><span style="color:#A6ACCD;">        ...</span></span></code></pre></div></li></ul><h2 id="pytester" tabindex="-1">pytester <a class="header-anchor" href="#pytester" aria-label="Permalink to &quot;pytester {#pytester}&quot;">​</a></h2><p><em>New in version 6.2.</em></p><p>Provides a <code>Pytester</code> instance that can be used to run and test pytest itself.</p><p>It provides an empty directory where pytest can be executed in isolation, and contains facilities to write tests, configuration files, and match against expected output.</p><p>To use it, include in your topmost <code>conftest.py</code> file:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">pytest_plugins </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">pytester</span><span style="color:#89DDFF;">&quot;</span></span></code></pre></div><ul><li><h3 id="final-class-pytester" tabindex="-1"><code>final class</code> Pytester <a class="header-anchor" href="#final-class-pytester" aria-label="Permalink to &quot;\`final class\` Pytester&quot;">​</a></h3><p>Facilities to write tests/configuration files, execute pytest in isolation, and match against expected output, perfect for black-box testing of pytest plugins.</p><p>It attempts to isolate the test run from external factors as much as possible, modifying the current working directory to <code>path</code> and environment variables during initialization.</p><ul><li><p><code>exception</code> <strong>TimeoutExpired</strong></p></li><li><p><strong>plugins</strong>: <code>List[Union[str, object]]</code></p><p>A list of plugins to use with <code>parseconfig()</code> and <code>runpytest()</code>. Initially this is an empty list but plugins can be added to the list. The type of items to add to the list depends on the method using them so refer to them for details.</p></li><li><p><code>property</code> <strong>path</strong>: <code>Path</code></p><p>Temporary directory path used to create files/run tests from, etc.</p></li><li><p><strong>make_hook_recorder</strong>(<code>pluginmanager</code>)</p><p>Create a new <code>HookRecorder</code> for a <code>PytestPluginManager</code>.</p></li><li><p><strong>chdir</strong>()</p><p>Cd into the temporary directory.</p><p>This is done automatically upon instantiation.</p></li><li><p><strong>makefile</strong>(<code>ext, *args, **kwargs</code>)</p><p>Create new text file(s) in the test directory.</p><p><em>Parameters</em>:</p><ul><li><p><code>ext (str)</code> – The extension the file(s) should use, including the dot, e.g. <code>.py</code>.</p></li><li><p><code>args (str)</code> – All args are treated as strings and joined using newlines. The result is written as contents to the file. The name of the file is based on the test function requesting this fixture.</p></li><li><p><code>kwargs (str)</code> – Each keyword is the name of a file, while the value of it will be written as contents of the file.</p></li></ul><p><em>Returns</em>: The first created file.</p><p><em>Return type</em>: <code>Path</code></p><p><em>Examples</em>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">pytester</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">makefile</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">.txt</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">line1</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">line2</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>
<span class="line"></span>
<span class="line"><span style="color:#A6ACCD;">pytester</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">makefile</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">.ini</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#A6ACCD;font-style:italic;">pytest</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">[pytest]</span><span style="color:#A6ACCD;">\\n</span><span style="color:#C3E88D;">addopts=-rs</span><span style="color:#A6ACCD;">\\n</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span></code></pre></div><p>To create binary files, use <code>pathlib.Path.write_bytes()</code> directly:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">filename </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> pytester</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">path</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">joinpath</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">foo.bin</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>
<span class="line"><span style="color:#A6ACCD;">filename</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">write_bytes</span><span style="color:#89DDFF;">(</span><span style="color:#C792EA;">b</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">...</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span></code></pre></div></li><li><p><strong>makeconftest</strong>(<code>source</code>)</p><p>Write a contest.py file.</p><p><em>Parameters</em>:</p><ul><li><code>source (str)</code> – The contents.</li></ul><p><em>Returns</em>: The <code>conftest.py</code> file.</p><p><em>Return type</em>: <code>Path</code></p></li><li><p><strong>makeini</strong>(<code>source</code>)</p><p>Write a <code>tox.ini</code> file.</p><p><em>Parameters</em>:</p><ul><li><code>source (str)</code> – The contents.</li></ul><p><em>Returns</em>: The <code>tox.ini</code> file.</p><p><em>Return type</em>: <code>Path</code></p></li><li><p><strong>getinicfg</strong>(<code>source</code>)</p><p>Return the pytest section from the <code>tox.ini</code> config file.</p></li><li><p><strong>makepyprojecttoml</strong>(<code>source</code>)</p><p>Write a <code>pyproject.toml</code> file.</p><p><em>Parameters</em>:</p><ul><li><code>source (str)</code> – The contents.</li></ul><p><em>Returns</em>: The <code>pyproject.ini</code> file.</p><p><em>Return type</em>: <code>Path</code></p><p><em>New in version 6.0.</em></p></li><li><p><strong>makepyfile</strong>(<code>*args, **kwargs</code>)</p><p>Shortcut for <code>.makefile()</code> with a .py extension.</p><p>Defaults to the test name with a ‘.py’ extension, e.g <code>test_foobar.py</code>, overwriting existing files.</p><p><em>Examples</em>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">test_something</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">pytester</span><span style="color:#89DDFF;">):</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#676E95;font-style:italic;"># Initial file is created test_something.py.</span></span>
<span class="line"><span style="color:#A6ACCD;">    pytester</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">makepyfile</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">foobar</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#676E95;font-style:italic;"># To create multiple files, pass kwargs accordingly.</span></span>
<span class="line"><span style="color:#A6ACCD;">    pytester</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">makepyfile</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">custom</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">foobar</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#676E95;font-style:italic;"># At this point, both &#39;test_something.py&#39; &amp; &#39;custom.py&#39; exist in the test directory.</span></span></code></pre></div></li><li><p><strong>maketxtfile</strong>(<code>*args, **kwargs</code>)</p><p>Shortcut for <code>.makefile()</code> with a .txt extension.</p><p>Defaults to the test name with a ‘.txt’ extension, e.g <code>test_foobar.txt</code>, overwriting existing files.</p><p><em>Examples</em>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">test_something</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">pytester</span><span style="color:#89DDFF;">):</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#676E95;font-style:italic;"># Initial file is created test_something.txt.</span></span>
<span class="line"><span style="color:#A6ACCD;">    pytester</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">maketxtfile</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">foobar</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#676E95;font-style:italic;"># To create multiple files, pass kwargs accordingly.</span></span>
<span class="line"><span style="color:#A6ACCD;">    pytester</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">maketxtfile</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">custom</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">foobar</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#676E95;font-style:italic;"># At this point, both &#39;test_something.txt&#39; &amp; &#39;custom.txt&#39; exist in the test directory.</span></span></code></pre></div></li><li><p><strong>syspathinsert</strong>(<code>path=None</code>)</p><p>Prepend a directory to sys.path, defaults to <code>path</code>.</p><p>This is undone automatically when this object dies at the end of each test.</p><p><em>Parameters</em>:</p><ul><li><code>path (Optional[Union[str, PathLike[str]]])</code> – The path.</li></ul></li><li><p><strong>mkdir</strong>(<code>name</code>)</p><p>Create a new (sub)directory.</p><p><em>Parameters</em>:</p><ul><li><code>name (Union[str, PathLike[str]])</code> – The name of the directory, relative to the pytester path.</li></ul><p><em>Returns</em>: The created directory.</p><p><em>Return type</em>: <code>Path</code></p></li><li><p><strong>mkpydir</strong>(<code>name</code>)</p><p>Create a new python package.</p><p>This creates a (sub)directory with an empty <code>__init__.py</code> file so it gets recognised as a Python package.</p></li><li><p><strong>copy_example</strong>(<code>name=None</code>)</p><p>Copy file from project’s directory into the <code>testdir</code>.</p><p><em>Parameters</em>:</p><ul><li><code>name (Optional[str])</code> – The name of the file to copy.</li></ul><p><em>Returns</em>: Path to the copied directory (inside <code>self.path</code>).</p><p><em>Return type</em>: <code>Path</code></p></li><li><p><strong>getnode</strong>(<code>config, arg</code>)</p><p>Get the collection node of a file.</p><p><em>Parameters</em>:</p><ul><li><p><code>config (Config)</code> – A pytest config. See <code>parseconfig()</code> for creating it.</p></li><li><p><code>arg (Union[str, PathLike[str]])</code> – Path to the file.</p></li></ul><p><em>Returns</em>: The node.</p><p><em>Return type</em>: <code>Union[Collector, Item]</code></p></li><li><p><strong>getpathnode</strong>(<code>path</code>)</p><p>Return the collection node of a file.</p><p>This is like <code>getnode()</code> but uses <code>parseconfigure()</code> to create the (configured) pytest Config instance.</p><p><em>Parameters</em>:</p><ul><li><code>path (Union[str, PathLike[str]])</code> – Path to the file.</li></ul><p><em>Returns</em>: The node.</p><p><em>Return type</em>: <code>Union[Collector, Item]</code></p></li><li><p><strong>genitems</strong>(<code>colitems</code>)</p><p>Generate all test items from a collection node.</p><p>This recurses into the collection node and returns a list of all the test items contained within.</p><p><em>Parameters</em>:</p><ul><li><code>colitems (Sequence[Union[Item, Collector]])</code> – The collection nodes.</li></ul><p><em>Returns</em>: The collected items.</p><p><em>Return type</em>: <code>List[Item]</code></p></li><li><p><strong>runitem</strong>(<code>source</code>)</p><p>Run the “test_func” Item.</p><p>The calling test instance (class containing the test method) must provide a <code>.getrunner()</code> method which should return a runner which can run the test protocol for a single item, e.g. <code>_pytest.runner.runtestprotocol()</code>.</p></li><li><p><strong>inline_runsource</strong>(<code>source, *cmdlineargs</code>)</p><p>Run a test module in process using <code>pytest.main()</code>.</p><p>This run writes “source” into a temporary file and runs <code>pytest.main()</code> on it, returning a <code>HookRecorder</code> instance for the result.</p><p><em>Parameters</em>:</p><ul><li><p><code>source (str)</code> – The source code of the test module.</p></li><li><p><code>cmdlineargs</code> – Any extra command line arguments to use.</p></li></ul></li><li><p><strong>inline_genitems</strong>(<code>*args</code>)</p><p>Run <code>pytest.main([&#39;--collectonly&#39;])</code> in-process.</p><p>Runs the <code>pytest.main()</code> function to run all of pytest inside the test process itself <code>like inline_run()</code>, but returns a tuple of the collected items and a <code>HookRecorder</code> instance.</p></li><li><p><strong>inline_run</strong>(<code>*args, plugins=(), no_reraise_ctrlc=False</code>)</p><p>Run <code>pytest.main()</code> in-process, returning a HookRecorder.</p><p>Runs the <code>pytest.main()</code> function to run all of pytest inside the test process itself. This means it can return a <code>HookRecorder</code> instance which gives more detailed results from that run than can be done by matching stdout/stderr from <code>runpytest()</code>.</p><p><em>Parameters</em>:</p><ul><li><p><code>args (Union[str, PathLike[str]])</code> – Command line arguments to pass to <code>pytest.main()</code>.</p></li><li><p><code>plugins</code> – Extra plugin instances the <code>pytest.main()</code> instance should use.</p></li><li><p><code>no_reraise_ctrlc (bool)</code> – Typically we reraise keyboard interrupts from the child run. If True, the <code>KeyboardInterrupt</code> exception is captured.</p></li></ul></li><li><p><strong>runpytest_inprocess</strong>(<code>*args, **kwargs</code>)</p><p>Return result of running pytest in-process, providing a similar interface to what <code>self.runpytest()</code> provides.</p></li><li><p><strong>runpytest</strong>(<code>*args, **kwargs</code>)</p><p>Run pytest inline or in a subprocess, depending on the command line option “–runpytest” and return a <code>RunResult</code>.</p></li><li><p><strong>parseconfig</strong>(<code>*args</code>)</p><p>Return a new pytest <code>pytest.Config</code> instance from given commandline args.</p><p>This invokes the pytest bootstrapping code in _pytest.config to create a new <code>pytest.PytestPluginManager</code> and call the <code>pytest_cmdline_parse</code> hook to create a new pytest.Config instance.</p><p>If <code>plugins</code> has been populated they should be plugin modules to be registered with the plugin manager.</p></li><li><p><strong>parseconfigure</strong>(<code>*args</code>)</p><p>Return a new pytest configured <code>Config</code> instance.</p><p>Returns a new <code>pytest.Config</code> instance like <code>parseconfig()</code>, but also calls the <code>pytest_configure</code> hook.</p></li><li><p><strong>getitem</strong>(<code>source, funcname=&#39;test_func&#39;</code>)</p><p>Return the test item for a test function.</p><p>Writes the source to a python file and runs pytest’s collection on the resulting module, returning the test item for the requested function name.</p><p><em>Parameters</em>:</p><ul><li><p><code>source(Union[str, PathLike[str]])</code> – The module source.</p></li><li><p><code>funcname(str)</code> – The name of the test function for which to return a test item.</p></li></ul><p><em>Returns</em>: The test item.</p><p><em>Return type</em>: <code>Item</code></p></li><li><p><strong>getitems</strong>(<code>source</code>)</p><p>Return all test items collected from the module.</p><p>Writes the source to a Python file and runs pytest’s collection on the resulting module, returning all test items contained within.</p></li><li><p><strong>getmodulecol</strong>(<code>source, configargs=(), *, withinit=False</code>)</p><p>Return the module collection node for <code>source</code>.</p><p>Writes <code>source</code> to a file using <code>makepyfile()</code> and then runs the pytest collection on it, returning the collection node for the test module.</p><p><em>Parameters</em>:</p><ul><li><p><code>source (Union[str, PathLike[str]])</code> – The source code of the module to collect.</p></li><li><p><code>configargs</code> – Any extra arguments to pass to <code>parseconfigure()</code>.</p></li><li><p><code>withinit (bool)</code> – Whether to also write an <code>__init__.py</code> file to the same directory to ensure it is a package.</p></li></ul></li><li><p><strong>collect_by_name</strong>(<code>modcol, name</code>)</p><p>Return the collection node for name from the module collection.</p><p>Searches a module collection node for a collection node matching the given name.</p><p><em>Parameters</em>:</p><ul><li><p><code>modcol (Collector)</code> – A module collection node; see getmodulecol().</p></li><li><p><code>name (str)</code> – The name of the node to return.</p></li></ul></li><li><p><strong>popen</strong>(<code>cmdargs, stdout=-1, stderr=-1, stdin=NotSetType.token, **kw</code>)</p><p>Invoke <code>subprocess.Popen</code>.</p><p>Calls <code>subprocess.Popen</code> making sure the current working directory is in <code>PYTHONPATH</code>.</p><p>You probably want to use <code>run()</code> instead.</p></li><li><p><strong>run</strong>(<code>*cmdargs, timeout=None, stdin=NotSetType.token</code>)</p><p>Run a command with arguments.</p><p>Run a process using <code>subprocess.Popen</code> saving the <code>stdout</code> and <code>stderr</code>.</p><p><em>Parameters</em>:</p><ul><li><p><code>cmdargs (Union[str, PathLike[str]])</code> – The sequence of arguments to pass to <code>subprocess.Popen</code>, with path-like objects being converted to str automatically.</p></li><li><p><code>timeout (Optional[float])</code> – The period in seconds after which to timeout and raise <code>Pytester.TimeoutExpired</code>.</p></li><li><p><code>stdin (Union[NotSetType, bytes, IO[Any], int])</code> – Optional standard input.</p><ul><li><p>If it is <code>CLOSE_STDIN</code> (Default), then this method calls <code>subprocess.Popen</code> with <code>stdin=subprocess.PIPE</code>, and the standard input is closed immediately after the new command is started.</p></li><li><p>If it is of type <code>bytes</code>, these bytes are sent to the standard input of the command.</p></li><li><p>Otherwise, it is passed through to <code>subprocess.Popen</code>. For further information in this case, consult the document of the · parameter in <code>subprocess.Popen</code>.</p></li></ul></li></ul><p><em>Returns</em>: The result.</p><p><em>Return type</em>: <code>RunResult</code></p></li><li><p><strong>runpython</strong>(<code>script</code>)</p><p>Run a python script using <code>sys.executable</code> as interpreter.</p></li><li><p><strong>runpython_c</strong>(<code>command</code>)</p><p>Run <code>python -c &quot;command&quot;</code>.</p></li><li><p><strong>runpytest_subprocess</strong>(<code>*args, timeout=None</code>)</p><p>Run pytest as a subprocess with given arguments.</p><p>Any plugins added to the <code>plugins</code> list will be added using the <code>-p</code> command line option. Additionally <code>--basetemp</code> is used to put any temporary files and directories in a numbered directory prefixed with “runpytest-” to not conflict with the normal numbered pytest location for temporary files and directories.</p><p><em>Parameters</em>:</p><ul><li><p><code>args (Union[str, PathLike[str]])</code> – The sequence of arguments to pass to the pytest subprocess.</p></li><li><p><code>timeout (Optional[float])</code> – The period in seconds after which to timeout and raise <code>Pytester.TimeoutExpired</code>.</p></li></ul><p><em>Returns</em>: The result.</p><p><em>Return type</em>: <code>RunResult</code></p></li><li><p><strong>spawn_pytest</strong>(<code>string, expect_timeout=10.0</code>)</p><p>Run pytest using pexpect.</p><p>This makes sure to use the right pytest and sets up the temporary directory locations.</p><p>The pexpect child is returned.</p></li><li><p><strong>spawn</strong>(<code>cmd, expect_timeout=10.0</code>)</p><p>Run a command using pexpect.</p><p>The pexpect child is returned.</p></li></ul></li><li><h3 id="final-class-runresult" tabindex="-1"><code>final class</code> RunResult <a class="header-anchor" href="#final-class-runresult" aria-label="Permalink to &quot;\`final class\` RunResult&quot;">​</a></h3><p>The result of running a command from <code>Pytester</code>.</p><ul><li><p><strong>ret</strong>: <code>Union[int, ExitCode]</code></p><p>The return value.</p></li><li><p><strong>outlines</strong></p><p>List of lines captured from <code>stdout</code>.</p></li><li><p><strong>errlines</strong></p><p>List of lines captured from <code>stderr</code>.</p></li><li><p><strong>stdout</strong></p><p><code>LineMatcher</code> of <code>stdout</code>.</p><p>Use e.g. <code>str(stdout)</code> to reconstruct stdout, or the commonly used <code>stdout.fnmatch_lines()</code> method.</p></li><li><p><strong>stderr</strong></p><ul><li><code>LineMatcher</code> of <code>stderr</code>.</li></ul></li><li><p><strong>duration</strong></p><ul><li>Duration in seconds.</li></ul></li><li><p><strong>parseoutcomes</strong>()</p><p>Return a dictionary of outcome noun -&gt; count from parsing the terminal output that the test process produced.</p><p>The returned nouns will always be in plural form:</p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">======= </span><span style="color:#F78C6C;">1</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">failed,</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">1</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">passed,</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">1</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">warning,</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">1</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">error</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">in</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">0.13</span><span style="color:#C3E88D;">s</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">====</span></span></code></pre></div><p>Will return <code>{&quot;failed&quot;: 1, &quot;passed&quot;: 1, &quot;warnings&quot;: 1, &quot;errors&quot;: 1}</code>.</p></li><li><p><code>classmethod</code> <strong>parse_summary_nouns</strong>(<code>lines</code>)</p><p>Extract the nouns from a pytest terminal summary line.</p><p>It always returns the plural noun for consistency:</p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">======= </span><span style="color:#F78C6C;">1</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">failed,</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">1</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">passed,</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">1</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">warning,</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">1</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">error</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">in</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">0.13</span><span style="color:#C3E88D;">s</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">====</span></span></code></pre></div><p>Will return {&quot;failed&quot;: 1, &quot;passed&quot;: 1, &quot;warnings&quot;: 1, &quot;errors&quot;: 1}.</p></li><li><p><strong>assert_outcomes</strong>(<code>passed=0, skipped=0, failed=0, errors=0, xpassed=0, xfailed=0, warnings=None, deselected=None</code>)</p><p>Assert that the specified outcomes appear with the respective numbers (0 means it didn’t occur) in the text output from a test run.</p><p><code>warnings</code> and <code>deselected</code> are only checked if not None.</p></li></ul></li><li><h3 id="class-linematcher" tabindex="-1"><code>class</code> LineMatcher <a class="header-anchor" href="#class-linematcher" aria-label="Permalink to &quot;\`class\` LineMatcher&quot;">​</a></h3><p>Flexible matching of text.</p><p>This is a convenience class to test large texts like the output of commands.</p><p>The constructor takes a list of lines without their trailing newlines, i.e. <code>text.splitlines()</code>.</p><ul><li><p><strong><strong>str</strong></strong>()</p><p>Return the entire original text.</p><p><em>New in version 6.2</em>: You can use <code>str()</code> in older versions.</p></li><li><p><strong>fnmatch_lines_random</strong>(<code>lines2</code>)</p><p>Check lines exist in the output in any order (using <code>fnmatch.fnmatch()</code>).</p></li><li><p><strong>re_match_lines_random</strong>(<code>lines2</code>)</p><p>Check lines exist in the output in any order (using <code>re.match()</code>).</p></li><li><p><strong>get_lines_after</strong>(<code>fnline</code>)</p><p>Return all lines following the given line in the text.</p><p>The given line can contain glob wildcards.</p></li><li><p><strong>fnmatch_lines</strong>(<code>lines2, *, consecutive=False</code>)</p><p>Check lines exist in the output (using <code>fnmatch.fnmatch()</code>).</p><p>The argument is a list of lines which have to match and can use glob wildcards. If they do not match a <code>pytest.fail()</code> is called. The matches and non-matches are also shown as part of the error message.</p><p><em>Parameters</em>:</p><ul><li><p><code>lines2 (Sequence[str])</code> – String patterns to match.</p></li><li><p><code>consecutive (bool)</code> – Match lines consecutively?</p></li></ul></li><li><p><strong>re_match_lines</strong>(<code>lines2, *, consecutive=False</code>)</p><p>Check lines exist in the output (using <code>re.match()</code>).</p><p>The argument is a list of lines which have to match using <code>re.match</code>. If they do not match a <code>pytest.fail()</code> is called.</p><p>The matches and non-matches are also shown as part of the error message.</p><p><em>Parameters</em>:</p><ul><li><p><code>lines2 (Sequence[str])</code> – string patterns to match.</p></li><li><p><code>consecutive (bool)</code> – match lines consecutively?</p></li></ul></li><li><p><strong>no_fnmatch_line</strong>(<code>pat</code>)</p><p>Ensure captured lines do not match the given pattern, using <code>fnmatch.fnmatch</code>.</p><p><em>Parameters</em>:</p><ul><li><code>pat (str)</code> – The pattern to match lines.</li></ul></li><li><p><strong>no_re_match_line</strong>(<code>pat</code>)</p><p>Ensure captured lines do not match the given pattern, using <code>re.match</code>.</p><p><em>Parameters</em>:</p><ul><li><code>pat (str)</code> – The regular expression to match lines.</li></ul></li><li><p><strong>str</strong>()</p><p>Return the entire original text.</p></li></ul></li><li><h3 id="final-class-hookrecorder" tabindex="-1"><code>final class</code> HookRecorder <a class="header-anchor" href="#final-class-hookrecorder" aria-label="Permalink to &quot;\`final class\` HookRecorder&quot;">​</a></h3><p>Record all hooks called in a plugin manager.</p><p>Hook recorders are created by <code>Pytester</code>.</p><p>This wraps all the hook calls in the plugin manager, recording each call before propagating the normal calls.</p><ul><li><p><strong>getcalls</strong>(<code>names</code>)</p><p>Get all recorded calls to hooks with the given names (or name).</p></li><li><p><strong>matchreport</strong>(<code>inamepart=&#39;&#39;, names=(&#39;pytest_runtest_logreport&#39;, &#39;pytest_collectreport&#39;), when=None</code>)</p><p>Return a testreport whose dotted import path matches.</p></li></ul></li><li><h3 id="final-class-recordedhookcall" tabindex="-1"><code>final class</code> RecordedHookCall <a class="header-anchor" href="#final-class-recordedhookcall" aria-label="Permalink to &quot;\`final class\` RecordedHookCall&quot;">​</a></h3><p>A recorded call to a hook.</p><p>The arguments to the hook call are set as attributes. For example:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">calls </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> hook_recorder</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">getcalls</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">pytest_runtest_setup</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>
<span class="line"><span style="color:#676E95;font-style:italic;"># Suppose pytest_runtest_setup was called once with \`item=an_item\`.</span></span>
<span class="line"><span style="color:#89DDFF;font-style:italic;">assert</span><span style="color:#A6ACCD;"> calls</span><span style="color:#89DDFF;">[</span><span style="color:#F78C6C;">0</span><span style="color:#89DDFF;">].</span><span style="color:#F07178;">item</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">is</span><span style="color:#A6ACCD;"> an_item</span></span></code></pre></div></li></ul><h2 id="record-property" tabindex="-1">record_property <a class="header-anchor" href="#record-property" aria-label="Permalink to &quot;record_property {#record-property}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="https://docs.pytest.org/en/latest/how-to/output.html#record-property-example" target="_blank" rel="noreferrer">record_property</a></p><ul><li><h3 id="record-property-1" tabindex="-1">record_property() <a class="header-anchor" href="#record-property-1" aria-label="Permalink to &quot;record_property()&quot;">​</a></h3><p>Add extra properties to the calling test.</p><p>User properties become part of the test report and are available to the configured reporters, like JUnit XML.</p><p>The fixture is callable with <code>name, value</code>. The value is automatically XML-encoded.</p><p><em>Example</em>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">test_function</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">record_property</span><span style="color:#89DDFF;">):</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#82AAFF;">record_property</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">example_key</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#F78C6C;">1</span><span style="color:#89DDFF;">)</span></span></code></pre></div></li></ul><h2 id="record-testsuite-property" tabindex="-1">record_testsuite_property <a class="header-anchor" href="#record-testsuite-property" aria-label="Permalink to &quot;record_testsuite_property {#record-testsuite-property}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="https://docs.pytest.org/en/latest/how-to/output.html#record-testsuite-property-example" target="_blank" rel="noreferrer">record_testsuite_property</a></p><ul><li><h3 id="record-testsuite-property-1" tabindex="-1">record_testsuite_property() <a class="header-anchor" href="#record-testsuite-property-1" aria-label="Permalink to &quot;record_testsuite_property()&quot;">​</a></h3><p>Record a new <code>&lt;property&gt;</code> tag as child of the root <code>&lt;testsuite&gt;</code>.</p><p>This is suitable to writing global information regarding the entire test suite, and is compatible with <code>xunit2</code> JUnit family.</p><p>This is a <code>session-scoped</code> fixture which is called with <code>(name, value)</code>.</p><p><em>Example</em>:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">test_foo</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">record_testsuite_property</span><span style="color:#89DDFF;">):</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#82AAFF;">record_testsuite_property</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">ARCH</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">PPC</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#82AAFF;">record_testsuite_property</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">STORAGE_TYPE</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">CEPH</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span></code></pre></div><p><em>Parameters</em>:</p><ul><li><p><code>name</code> – The property name.</p></li><li><p><code>value</code> – The property value. Will be converted to a string.</p></li></ul><div class="warning custom-block"><p class="custom-block-title">Warning</p><p>Currently this fixture does not work with the <a href="https://github.com/pytest-dev/pytest-xdist" target="_blank" rel="noreferrer">pytest-xdist</a> plugin. See <a href="https://github.com/pytest-dev/pytest/issues/7767" target="_blank" rel="noreferrer">issue #7767</a> for details.</p></div></li></ul><h2 id="recwarn" tabindex="-1">recwarn <a class="header-anchor" href="#recwarn" aria-label="Permalink to &quot;recwarn {#recwarn}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="/python/pytest/how_to_guides/warning#asserting-warnings-with-the-warns-function">Asserting warnings with the warns function</a></p><ul><li><h3 id="recwarn-1" tabindex="-1">recwarn() <a class="header-anchor" href="#recwarn-1" aria-label="Permalink to &quot;recwarn()&quot;">​</a></h3><p>Return a <code>WarningsRecorder</code> instance that records all warnings emitted by test functions.</p><p>See <a href="/python/pytest/how_to_guides/warning#how-to-capture-warnings">How to capture warnings</a> for information on warning categories.</p></li><li><h3 id="class-warningsrecorder" tabindex="-1"><code>class</code> WarningsRecorder <a class="header-anchor" href="#class-warningsrecorder" aria-label="Permalink to &quot;\`class\` WarningsRecorder&quot;">​</a></h3><p>A context manager to record raised warnings.</p><p>Each recorded warning is an instance of <code>warnings.WarningMessage</code>.</p><p>Adapted from <code>warnings.catch_warnings</code>.</p><div class="tip custom-block"><p class="custom-block-title">Note</p><p><code>DeprecationWarning</code> and <code>PendingDeprecationWarning</code> are treated differently; see <a href="/python/pytest/how_to_guides/warning#ensuring-code-triggers-a-deprecation-warning">Ensuring code triggers a deprecation warning</a>.</p></div><ul><li><p><code>property</code> <strong>list</strong>: <code>List[WarningMessage]</code></p><p>The list of recorded warnings.</p></li><li><p><strong>pop</strong>(<code>cls=&lt;class &#39;Warning&#39;&gt;</code>)</p><p>Pop the first recorded warning which is an instance of <code>cls</code>, but not an instance of a child class of any other match. Raises <code>AssertionError</code> if there is no match.</p></li><li><p><strong>clear</strong>()</p><p>Clear the list of recorded warnings.</p></li></ul></li></ul><h2 id="request" tabindex="-1">request <a class="header-anchor" href="#request" aria-label="Permalink to &quot;request {#request}&quot;">​</a></h2><p><em>Example</em>: <a href="/python/pytest/further_topics/example_trick/basic_pattern#pass-different-values-to-a-test-function-depending-on-command-line-options">Pass different values to a test function, depending on command line options</a></p><p>The <code>request</code> fixture is a special fixture providing information of the requesting test function.</p><ul><li><h3 id="class-fixturerequest" tabindex="-1"><code>class</code> FixtureRequest <a class="header-anchor" href="#class-fixturerequest" aria-label="Permalink to &quot;\`class\` FixtureRequest&quot;">​</a></h3><p>A request for a fixture from a test or fixture function.</p><p>A request object gives access to the requesting test context and has an optional <code>param</code> attribute in case the fixture is parametrized indirectly.</p><ul><li><p><strong>fixturename</strong>:<code> Optional[str]</code></p><p>Fixture for which this request is being performed.</p></li><li><p><code>property</code> <strong>scope</strong>: <code>Literal[&#39;session&#39;, &#39;package&#39;, &#39;module&#39;, &#39;class&#39;, &#39;function&#39;]</code></p><p>Scope string, one of “function”, “class”, “module”, “package”, “session”.</p></li><li><p><code>property</code> <strong>fixturenames</strong>: <code>List[str]</code></p><p>Names of all active fixtures in this request.</p></li><li><p><code>property</code> <strong>node</strong></p><p>Underlying collection node (depends on current request scope).</p></li><li><p><code>property</code> <strong>config</strong>: <code>Config</code></p><p>The pytest config object associated with this request.</p></li><li><p><code>property</code> <strong>function</strong></p><p>Test function object if the request has a per-function scope.</p></li><li><p><code>property</code> <strong>cls</strong></p><p>Class (can be None) where the test function was collected.</p></li><li><p><code>property</code> <strong>instance</strong></p><p>Instance (can be None) on which test function was collected.</p></li><li><p><code>property</code> <strong>module</strong></p><p>Python module object where the test function was collected.</p></li><li><p><code>property</code> <strong>path</strong>: <code>Path</code></p><p>Path where the test function was collected.</p></li><li><p><code>property</code> <strong>keywords</strong>: <code>MutableMapping[str, Any]</code></p><p>Keywords/markers dictionary for the underlying node.</p></li><li><p><code>property</code> <strong>session</strong>: <code>Session</code></p><p>Pytest session object.</p></li><li><p><strong>addfinalizer</strong>(<code>finalizer</code>)</p><p>Add finalizer/teardown function to be called without arguments after the last test within the requesting test context finished execution.</p></li><li><p><strong>applymarker</strong>(<code>marker</code>)</p><p>Apply a marker to a single test function invocation.</p><p>This method is useful if you don’t want to have a keyword/marker on all function invocations.</p><p><em>Parameters</em>:</p><ul><li><code>marker(Union[str, MarkDecorator])</code> – An object created by a call to <code>pytest.mark.NAME(...)</code>.</li></ul></li><li><p><strong>raiseerror</strong>(<code>msg</code>)</p><p>Raise a FixtureLookupError exception.</p><p><em>Parameters</em>:</p><ul><li><code>msg (Optional[str])</code> – An optional custom error message.</li></ul></li><li><p><strong>getfixturevalue</strong>(<code>argname</code>)</p><p>Dynamically run a named fixture function.</p><p>Declaring fixtures via function argument is recommended where possible. But if you can only decide whether to use another fixture at test setup time, you may use this function to retrieve it inside a fixture or test function body.</p><p>This method can be used during the test setup phase or the test run phase, but during the test teardown phase a fixture’s value may not be available.</p><p><em>Parameters</em>:</p><ul><li><code>argname (str)</code> – The fixture name.</li></ul><p><em>Raises</em>:</p><ul><li><code>pytest.FixtureLookupError</code> – If the given fixture could not be found.</li></ul></li></ul></li></ul><h2 id="testdir" tabindex="-1">testdir <a class="header-anchor" href="#testdir" aria-label="Permalink to &quot;testdir {#testdir}&quot;">​</a></h2><p>Identical to <code>pytester</code>, but provides an instance whose methods return legacy <code>py.path.local</code> objects instead when applicable.</p><p>New code should avoid using <code>testdir</code> in favor of <code>pytester</code>.</p><ul><li><h3 id="final-class-testdir" tabindex="-1"><code>final class</code> Testdir <a class="header-anchor" href="#final-class-testdir" aria-label="Permalink to &quot;\`final class\` Testdir&quot;">​</a></h3><p>Similar to <code>Pytester</code>, but this class works with legacy <code>legacy_path</code> objects instead.</p><p>All methods just forward to an internal <code>Pytester</code> instance, converting results to <code>legacy_path</code> objects as necessary.</p><ul><li><p><code>exception</code> <strong>TimeoutExpired</strong></p></li><li><p><code>property</code> <strong>tmpdir</strong>: <code>LocalPath</code></p><p>Temporary directory where tests are executed.</p></li><li><p><strong>make_hook_recorder</strong>(<code>pluginmanager</code>)</p><p>See <code>Pytester.make_hook_recorder()</code>.</p></li><li><p><strong>chdir</strong>()</p><p>See <code>Pytester.chdir()</code>.</p></li><li><p><strong>finalize</strong>()</p><p>See <code>Pytester._finalize()</code>.</p></li><li><p><strong>makefile</strong>(<code>ext, *args, **kwargs</code>)</p><p>See <code>Pytester.makefile()</code>.</p></li><li><p><strong>makeconftest</strong>(<code>source</code>)</p><p>See <code>Pytester.makeconftest()</code>.</p></li><li><p><strong>makeini</strong>(<code>source</code>)</p><p>See <code>Pytester.makeini()</code>.</p></li><li><p><strong>getinicfg</strong>(<code>source</code>)</p><p>See <code>Pytester.getinicfg()</code>.</p></li><li><p><strong>makepyprojecttoml</strong>(<code>source</code>)</p><p>See <code>Pytester.makepyprojecttoml()</code>.</p></li><li><p><strong>makepyfile</strong>(<code>*args, **kwargs</code>)</p><p>See <code>Pytester.makepyfile()</code>.</p></li><li><p><strong>maketxtfile</strong>(<code>*args, **kwargs</code>)</p><p>See <code>Pytester.maketxtfile()</code>.</p></li><li><p><strong>syspathinsert</strong>(<code>path=None</code>)</p><p>See <code>Pytester.syspathinsert()</code>.</p></li><li><p><strong>mkdir</strong>(<code>name</code>)</p><p>See <code>Pytester.mkdir()</code>.</p></li><li><p><strong>mkpydir</strong>(<code>name</code>)</p><p>See <code>Pytester.mkpydir()</code>.</p></li><li><p><strong>copy_example</strong>(<code>name=None</code>)</p><p>See <code>Pytester.copy_example()</code>.</p></li><li><p><strong>getnode</strong>(<code>config, arg</code>)</p><p>See <code>Pytester.getnode()</code>.</p></li><li><p><strong>getpathnode</strong>(<code>path</code>)</p><p>See <code>Pytester.getpathnode()</code>.</p></li><li><p><strong>genitems</strong>(<code>colitems</code>)</p><p>See <code>Pytester.genitems()</code>.</p></li><li><p><strong>runitem</strong>(<code>source</code>)</p><p>See <code>Pytester.runitem()</code>.</p></li><li><p><strong>inline_runsource</strong>(<code>source, *cmdlineargs</code>)</p><p>See <code>Pytester.inline_runsource()</code>.</p></li><li><p><strong>inline_genitems</strong>(<code>*args</code>)</p><p>See <code>Pytester.inline_genitems()</code>.</p></li><li><p><strong>inline_run</strong>(<code>*args, plugins=(), no_reraise_ctrlc=False</code>)</p><p>See <code>Pytester.inline_run()</code>].</p></li><li><p><strong>runpytest_inprocess</strong>(<code>*args, **kwargs</code>)</p><p>See <code>Pytester.runpytest_inprocess()</code>.</p></li><li><p><strong>runpytest</strong>(<code>*args, **kwargs</code>)</p><p>See <code>Pytester.runpytest()</code>.</p></li><li><p><strong>parseconfig</strong>(<code>*args</code>)</p><p>See <code>Pytester.parseconfig()</code>.</p></li><li><p><strong>parseconfigure</strong>(<code>*args</code>)</p><p>See <code>Pytester.parseconfigure()</code>.</p></li><li><p><strong>getitem</strong>(<code>source, funcname=&#39;test_func&#39;</code>)</p><p>See <code>Pytester.getitem()</code>.</p></li><li><p><strong>getitems</strong>(<code>source</code>)</p><p>See <code>Pytester.getitems()</code>.</p></li><li><p><strong>getmodulecol</strong>(<code>source, configargs=(), withinit=False</code>)</p><p>See <code>Pytester.getmodulecol()</code>.</p></li><li><p><strong>collect_by_name</strong>(<code>modcol, name</code>)</p><p>See <code>Pytester.collect_by_name()</code>.</p></li><li><p><strong>popen</strong>(<code>cmdargs, stdout=-1, stderr=-1, stdin=NotSetType.token, **kw</code>)</p><p>See <code>Pytester.popen()</code>.</p></li><li><p><strong>run</strong>(<code>*cmdargs, timeout=None, stdin=NotSetType.token</code>)</p><p>See <code>Pytester.run()</code>.</p></li><li><p><strong>runpython</strong>(<code>script</code>)</p><p>See <code>Pytester.runpython()</code>.</p></li><li><p><strong>runpython_c</strong>(<code>command</code>)</p><p>See <code>Pytester.runpython_c()</code>.</p></li><li><p><strong>runpytest_subprocess</strong>(<code>*args, timeout=None</code>)</p><p>See <code>Pytester.runpytest_subprocess()</code>.</p></li><li><p><strong>spawn_pytest</strong>(<code>string, expect_timeout=10.0</code>)</p><p>See <code>Pytester.spawn_pytest()</code>.</p></li><li><p><strong>spawn</strong>(<code>cmd, expect_timeout=10.0</code>)</p><p>See <code>Pytester.spawn()</code>.</p></li></ul></li></ul><h2 id="tmp-path" tabindex="-1">tmp_path <a class="header-anchor" href="#tmp-path" aria-label="Permalink to &quot;tmp_path {#tmp-path}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="/python/pytest/how_to_guides/temp#how-to-use-temporary-directories-and-files-in-tests">How to use temporary directories and files in tests</a></p><ul><li><h3 id="tmp-path-1" tabindex="-1">tmp_path() <a class="header-anchor" href="#tmp-path-1" aria-label="Permalink to &quot;tmp_path()&quot;">​</a></h3><p>Return a temporary directory path object which is unique to each test function invocation, created as a sub directory of the base temporary directory.</p><p>By default, a new base temporary directory is created each test session, and old bases are removed after 3 sessions, to aid in debugging. This behavior can be configured with <code>tmp_path_retention_count</code> and <code>tmp_path_retention_policy</code>. If <code>--basetemp</code> is used then it is cleared each session. See <a href="/python/pytest/how_to_guides/temp#the-default-base-temporary-directory">The default base temporary</a> directory.</p><p>The returned object is a <code>pathlib.Path</code> object.</p></li></ul><h2 id="tmp-path-factory" tabindex="-1">tmp_path_factory <a class="header-anchor" href="#tmp-path-factory" aria-label="Permalink to &quot;tmp_path_factory {#tmp-path-factory}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="/python/pytest/how_to_guides/temp#the-tmp-path-factory-fixture">The tmp_path_factory fixture</a></p><p><code>tmp_path_factory</code> is an instance of <code>TempPathFactory</code>:</p><ul><li><h3 id="final-class-temppathfactory" tabindex="-1"><code>final class</code> TempPathFactory <a class="header-anchor" href="#final-class-temppathfactory" aria-label="Permalink to &quot;\`final class\` TempPathFactory&quot;">​</a></h3><p>Factory for temporary directories under the common base temp directory.</p><p>The base directory can be configured using the <code>--basetemp</code> option.</p><ul><li><p><strong>mktemp</strong>(<code>basename, numbered=True</code>)</p><p>Create a new temporary directory managed by the factory.</p><p><em>Parameters</em>:</p><ul><li><p><code>basename(str)</code> – Directory base name, must be a relative path.</p></li><li><p><code>numbered(bool)</code> – If <code>True</code>, ensure the directory is unique by adding a numbered suffix greater than - any existing one: <code>basename=&quot;foo-&quot;</code> and <code>numbered=True</code> means that this function will create directories named <code>&quot;foo-0&quot;</code>, <code>&quot;foo-1&quot;</code>, <code>&quot;foo-2&quot;</code> and so on.</p></li></ul><p><em>Returns</em>: The path to the new directory.</p><p><em>Return type</em>: <code>Path</code></p></li><li><p><strong>getbasetemp</strong>()</p><p>Return the base temporary directory, creating it if needed.</p><p><em>Returns</em>: The base temporary directory.</p><p><em>Return type</em>: <code>Path</code></p></li></ul></li></ul><h2 id="tmpdir" tabindex="-1">tmpdir <a class="header-anchor" href="#tmpdir" aria-label="Permalink to &quot;tmpdir {#tmpdir}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="/python/pytest/how_to_guides/temp#the-tmpdir-and-tmpdir-factory-fixtures">The tmpdir and tmpdir_factory fixtures</a></p><ul><li><h3 id="tmpdir-1" tabindex="-1">tmpdir() <a class="header-anchor" href="#tmpdir-1" aria-label="Permalink to &quot;tmpdir()&quot;">​</a></h3><p>Return a temporary directory path object which is unique to each test function invocation, created as a sub directory of the base temporary directory.</p><p>By default, a new base temporary directory is created each test session, and old bases are removed after 3 sessions, to aid in debugging. If <code>--basetemp</code> is used then it is cleared each session. See <a href="/python/pytest/how_to_guides/temp#the-default-base-temporary-directory">The default base temporary directory</a>.</p><p>The returned object is a <code>legacy_path</code> object.</p><div class="tip custom-block"><p class="custom-block-title">Note</p><p>These days, it is preferred to use <code>tmp_path</code>.</p><p><a href="/python/pytest/how_to_guides/temp#the-tmpdir-and-tmpdir-factory-fixtures">About the tmpdir and tmpdir_factory fixtures</a>.</p></div></li></ul><h2 id="tmpdir-factory" tabindex="-1">tmpdir_factory <a class="header-anchor" href="#tmpdir-factory" aria-label="Permalink to &quot;tmpdir_factory {#tmpdir-factory}&quot;">​</a></h2><p><em>Tutorial</em>: <a href="/python/pytest/how_to_guides/temp#the-tmpdir-and-tmpdir-factory-fixtures">The tmpdir and tmpdir_factory fixtures</a></p><p><code>tmpdir_factory</code> is an instance of <code>TempdirFactory</code>:</p><ul><li><h3 id="final-class-tempdirfactory" tabindex="-1"><code>final class</code> TempdirFactory <a class="header-anchor" href="#final-class-tempdirfactory" aria-label="Permalink to &quot;\`final class\` TempdirFactory&quot;">​</a></h3><p>Backward compatibility wrapper that implements <code>py.path.local</code> for <code>TempPathFactory</code>.</p><div class="tip custom-block"><p class="custom-block-title">Note</p><p>These days, it is preferred to use <code>tmp_path_factory</code>.</p><p><a href="/python/pytest/how_to_guides/temp#the-tmpdir-and-tmpdir-factory-fixtures">About the tmpdir and tmpdir_factory fixtures</a>.</p></div><ul><li><p><strong>mktemp</strong>(<code>basename, numbered=True</code>)</p><p>Same as <code>TempPathFactory.mktemp()</code>, but returns a <code>py.path.local</code> object.</p></li><li><p><strong>getbasetemp</strong>()</p><p>Same as <code>TempPathFactory.getbasetemp()</code>, but returns a <code>py.path.local</code> object.</p></li></ul></li></ul>`,77),p=[n];function l(r,c,i,d,u,h){return t(),o("div",null,p)}const g=e(a,[["render",l]]);export{m as __pageData,g as default};
