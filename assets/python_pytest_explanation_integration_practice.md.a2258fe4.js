import{_ as e,o as s,c as t,X as o}from"./chunks/framework.6e839c56.js";const m=JSON.parse('{"title":"Good Integration Practices","description":"","frontmatter":{},"headers":[],"relativePath":"python/pytest/explanation/integration_practice.md","filePath":"python/pytest/explanation/integration_practice.md","lastUpdated":1692807718000}'),a={name:"python/pytest/explanation/integration_practice.md"},n=o(`<h1 id="good-integration-practices" tabindex="-1">Good Integration Practices <a class="header-anchor" href="#good-integration-practices" aria-label="Permalink to &quot;Good Integration Practices {#good-integration-practices}&quot;">​</a></h1><h2 id="install-package-with-pip" tabindex="-1">Install package with pip <a class="header-anchor" href="#install-package-with-pip" aria-label="Permalink to &quot;Install package with pip {#install-package-with-pip}&quot;">​</a></h2><p>For development, we recommend you use <code>venv</code> for virtual environments and pip for installing your application and any dependencies, as well as the <code>pytest</code> package itself. This ensures your code and dependencies are isolated from your system Python installation.</p><p>Create a <code>pyproject.toml</code> file in the root of your repository as described in <a href="https://packaging.python.org/en/latest/tutorials/packaging-projects/" target="_blank" rel="noreferrer">Packaging Python Projects</a>. The first few lines should look like this:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">[</span><span style="color:#FFCB6B;">build-system</span><span style="color:#89DDFF;">]</span></span>
<span class="line"><span style="color:#A6ACCD;">requires </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">[</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">hatchling</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">]</span></span>
<span class="line"><span style="color:#A6ACCD;">build-backend </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">hatchling.build</span><span style="color:#89DDFF;">&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#89DDFF;">[</span><span style="color:#FFCB6B;">project</span><span style="color:#89DDFF;">]</span></span>
<span class="line"><span style="color:#A6ACCD;">name </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">PACKAGENAME</span><span style="color:#89DDFF;">&quot;</span></span>
<span class="line"><span style="color:#A6ACCD;">version </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">PACKAGEVERSION</span><span style="color:#89DDFF;">&quot;</span></span></code></pre></div><p>where <code>PACKAGENAME</code> and <code>PACKAGEVERSION</code> are the name and version of your package respectively.</p><p>You can then install your package in “editable” mode by running from the same directory:</p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#FFCB6B;">pip</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">install</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">-e</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">.</span></span></code></pre></div><p>which lets you change your source code (both tests and application) and rerun tests at will.</p><h2 id="conventions-for-python-test-discovery" tabindex="-1">Conventions for Python test discovery <a class="header-anchor" href="#conventions-for-python-test-discovery" aria-label="Permalink to &quot;Conventions for Python test discovery {#conventions-for-python-test-discovery}&quot;">​</a></h2><p><code>pytest</code> implements the following standard test discovery:</p><ul><li><p>If no arguments are specified then collection starts from <code>testpaths</code> (if configured) or the current directory. Alternatively, command line arguments can be used in any combination of directories, file names or node ids.</p></li><li><p>Recurse into directories, unless they match <code>norecursedirs</code>.</p></li><li><p>In those directories, search for <code>test_*.py</code> or <code>*_test.py</code> files, imported by their test package name.</p></li><li><p>From those files, collect test items:</p><ul><li><p><code>test</code> prefixed test functions or methods outside of class.</p></li><li><p><code>test</code> prefixed test functions or methods inside <code>Test</code> prefixed test classes (without an <code>__init__</code> method). Methods decorated with <code>@staticmethod</code> and <code>@classmethods</code> are also considered.</p></li></ul></li></ul><p>For examples of how to customize your test discovery <a href="/python/pytest/further_topics/example_trick/test_discovery#changing-standard-python-test-discovery">Changing standard (Python) test discovery</a>.</p><p>Within Python modules, <code>pytest</code> also discovers tests using the standard <code>unittest.TestCase</code> subclassing technique.</p><h2 id="choosing-a-test-layout-import-rules" tabindex="-1">Choosing a test layout / import rules <a class="header-anchor" href="#choosing-a-test-layout-import-rules" aria-label="Permalink to &quot;Choosing a test layout / import rules {#choosing-a-test-layout-import-rules}&quot;">​</a></h2><p><code>pytest</code> supports two common test layouts:</p><h3 id="tests-outside-application-code" tabindex="-1">Tests outside application code <a class="header-anchor" href="#tests-outside-application-code" aria-label="Permalink to &quot;Tests outside application code {#tests-outside-application-code}&quot;">​</a></h3><p>Putting tests into an extra directory outside your actual application code might be useful if you have many functional tests or for other reasons want to keep tests separate from actual application code (often a good idea):</p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#FFCB6B;">pyproject.toml</span></span>
<span class="line"><span style="color:#FFCB6B;">src/</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#FFCB6B;">mypkg/</span></span>
<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#FFCB6B;">__init__.py</span></span>
<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#FFCB6B;">app.py</span></span>
<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#FFCB6B;">view.py</span></span>
<span class="line"><span style="color:#FFCB6B;">tests/</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#FFCB6B;">test_app.py</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#FFCB6B;">test_view.py</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#82AAFF;">...</span></span></code></pre></div><p>This has the following benefits:</p><ul><li><p>Your tests can run against an installed version after executing <code>pip install .</code>.</p></li><li><p>Your tests can run against the local copy with an editable install after executing <code>pip install --editable .</code>.</p></li></ul><p>For new projects, we recommend to use <code>importlib</code> <a href="/python/pytest/explanation/import_mechanism#import-modes">import mode</a> (see <a href="/python/pytest/explanation/integration_practice#choosing-an-import-mode">which-import-mode</a> for a detailed explanation). To this end, add the following to your <code>pyproject.toml</code>:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">[</span><span style="color:#FFCB6B;">tool</span><span style="color:#A6ACCD;">.</span><span style="color:#FFCB6B;">pytest</span><span style="color:#A6ACCD;">.</span><span style="color:#FFCB6B;">ini_options</span><span style="color:#89DDFF;">]</span></span>
<span class="line"><span style="color:#A6ACCD;">addopts </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">[</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">--import-mode=importlib</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#89DDFF;">]</span></span></code></pre></div><p>Generally, but especially if you use the default import mode <code>prepend</code>, it is strongly suggested to use a <code>src</code> layout. Here, your application root package resides in a sub-directory of your root, i.e. <code>src/mypkg/</code> instead of <code>mypkg</code>.</p><p>This layout prevents a lot of common pitfalls and has many benefits, which are better explained in this excellent <a href="https://blog.ionelmc.ro/2014/05/25/python-packaging/#the-structure%3E" target="_blank" rel="noreferrer">blog post</a> by Ionel Cristian Mărieș.</p><div class="tip custom-block"><p class="custom-block-title">Note</p><p>If you do not use an editable install and use the <code>src</code> layout as above you need to extend the Python’s search path for module files to execute the tests against the local copy directly. You can do it in an ad-hoc manner by setting the <code>PYTHONPATH</code> environment variable:</p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">PYTHONPATH</span><span style="color:#89DDFF;">=</span><span style="color:#C3E88D;">src</span><span style="color:#A6ACCD;"> </span><span style="color:#FFCB6B;">pytest</span></span></code></pre></div><p>or in a permanent manner by using the <code>pythonpath</code> configuration variable and adding the following to your <code>pyproject.toml</code>:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">[</span><span style="color:#FFCB6B;">tool</span><span style="color:#A6ACCD;">.</span><span style="color:#FFCB6B;">pytest</span><span style="color:#A6ACCD;">.</span><span style="color:#FFCB6B;">ini_options</span><span style="color:#89DDFF;">]</span></span>
<span class="line"><span style="color:#A6ACCD;">pythonpath </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">src</span><span style="color:#89DDFF;">&quot;</span></span></code></pre></div></div><div class="tip custom-block"><p class="custom-block-title">Note</p><p>If you do not use an editable install and not use the <code>src</code> layout (<code>mypkg</code> directly in the root directory) you can rely on the fact that Python by default puts the current directory in <code>sys.path</code> to import your package and run <code>python -m pytest</code> to execute the tests against the local copy directly.</p><p>See <a href="/python/pytest/explanation/import_mechanism#invoking-pytest-versus-python-m-pytest">Invoking pytest versus python -m pytest</a> for more information about the difference between calling <code>pytest</code> and <code>python -m pytest</code>.</p></div><h3 id="tests-as-part-of-application-code" tabindex="-1">Tests as part of application code <a class="header-anchor" href="#tests-as-part-of-application-code" aria-label="Permalink to &quot;Tests as part of application code {#tests-as-part-of-application-code}&quot;">​</a></h3><p>Inlining test directories into your application package is useful if you have direct relation between tests and application modules and want to distribute them along with your application:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">pyproject.toml</span></span>
<span class="line"><span style="color:#89DDFF;">[</span><span style="color:#FFCB6B;">src/</span><span style="color:#89DDFF;">]</span><span style="color:#A6ACCD;">mypkg/</span></span>
<span class="line"><span style="color:#A6ACCD;">    __init__.py</span></span>
<span class="line"><span style="color:#A6ACCD;">    app.py</span></span>
<span class="line"><span style="color:#A6ACCD;">    view.py</span></span>
<span class="line"><span style="color:#A6ACCD;">    tests/</span></span>
<span class="line"><span style="color:#A6ACCD;">        __init__.py</span></span>
<span class="line"><span style="color:#A6ACCD;">        test_app.py</span></span>
<span class="line"><span style="color:#A6ACCD;">        test_view.py</span></span>
<span class="line"><span style="color:#A6ACCD;">        ...</span></span></code></pre></div><p>In this scheme, it is easy to run your tests using the <code>--pyargs</code> option:</p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#FFCB6B;">pytest</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">--pyargs</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">mypkg</span></span></code></pre></div><p><code>pytest</code> will discover where <code>mypkg</code> is installed and collect tests from there.</p><p>Note that this layout also works in conjunction with the <code>src</code> layout mentioned in the previous section.</p><div class="tip custom-block"><p class="custom-block-title">Note</p><p>You can use namespace packages (PEP420) for your application but pytest will still perform test package name discovery based on the presence of <code>__init__.py</code> files. If you use one of the two recommended file system layouts above but leave away the <code>__init__.py</code> files from your directories, it should just work. From “inlined tests”, however, you will need to use absolute imports for getting at your application code.</p></div><div class="tip custom-block"><p class="custom-block-title">Note</p><p>In <code>prepend</code> and <code>append</code> import-modes, if pytest finds a <code>&quot;a/b/test_module.py&quot;</code> test file while recursing into the filesystem it determines the import name as follows:</p><ul><li><p>determine <code>basedir</code>: this is the first “upward” (towards the root) directory not containing an <code>__init__.py</code>. If e.g. both a and b contain an <code>__init__.py</code> file then the parent directory of a will become the <code>basedir</code>.</p></li><li><p>perform <code>sys.path.insert(0, basedir)</code> to make the test module importable under the fully qualified import name.</p></li><li><p><code>import a.b.test_module</code> where the path is determined by converting path separators <code>/</code> into <code>“.”</code> characters. This means you must follow the convention of having directory and file names map directly to the import names.</p></li></ul><p>The reason for this somewhat evolved importing technique is that in larger projects multiple test modules might import from each other and thus deriving a canonical import name helps to avoid surprises such as a test module getting imported twice.</p><p>With <code>--import-mode=importlib</code> things are less convoluted because pytest doesn’t need to change <code>sys.path</code> or <code>sys.modules</code>, making things much less surprising.</p></div><h3 id="choosing-an-import-mode" tabindex="-1">Choosing an import mode <a class="header-anchor" href="#choosing-an-import-mode" aria-label="Permalink to &quot;Choosing an import mode {#choosing-an-import-mode}&quot;">​</a></h3><p>For historical reasons, pytest defaults to the <code>prepend</code> <a href="/python/pytest/explanation/import_mechanism#import-modes">import mode</a> instead of the <code>importlib</code> import mode we recommend for new projects. The reason lies in the way the <code>prepend</code> mode works:</p><p>Since there are no packages to derive a full package name from, <code>pytest</code> will import your test files as top-level modules. The test files in the first example (src layout) would be imported as <code>test_app</code> and <code>test_view</code> top-level modules by adding <code>tests/</code> to <code>sys.path</code>.</p><p>This results in a drawback compared to the import mode <code>importlib</code>: your test files must have unique names.</p><p>If you need to have test modules with the same name, as a workaround you might add <code>__init__.py</code> files to your <code>tests</code> folder and subfolders, changing them to packages:</p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#FFCB6B;">pyproject.toml</span></span>
<span class="line"><span style="color:#FFCB6B;">mypkg/</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#82AAFF;">...</span></span>
<span class="line"><span style="color:#FFCB6B;">tests/</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#FFCB6B;">__init__.py</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#FFCB6B;">foo/</span></span>
<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#FFCB6B;">__init__.py</span></span>
<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#FFCB6B;">test_view.py</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#FFCB6B;">bar/</span></span>
<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#FFCB6B;">__init__.py</span></span>
<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#FFCB6B;">test_view.py</span></span></code></pre></div><p>Now pytest will load the modules as <code>tests.foo.test_view</code> and <code>tests.bar.test_view</code>, allowing you to have modules with the same name. But now this introduces a subtle problem: in order to load the test modules from the <code>tests</code> directory, pytest prepends the root of the repository to <code>sys.path</code>, which adds the side-effect that now <code>mypkg</code> is also importable.</p><p>This is problematic if you are using a tool like <a href="https://docs.pytest.org/en/latest/explanation/goodpractices.html#tox" target="_blank" rel="noreferrer">tox</a> to test your package in a virtual environment, because you want to test the installed version of your package, not the local code from the repository.</p><p>The <code>importlib</code> import mode does not have any of the drawbacks above, because <code>sys.path</code> is not changed when importing test modules.</p><h2 id="tox" tabindex="-1">tox <a class="header-anchor" href="#tox" aria-label="Permalink to &quot;tox {#tox}&quot;">​</a></h2><p>Once you are done with your work and want to make sure that your actual package passes all tests you may want to look into <a href="https://tox.wiki/en/stable/index.html" target="_blank" rel="noreferrer">tox</a>, the virtualenv test automation tool. <code>tox</code> helps you to setup virtualenv environments with pre-defined dependencies and then executing a pre-configured test command with options. It will run tests against the installed package and not against your source code checkout, helping to detect packaging glitches.</p><h2 id="do-not-run-via-setuptools" tabindex="-1">Do not run via setuptools <a class="header-anchor" href="#do-not-run-via-setuptools" aria-label="Permalink to &quot;Do not run via setuptools {#do-not-run-via-setuptools}&quot;">​</a></h2><p>Integration with setuptools is not recommended, i.e. you should not be using <code>python setup.py test</code> or <code>pytest-runner</code>, and may stop working in the future.</p><p>This is deprecated since it depends on deprecated features of setuptools and relies on features that break security mechanisms in pip. For example ‘setup_requires’ and ‘tests_require’ bypass <code>pip --require-hashes</code>. For more information and migration instructions, see the <a href="https://github.com/pytest-dev/pytest-runner#deprecation-notice" target="_blank" rel="noreferrer">pytest-runner notice</a>. See also <a href="https://github.com/pypa/setuptools/issues/1684" target="_blank" rel="noreferrer">pypa/setuptools#1684</a>.</p><p>setuptools intends to <a href="https://github.com/pypa/setuptools/issues/931" target="_blank" rel="noreferrer">remove the test command</a>.</p><h2 id="checking-with-flake8-pytest-style" tabindex="-1">Checking with flake8-pytest-style <a class="header-anchor" href="#checking-with-flake8-pytest-style" aria-label="Permalink to &quot;Checking with flake8-pytest-style {#checking-with-flake8-pytest-style}&quot;">​</a></h2><p>In order to ensure that pytest is being used correctly in your project, it can be helpful to use the <a href="https://github.com/m-burst/flake8-pytest-style" target="_blank" rel="noreferrer">flake8-pytest-style</a> flake8 plugin.</p><p>flake8-pytest-style checks for common mistakes and coding style violations in pytest code, such as incorrect use of fixtures, test function names, and markers. By using this plugin, you can catch these errors early in the development process and ensure that your pytest code is consistent and easy to maintain.</p><p>A list of the lints detected by flake8-pytest-style can be found on its <a href="https://pypi.org/project/flake8-pytest-style/" target="_blank" rel="noreferrer">PyPI page</a>.</p><div class="tip custom-block"><p class="custom-block-title">Note</p><p>flake8-pytest-style is not an official pytest project. Some of the rules enforce certain style choices, such as using <code>@pytest.fixture()</code> over <code>@pytest.fixture</code>, but you can configure the plugin to fit your preferred style.</p></div>`,56),p=[n];function l(i,c,r,d,y,h){return s(),t("div",null,p)}const g=e(a,[["render",l]]);export{m as __pageData,g as default};
