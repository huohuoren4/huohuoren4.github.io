import{_ as e,o as t,c as o,V as a}from"./chunks/framework.48c56699.js";const f=JSON.parse('{"title":"Blueprint Objects","description":"","frontmatter":{},"headers":[],"relativePath":"python/flask/api_reference/blueprint_obj.md","filePath":"python/flask/api_reference/blueprint_obj.md","lastUpdated":1692437254000}'),r={name:"python/flask/api_reference/blueprint_obj.md"},l=a('<h1 id="blueprint-objects" tabindex="-1">Blueprint Objects <a class="header-anchor" href="#blueprint-objects" aria-label="Permalink to &quot;Blueprint Objects&quot;">​</a></h1><h2 id="class-flask-blueprint-name-import-name-static-folder-none-static-url-path-none-template-folder-none-url-prefix-none-subdomain-none-url-defaults-none-root-path-none-cli-group-object-object" tabindex="-1"><code>class</code> flask.Blueprint(<code>name, import_name, static_folder=None, static_url_path=None, template_folder=None, url_prefix=None, subdomain=None, url_defaults=None, root_path=None, cli_group=&lt;object object&gt;</code>) <a class="header-anchor" href="#class-flask-blueprint-name-import-name-static-folder-none-static-url-path-none-template-folder-none-url-prefix-none-subdomain-none-url-defaults-none-root-path-none-cli-group-object-object" aria-label="Permalink to &quot;`class` flask.Blueprint(`name, import_name, static_folder=None, static_url_path=None, template_folder=None, url_prefix=None, subdomain=None, url_defaults=None, root_path=None, cli_group=&lt;object object&gt;`)&quot;">​</a></h2><p>Represents a blueprint, a collection of routes and other app-related functions that can be registered on a real application later.</p><p>A blueprint is an object that allows defining application functions without requiring an application object ahead of time. It uses the same decorators as <code>Flask</code>, but defers the need for an application by recording them for later registration.</p><p>Decorating a function with a blueprint creates a deferred function that is called with <code>BlueprintSetupState</code> when the blueprint is registered on an application.</p><p>See <a href="https://flask.palletsprojects.com/en/2.3.x/blueprints/" target="_blank" rel="noreferrer">Modular Applications with Blueprints</a> for more information.</p><ul><li><p><em>Parameters</em>:</p><ul><li><p><code>name (str)</code> – The name of the blueprint. Will be prepended to each endpoint name.</p></li><li><p><code>import_name (str)</code> – The name of the blueprint package, usually <code>__name__</code>. This helps locate the <code>root_path</code> for the blueprint.</p></li><li><p><code>static_folder (str | os.PathLike | None)</code> – A folder with static files that should be served by the blueprint’s static route. The path is relative to the blueprint’s root path. Blueprint static files are disabled by default.</p></li><li><p><code>static_url_path (str | None)</code> – The url to serve static files from. Defaults to <code>static_folder</code>. If the blueprint does not have a <code>url_prefix</code>, the app’s static route will take precedence, and the blueprint’s static files won’t be accessible.</p></li><li><p><code>template_folder (str | os.PathLike | None)</code> – A folder with templates that should be added to the app’s template search path. The path is relative to the blueprint’s root path. Blueprint templates are disabled by default. Blueprint templates have a lower precedence than those in the app’s templates folder.</p></li><li><p><code>url_prefix (str | None)</code> – A path to prepend to all of the blueprint’s URLs, to make them distinct from the rest of the app’s routes.</p></li><li><p><code>subdomain (str | None)</code> – A subdomain that blueprint routes will match on by default.</p></li><li><p><code>url_defaults (dict | None)</code> – A dict of default values that blueprint routes will receive by default.</p></li><li><p><code>root_path (str | None)</code> – By default, the blueprint will automatically set this based on <code>import_name</code>. In certain situations this automatic detection can fail, so the path can be specified manually instead.</p></li><li><p><code>cli_group (str | None)</code> –</p></li></ul></li></ul><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 1.1.0</em>: Blueprints have a <code>cli</code> group to register nested CLI commands. The <code>cli_group</code> parameter controls the name of the group under the <code>flask</code> command.</p><p><em>New in version 0.7.</em></p></details><ul><li><h3 id="add-app-template-filter-f-name-none" tabindex="-1">add_app_template_filter(<code>f, name=None</code>) <a class="header-anchor" href="#add-app-template-filter-f-name-none" aria-label="Permalink to &quot;add_app_template_filter(`f, name=None`)&quot;">​</a></h3><p>Register a template filter, available in any template rendered by the application. Works like the <code>app_template_filter()</code> decorator. Equivalent to <code>Flask.add_template_filter()</code>.</p><p><em>Parameters</em>:</p><ul><li><p><code>name (str | None)</code> – the optional name of the filter, otherwise the function name will be used.</p></li><li><p><code>f (Callable[[...], Any])</code> –</p></li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h3 id="add-app-template-global-f-name-none" tabindex="-1">add_app_template_global(<code>f, name=None</code>) <a class="header-anchor" href="#add-app-template-global-f-name-none" aria-label="Permalink to &quot;add_app_template_global(`f, name=None`)&quot;">​</a></h3><p>Register a template global, available in any template rendered by the application. Works like the <code>app_template_global()</code> decorator. Equivalent to <code>Flask.add_template_global()</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.10.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>name (str | None)</code> – the optional name of the global, otherwise the function name will be used.</p></li><li><p><code>f (Callable[[...], Any])</code> –</p></li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h3 id="add-app-template-test-f-name-none" tabindex="-1">add_app_template_test(<code>f, name=None</code>) <a class="header-anchor" href="#add-app-template-test-f-name-none" aria-label="Permalink to &quot;add_app_template_test(`f, name=None`)&quot;">​</a></h3><p>Register a template test, available in any template rendered by the application. Works like the <code>app_template_test()</code> decorator. Equivalent to <code>Flask.add_template_test()</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.10.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>name (str | None)</code> – the optional name of the test, otherwise the function name will be used.</p></li><li><p><code>f (Callable[[...], bool])</code> –</p></li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h3 id="add-url-rule-rule-endpoint-none-view-func-none-provide-automatic-options-none-options" tabindex="-1">add_url_rule(<code>rule, endpoint=None, view_func=None, provide_automatic_options=None, **options</code>) <a class="header-anchor" href="#add-url-rule-rule-endpoint-none-view-func-none-provide-automatic-options-none-options" aria-label="Permalink to &quot;add_url_rule(`rule, endpoint=None, view_func=None, provide_automatic_options=None, **options`)&quot;">​</a></h3><p>Register a URL rule with the blueprint. See <code>Flask.add_url_rule()</code> for full documentation.</p><p>The URL rule is prefixed with the blueprint’s URL prefix. The endpoint name, used with <code>url_for()</code>, is prefixed with the blueprint’s name.</p><p><em>Parameters</em>:</p><ul><li><p><code>rule (str)</code> –</p></li><li><p><code>endpoint (str | None)</code> –</p></li><li><p><code>view_func (ft.RouteCallable | None)</code> –</p></li><li><p><code>provide_automatic_options (bool | None)</code> –</p></li><li><p><code>options (t.Any)</code> –</p></li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h3 id="after-app-request-f" tabindex="-1">after_app_request(<code>f</code>) <a class="header-anchor" href="#after-app-request-f" aria-label="Permalink to &quot;after_app_request(`f`)&quot;">​</a></h3><p>Like <code>after_request()</code>, but after every request, not only those handled by the blueprint. Equivalent to <code>Flask.after_request()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_after_request)</code> –</li></ul><p><em>Return type</em>: <code>T_after_request</code></p></li><li><h3 id="after-request-f" tabindex="-1">after_request(<code>f</code>) <a class="header-anchor" href="#after-request-f" aria-label="Permalink to &quot;after_request(`f`)&quot;">​</a></h3><p>Register a function to run after each request to this object.</p><p>The function is called with the response object, and must return a response object. This allows the functions to modify or replace the response before it is sent.</p><p>If a function raises an exception, any remaining <code>after_request</code> functions will not be called. Therefore, this should not be used for actions that must execute, such as to close resources. Use <code>teardown_request()</code> for that.</p><p>This is available on both app and blueprint objects. When used on an app, this executes after every request. When used on a blueprint, this executes after every request that the blueprint handles. To register with a blueprint and execute after every request, use <code>Blueprint.after_app_request()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_after_request)</code> –</li></ul><p><em>Return type</em>: <code>T_after_request</code></p></li><li><h3 id="after-request-funcs-dict-ft-apporblueprintkey-list-ft-afterrequestcallable" tabindex="-1">after_request_funcs: <code>dict[ft.AppOrBlueprintKey, list[ft.AfterRequestCallable]]</code> <a class="header-anchor" href="#after-request-funcs-dict-ft-apporblueprintkey-list-ft-afterrequestcallable" aria-label="Permalink to &quot;after_request_funcs: `dict[ft.AppOrBlueprintKey, list[ft.AfterRequestCallable]]`&quot;">​</a></h3><p>A data structure of functions to call at the end of each request, in the format <code>{scope: [functions]}</code>. The scope key is the name of a blueprint the functions are active for, or <code>None</code> for all requests.</p><p>To register a function, use the <code>after_request()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li><li><h3 id="app-context-processor-f" tabindex="-1">app_context_processor(<code>f</code>) <a class="header-anchor" href="#app-context-processor-f" aria-label="Permalink to &quot;app_context_processor(`f`)&quot;">​</a></h3><p>Like <code>context_processor()</code>, but for templates rendered by every view, not only by the blueprint. Equivalent to <code>Flask.context_processor()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_template_context_processor)</code> –</li></ul><p><em>Return type</em>: T_template_context_processor</p></li><li><h3 id="app-errorhandler-code" tabindex="-1">app_errorhandler(<code>code</code>) <a class="header-anchor" href="#app-errorhandler-code" aria-label="Permalink to &quot;app_errorhandler(`code`)&quot;">​</a></h3><p>Like <code>errorhandler()</code>, but for every request, not only those handled by the blueprint. Equivalent to <code>Flask.errorhandler()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>code (type[Exception] | int)</code> –</li></ul><p><em>Return type</em>: <code>Callable[[T_error_handler], T_error_handler]</code></p></li><li><h3 id="app-template-filter-name-none" tabindex="-1">app_template_filter(<code>name=None</code>) <a class="header-anchor" href="#app-template-filter-name-none" aria-label="Permalink to &quot;app_template_filter(`name=None`)&quot;">​</a></h3><p>Register a template filter, available in any template rendered by the application. Equivalent to <code>Flask.template_filter()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>name (str | None)</code> – the optional name of the filter, otherwise the function name will be used.</li></ul><p><em>Return type</em>: <code>Callable[[T_template_filter], T_template_filter]</code></p></li><li><h3 id="app-template-global-name-none" tabindex="-1">app_template_global(<code>name=None</code>) <a class="header-anchor" href="#app-template-global-name-none" aria-label="Permalink to &quot;app_template_global(`name=None`)&quot;">​</a></h3><p>Register a template global, available in any template rendered by the application. Equivalent to <code>Flask.template_global()</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.10.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>name (str | None)</code> – the optional name of the global, otherwise the function name will be used.</li></ul><p><em>Return type</em>: <code>Callable[[T_template_global], T_template_global]</code></p></li><li><h3 id="app-template-test-name-none" tabindex="-1">app_template_test(<code>name=None</code>) <a class="header-anchor" href="#app-template-test-name-none" aria-label="Permalink to &quot;app_template_test(`name=None`)&quot;">​</a></h3><p>Register a template test, available in any template rendered by the application. Equivalent to <code>Flask.template_test()</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.10.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>name (str | None)</code> – the optional name of the test, otherwise the function name will be used.</li></ul><p><em>Return type</em>: <code>Callable[[T_template_test], T_template_test]</code></p></li><li><h3 id="app-url-defaults-f" tabindex="-1">app_url_defaults(<code>f</code>) <a class="header-anchor" href="#app-url-defaults-f" aria-label="Permalink to &quot;app_url_defaults(`f`)&quot;">​</a></h3><p>Like <code>url_defaults()</code>, but for every request, not only those handled by the blueprint. Equivalent to <code>Flask.url_defaults()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_url_defaults)</code> –</li></ul><p><em>Return type</em>: <code>T_url_defaults</code></p></li><li><h3 id="app-url-value-preprocessor-f" tabindex="-1">app_url_value_preprocessor(<code>f</code>) <a class="header-anchor" href="#app-url-value-preprocessor-f" aria-label="Permalink to &quot;app_url_value_preprocessor(`f`)&quot;">​</a></h3><p>Like <code>url_value_preprocessor()</code>, but for every request, not only those handled by the blueprint. Equivalent to <code>Flask.url_value_preprocessor()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_url_value_preprocessor)</code> –</li></ul><p><em>Return type</em>: <code>T_url_value_preprocessor</code></p></li><li><h3 id="before-app-request-f" tabindex="-1">before_app_request(<code>f</code>) <a class="header-anchor" href="#before-app-request-f" aria-label="Permalink to &quot;before_app_request(`f`)&quot;">​</a></h3><p>Like <code>before_request()</code>, but before every request, not only those handled by the blueprint. Equivalent to <code>Flask.before_request()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_before_request)</code> –</li></ul><p><em>Return type</em>: <code>T_before_request</code></p></li><li><h3 id="before-request-f" tabindex="-1">before_request(<code>f</code>) <a class="header-anchor" href="#before-request-f" aria-label="Permalink to &quot;before_request(`f`)&quot;">​</a></h3><p>Register a function to run before each request.</p><p>For example, this can be used to open a database connection, or to load the logged in user from the session.</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">before_request</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">load_user</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">if</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">user_id</span><span style="color:#89DDFF;">&quot;</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">in</span><span style="color:#A6ACCD;"> session</span><span style="color:#89DDFF;">:</span></span>\n<span class="line"><span style="color:#A6ACCD;">        g</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">user</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> db</span><span style="color:#89DDFF;">.</span><span style="color:#F07178;">session</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">get</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">session</span><span style="color:#89DDFF;">[</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">user_id</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">])</span></span></code></pre></div><p>The function will be called without any arguments. If it returns a non-<code>None</code> value, the value is handled as if it was the return value from the view, and further request handling is stopped.</p><p>This is available on both app and blueprint objects. When used on an app, this executes before every request. When used on a blueprint, this executes before every request that the blueprint handles. To register with a blueprint and execute before every request, use <code>Blueprint.before_app_request()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_before_request)</code> –</li></ul><p><em>Return type</em>: <code>T_before_request</code></p></li><li><h3 id="before-request-funcs-dict-ft-apporblueprintkey-list-ft-beforerequestcallable" tabindex="-1">before_request_funcs: <code>dict[ft.AppOrBlueprintKey, list[ft.BeforeRequestCallable]]</code> <a class="header-anchor" href="#before-request-funcs-dict-ft-apporblueprintkey-list-ft-beforerequestcallable" aria-label="Permalink to &quot;before_request_funcs: `dict[ft.AppOrBlueprintKey, list[ft.BeforeRequestCallable]]`&quot;">​</a></h3><p>A data structure of functions to call at the beginning of each request, in the format <code>{scope: [functions]}</code>. The scope key is the name of a blueprint the functions are active for, or None for all requests.</p><p>To register a function, use the <code>before_request()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li><li><h3 id="cli" tabindex="-1">cli <a class="header-anchor" href="#cli" aria-label="Permalink to &quot;cli&quot;">​</a></h3><p>The Click command group for registering CLI commands for this object. The commands are available from the <code>flask</code> command once the application has been discovered and blueprints have been registered.</p></li><li><h3 id="context-processor-f" tabindex="-1">context_processor(<code>f</code>) <a class="header-anchor" href="#context-processor-f" aria-label="Permalink to &quot;context_processor(`f`)&quot;">​</a></h3><p>Registers a template context processor function. These functions run before rendering a template. The keys of the returned dict are added as variables available in the template.</p><p>This is available on both app and blueprint objects. When used on an app, this is called for every rendered template. When used on a blueprint, this is called for templates rendered from the blueprint’s views. To register with a blueprint and affect every template, use <code>Blueprint.app_context_processor()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_template_context_processor)</code> –</li></ul><p><em>Return type</em>: <code>T_template_context_processor</code></p></li><li><h3 id="delete-rule-options" tabindex="-1">delete(<code>rule, **options</code>) <a class="header-anchor" href="#delete-rule-options" aria-label="Permalink to &quot;delete(`rule, **options`)&quot;">​</a></h3><p>Shortcut for <code>route()</code> with <code>methods=[&quot;DELETE&quot;]</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.0.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>rule (str)</code> –</p></li><li><p><code>options (Any)</code> –</p></li></ul><p><em>Return type</em>: <code>Callable[[T_route], T_route]</code></p></li><li><h3 id="endpoint-endpoint" tabindex="-1">endpoint(<code>endpoint</code>) <a class="header-anchor" href="#endpoint-endpoint" aria-label="Permalink to &quot;endpoint(`endpoint`)&quot;">​</a></h3><p>Decorate a view function to register it for the given endpoint. Used if a rule is added without a <code>view_func</code> with <code>add_url_rule()</code>.</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">add_url_rule</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">/ex</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span><span style="color:#82AAFF;"> </span><span style="color:#A6ACCD;font-style:italic;">endpoint</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">example</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">endpoint</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">example</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">example</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">    ...</span></span></code></pre></div><p><em>Parameters</em>:</p><ul><li><code>endpoint (str)</code> – The endpoint name to associate with the view function.</li></ul><p><em>Return type</em>: <code>Callable[[F], F]</code></p></li><li><h3 id="error-handler-spec-dict-ft-apporblueprintkey-dict-int-none-dict-type-exception-ft-errorhandlercallable" tabindex="-1">error_handler_spec: <code>dict[ft.AppOrBlueprintKey, dict[int | None, dict[type[Exception], ft.ErrorHandlerCallable]]]</code> <a class="header-anchor" href="#error-handler-spec-dict-ft-apporblueprintkey-dict-int-none-dict-type-exception-ft-errorhandlercallable" aria-label="Permalink to &quot;error_handler_spec: `dict[ft.AppOrBlueprintKey, dict[int | None, dict[type[Exception], ft.ErrorHandlerCallable]]]`&quot;">​</a></h3><p>A data structure of registered error handlers, in the format <code>{scope: {code: {class: handler}}}</code>. The scope key is the name of a blueprint the handlers are active for, or None for all requests. The code key is the HTTP status code for <code>HTTPException</code>, or <code>None</code> for other exceptions. The innermost dictionary maps exception classes to handler functions.</p><p>To register an error handler, use the <code>errorhandler()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li><li><h3 id="errorhandler-code-or-exception" tabindex="-1">errorhandler(<code>code_or_exception</code>) <a class="header-anchor" href="#errorhandler-code-or-exception" aria-label="Permalink to &quot;errorhandler(`code_or_exception`)&quot;">​</a></h3><p>Register a function to handle errors by code or exception class.</p><p>A decorator that is used to register a function given an error code. Example:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">errorhandler</span><span style="color:#89DDFF;">(</span><span style="color:#F78C6C;">404</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">page_not_found</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">error</span><span style="color:#89DDFF;">):</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">This page does not exist</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">404</span></span></code></pre></div><p>You can also register handlers for arbitrary exceptions:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">errorhandler</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">DatabaseError</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">special_exception_handler</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;font-style:italic;">error</span><span style="color:#89DDFF;">):</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">Database connection failed</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">500</span></span></code></pre></div><p>This is available on both app and blueprint objects. When used on an app, this can handle errors from every request. When used on a blueprint, this can handle errors from requests that the blueprint handles. To register with a blueprint and affect every request, use <code>Blueprint.app_errorhandler()</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.7</em>: Use <code>register_error_handler()</code> instead of modifying <code>error_handler_spec</code> directly, for application wide error handlers.</p><p><em>New in version 0.7</em>: One can now additionally also register custom exception types that do not necessarily have to be a subclass of the <code>HTTPException</code> class.</p></details><p><em>Parameters</em>:</p><ul><li><code>code_or_exception (type[Exception] | int)</code> – the code as integer for the handler, or an arbitrary exception</li></ul><p><em>Return type</em>: <code>Callable[[T_error_handler], T_error_handler]</code></p></li><li><h3 id="get-rule-options" tabindex="-1">get(<code>rule, **options</code>) <a class="header-anchor" href="#get-rule-options" aria-label="Permalink to &quot;get(`rule, **options`)&quot;">​</a></h3><p>Shortcut for <code>route()</code> with <code>methods=[&quot;GET&quot;]</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.0.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>rule (str)</code> –</p></li><li><p><code>options (Any)</code> –</p></li></ul><p><em>Return type</em>: <code>Callable[[T_route], T_route]</code></p></li><li><h3 id="get-send-file-max-age-filename" tabindex="-1">get_send_file_max_age(<code>filename</code>) <a class="header-anchor" href="#get-send-file-max-age-filename" aria-label="Permalink to &quot;get_send_file_max_age(`filename`)&quot;">​</a></h3><p>Used by <code>send_file()</code> to determine the <code>max_age</code> cache value for a given file path if it wasn’t passed.</p><p>By default, this returns <code>SEND_FILE_MAX_AGE_DEFAULT</code> from the configuration of <code>current_app</code>. This defaults to None, which tells the browser to use conditional requests instead of a timed cache, which is usually preferable.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 2.0</em>: The default configuration is None instead of <code>12</code> hours.</p><p><em>New in version 0.9.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>filename (str | None)</code> –</li></ul><p><em>Return type</em>: <code>int | None</code></p></li><li><h3 id="property-has-static-folder-bool" tabindex="-1"><code>property</code> has_static_folder: <code>bool</code> <a class="header-anchor" href="#property-has-static-folder-bool" aria-label="Permalink to &quot;`property` has_static_folder: `bool`&quot;">​</a></h3><p><code>True</code> if <code>static_folder</code> is set.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.5.</em></p></details></li><li><h3 id="import-name" tabindex="-1">import_name <a class="header-anchor" href="#import-name" aria-label="Permalink to &quot;import_name&quot;">​</a></h3><p>The name of the package or module that this object belongs to. Do not change this once it is set by the constructor.</p></li><li><h3 id="property-jinja-loader-filesystemloader-none" tabindex="-1"><code>property</code> jinja_loader: <code>FileSystemLoader | None</code> <a class="header-anchor" href="#property-jinja-loader-filesystemloader-none" aria-label="Permalink to &quot;`property` jinja_loader: `FileSystemLoader | None`&quot;">​</a></h3><p>The Jinja loader for this object’s templates. By default this is a class <code>jinja2.loaders.FileSystemLoader</code> to <code>template_folder</code> if it is set.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.5.</em></p></details></li><li><h3 id="make-setup-state-app-options-first-registration-false" tabindex="-1">make_setup_state(<code>app, options, first_registration=False</code>) <a class="header-anchor" href="#make-setup-state-app-options-first-registration-false" aria-label="Permalink to &quot;make_setup_state(`app, options, first_registration=False`)&quot;">​</a></h3><p>Creates an instance of <code>BlueprintSetupState()</code> object that is later passed to the register callback functions. Subclasses can override this to return a subclass of the setup state.</p><p><em>Parameters</em>:</p><ul><li><p><code>app (Flask)</code> –</p></li><li><p><code>options (dict)</code> –</p></li><li><p><code>first_registration (bool)</code> –</p></li></ul><p><em>Return type</em>: <code>BlueprintSetupState</code></p></li><li><h3 id="open-resource-resource-mode-rb" tabindex="-1">open_resource(<code>resource, mode=&#39;rb&#39;</code>) <a class="header-anchor" href="#open-resource-resource-mode-rb" aria-label="Permalink to &quot;open_resource(`resource, mode=&#39;rb&#39;`)&quot;">​</a></h3><p>Open a resource file relative to <code>root_path</code> for reading.</p><p>For example, if the file <code>schema.sql</code> is next to the file <code>app.py</code> where the <code>Flask</code> app is defined, it can be opened with:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;font-style:italic;">with</span><span style="color:#A6ACCD;"> app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">open_resource</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">schema.sql</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;font-style:italic;">as</span><span style="color:#A6ACCD;"> f</span><span style="color:#89DDFF;">:</span></span>\n<span class="line"><span style="color:#A6ACCD;">    conn</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">executescript</span><span style="color:#89DDFF;">(</span><span style="color:#82AAFF;">f</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">read</span><span style="color:#89DDFF;">())</span></span></code></pre></div><p><em>Parameters</em>:</p><ul><li><p><code>resource (str)</code> – Path to the resource relative to <code>root_path</code>.</p></li><li><p><code>mode (str)</code> – Open the file in this mode. Only reading is supported, valid values are <code>“r”</code> (or <code>“rt”</code>) and <code>“rb”</code>.</p></li></ul><p><em>Return type</em>: <code>IO</code></p></li><li><h3 id="patch-rule-options" tabindex="-1">patch(<code>rule, **options</code>) <a class="header-anchor" href="#patch-rule-options" aria-label="Permalink to &quot;patch(`rule, **options`)&quot;">​</a></h3><p>Shortcut for <code>route()</code> with <code>methods=[&quot;PATCH&quot;]</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.0.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>rule (str)</code> –</p></li><li><p><code>options (Any)</code> –</p></li></ul><p><em>Return type</em>: <code>Callable[[T_route], T_route]</code></p></li><li><h3 id="post-rule-options" tabindex="-1">post(<code>rule, **options</code>) <a class="header-anchor" href="#post-rule-options" aria-label="Permalink to &quot;post(`rule, **options`)&quot;">​</a></h3><p>Shortcut for <code>route()</code> with <code>methods=[&quot;POST&quot;]</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.0.</em></p></details><p><em>Parameters</em>:</p><ul><li><p>rule (str) –</p></li><li><p>options (Any) –</p></li></ul><p><em>Return type</em>: <code>Callable[[T_route], T_route]</code></p></li><li><h3 id="put-rule-options" tabindex="-1">put(<code>rule, **options</code>) <a class="header-anchor" href="#put-rule-options" aria-label="Permalink to &quot;put(`rule, **options`)&quot;">​</a></h3><p>Shortcut for <code>route()</code> with <code>methods=[&quot;PUT&quot;]</code>.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 2.0.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>rule (str)</code> –</p></li><li><p><code>options (Any)</code> –</p></li></ul><p><em>Return type</em>: <code>Callable[[T_route], T_route]</code></p></li><li><h3 id="record-func" tabindex="-1">record(<code>func</code>) <a class="header-anchor" href="#record-func" aria-label="Permalink to &quot;record(`func`)&quot;">​</a></h3><p>Registers a function that is called when the blueprint is registered on the application. This function is called with the state as argument as returned by the <code>make_setup_state()</code> method.</p><p><em>Parameters</em>:</p><ul><li><code>func (Callable)</code> –</li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h3 id="record-once-func" tabindex="-1">record_once(<code>func</code>) <a class="header-anchor" href="#record-once-func" aria-label="Permalink to &quot;record_once(`func`)&quot;">​</a></h3><p>Works like <code>record()</code> but wraps the function in another function that will ensure the function is only called once. If the blueprint is registered a second time on the application, the function passed is not called.</p><p><em>Parameters</em>:</p><ul><li><code>func (Callable)</code> –</li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h3 id="register-app-options" tabindex="-1">register(<code>app, options</code>) <a class="header-anchor" href="#register-app-options" aria-label="Permalink to &quot;register(`app, options`)&quot;">​</a></h3><p>Called by <code>Flask.register_blueprint()</code> to register all views and callbacks registered on the blueprint with the application. Creates a <code>BlueprintSetupState</code> and calls each <code>record()</code> callback with it.</p><p><em>Parameters</em>:</p><ul><li><p><code>app (Flask)</code> – The application this blueprint is being registered with.</p></li><li><p><code>options (dict)</code> – Keyword arguments forwarded from register_blueprint().</p></li></ul><p><em>Return type</em>: <code>None</code></p><p><em>Changed in version 2.3</em>: Nested blueprints now correctly apply subdomains.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 2.1</em>: Registering the same blueprint with the same name multiple times is an error.</p><p><em>Changed in version 2.0.1</em>: Nested blueprints are registered with their dotted name. This allows different blueprints with the same name to be nested at different locations.</p><p><em>Changed in version 2.0.1</em>: The <code>name</code> option can be used to change the (pre-dotted) name the blueprint is registered with. This allows the same blueprint to be registered multiple times with unique names for <code>url_for</code>.</p></details></li><li><h3 id="register-blueprint-blueprint-options" tabindex="-1">register_blueprint(<code>blueprint, **options</code>) <a class="header-anchor" href="#register-blueprint-blueprint-options" aria-label="Permalink to &quot;register_blueprint(`blueprint, **options`)&quot;">​</a></h3><p>Register a Blueprint on this blueprint. Keyword arguments passed to this method will override the defaults set on the blueprint.</p><details class="details custom-block"><summary>Changelog</summary><p><em>Changed in version 2.0.1</em>: The <code>name</code> option can be used to change the (pre-dotted) name the blueprint is registered with. This allows the same blueprint to be registered multiple times with unique names for <code>url_for</code>.</p></details><p><em>Parameters</em>:</p><ul><li><p><code>blueprint (Blueprint)</code> –</p></li><li><p><code>options (Any)</code> –</p></li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h3 id="register-error-handler-code-or-exception-f" tabindex="-1">register_error_handler(<code>code_or_exception, f</code>) <a class="header-anchor" href="#register-error-handler-code-or-exception-f" aria-label="Permalink to &quot;register_error_handler(`code_or_exception, f`)&quot;">​</a></h3><p>Alternative error attach function to the <code>errorhandler()</code> decorator that is more straightforward to use for non decorator usage.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.7.</em></p></details><p><em>Parameters</em>:</p><ul><li><p><code>code_or_exception (type[Exception] | int)</code> –</p></li><li><p><code>f (ft.ErrorHandlerCallable)</code> –</p></li></ul><p><em>Return type</em>: <code>None</code></p></li><li><h3 id="root-path" tabindex="-1">root_path <a class="header-anchor" href="#root-path" aria-label="Permalink to &quot;root_path&quot;">​</a></h3><p>Absolute path to the package on the filesystem. Used to look up resources contained in the package.</p></li><li><h3 id="route-rule-options" tabindex="-1">route(<code>rule, **options</code>) <a class="header-anchor" href="#route-rule-options" aria-label="Permalink to &quot;route(`rule, **options`)&quot;">​</a></h3><p>Decorate a view function to register it with the given URL rule and options. Calls <code>add_url_rule()</code>, which has more details about the implementation.</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">@</span><span style="color:#82AAFF;">app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">route</span><span style="color:#89DDFF;">(</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">/</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#C792EA;">def</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">index</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">Hello, World!</span><span style="color:#89DDFF;">&quot;</span></span></code></pre></div><p>See <a href="https://flask.palletsprojects.com/en/2.3.x/api/#url-route-registrations" target="_blank" rel="noreferrer">URL Route Registrations</a>.</p><p>The endpoint name for the route defaults to the name of the view function if the <code>endpoint</code> parameter isn’t passed.</p><p>The <code>methods</code> parameter defaults to <code>[&quot;GET&quot;]</code>. <code>HEAD</code> and <code>OPTIONS</code> are added automatically.</p><p><em>Parameters</em>:</p><ul><li><p>rule (str) – The URL rule string.</p></li><li><p>options (Any) – Extra options passed to the <code>Rule</code> object.</p></li></ul><p><em>Return type</em>: <code>Callable[[T_route], T_route]</code></p></li><li><h3 id="send-static-file-filename" tabindex="-1">send_static_file(<code>filename</code>) <a class="header-anchor" href="#send-static-file-filename" aria-label="Permalink to &quot;send_static_file(`filename`)&quot;">​</a></h3><p>The view function used to serve files from <code>static_folder</code>. A route is automatically registered for this view at <code>static_url_path</code> if <code>static_folder</code> is set.</p><details class="details custom-block"><summary>Changelog</summary><p><em>New in version 0.5.</em></p></details><p><em>Parameters</em>:</p><ul><li><code>filename (str)</code> –</li></ul><p><em>Return type</em>: <code>Response</code></p></li><li><h3 id="property-static-folder-str-none" tabindex="-1"><code>property</code> static_folder: <code>str | None</code> <a class="header-anchor" href="#property-static-folder-str-none" aria-label="Permalink to &quot;`property` static_folder: `str | None`&quot;">​</a></h3><p>The absolute path to the configured static folder. <code>None</code> if no static folder is set.</p></li><li><h3 id="property-static-url-path-str-none" tabindex="-1"><code>property</code> static_url_path: <code>str | None</code> <a class="header-anchor" href="#property-static-url-path-str-none" aria-label="Permalink to &quot;`property` static_url_path: `str | None`&quot;">​</a></h3><p>The URL prefix that the static route will be accessible from.</p><p>If it was not configured during init, it is derived from <code>static_folder</code>.</p></li><li><h3 id="teardown-app-request-f" tabindex="-1">teardown_app_request(<code>f</code>) <a class="header-anchor" href="#teardown-app-request-f" aria-label="Permalink to &quot;teardown_app_request(`f`)&quot;">​</a></h3><p>Like <code>teardown_request()</code>, but after every request, not only those handled by the blueprint. Equivalent to <code>Flask.teardown_request()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_teardown)</code> –</li></ul><p><em>Return type</em>: <code>T_teardown</code></p></li><li><h3 id="teardown-request-f" tabindex="-1">teardown_request(<code>f</code>) <a class="header-anchor" href="#teardown-request-f" aria-label="Permalink to &quot;teardown_request(`f`)&quot;">​</a></h3><p>Register a function to be called when the request context is popped. Typically this happens at the end of each request, but contexts may be pushed manually as well during testing.</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;font-style:italic;">with</span><span style="color:#A6ACCD;"> app</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">test_request_context</span><span style="color:#89DDFF;">():</span></span>\n<span class="line"><span style="color:#A6ACCD;">    ...</span></span></code></pre></div><p>When the <code>with</code> block exits (or <code>ctx.pop()</code> is called), the teardown functions are called just before the request context is made inactive.</p><p>When a teardown function was called because of an unhandled exception it will be passed an error object. If an <code>errorhandler()</code> is registered, it will handle the exception and the teardown will not receive it.</p><p>Teardown functions must avoid raising exceptions. If they execute code that might fail they must surround that code with a <code>try/except</code> block and log any errors.</p><p>The return values of teardown functions are ignored.</p><p>This is available on both app and blueprint objects. When used on an app, this executes after every request. When used on a blueprint, this executes after every request that the blueprint handles. To register with a blueprint and execute after every request, use <code>Blueprint.teardown_app_request()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_teardown)</code> –</li></ul><p><em>Return type</em>: <code>T_teardown</code></p></li><li><h3 id="teardown-request-funcs-dict-ft-apporblueprintkey-list-ft-teardowncallable" tabindex="-1">teardown_request_funcs: <code>dict[ft.AppOrBlueprintKey, list[ft.TeardownCallable]]</code> <a class="header-anchor" href="#teardown-request-funcs-dict-ft-apporblueprintkey-list-ft-teardowncallable" aria-label="Permalink to &quot;teardown_request_funcs: `dict[ft.AppOrBlueprintKey, list[ft.TeardownCallable]]`&quot;">​</a></h3><p>A data structure of functions to call at the end of each request even if an exception is raised, in the format <code>{scope: [functions]}</code>. The <code>scope</code> key is the name of a blueprint the functions are active for, or <code>None</code> for all requests.</p><p>To register a function, use the <code>teardown_request()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li><li><h3 id="template-context-processors-dict-ft-apporblueprintkey-list-ft-templatecontextprocessorcallable" tabindex="-1">template_context_processors: <code>dict[ft.AppOrBlueprintKey, list[ft.TemplateContextProcessorCallable]]</code> <a class="header-anchor" href="#template-context-processors-dict-ft-apporblueprintkey-list-ft-templatecontextprocessorcallable" aria-label="Permalink to &quot;template_context_processors: `dict[ft.AppOrBlueprintKey, list[ft.TemplateContextProcessorCallable]]`&quot;">​</a></h3><p>A data structure of functions to call to pass extra context values when rendering templates, in the format <code>{scope: [functions]}</code>. The <code>scope</code> key is the name of a blueprint the functions are active for, or <code>None</code> for all requests.</p><p>To register a function, use the <code>context_processor()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li><li><h3 id="template-folder" tabindex="-1">template_folder <a class="header-anchor" href="#template-folder" aria-label="Permalink to &quot;template_folder&quot;">​</a></h3><p>The path to the templates folder, relative to <code>root_path</code>, to add to the template loader. <code>None</code> if templates should not be added.</p></li><li><h3 id="url-default-functions-dict-ft-apporblueprintkey-list-ft-urldefaultcallable" tabindex="-1">url_default_functions: <code>dict[ft.AppOrBlueprintKey, list[ft.URLDefaultCallable]]</code> <a class="header-anchor" href="#url-default-functions-dict-ft-apporblueprintkey-list-ft-urldefaultcallable" aria-label="Permalink to &quot;url_default_functions: `dict[ft.AppOrBlueprintKey, list[ft.URLDefaultCallable]]`&quot;">​</a></h3><p>A data structure of functions to call to modify the keyword arguments when generating URLs, in the format <code>{scope: [functions]}</code>. The <code>scope</code> key is the name of a blueprint the functions are active for, or <code>None</code> for all requests.</p><p>To register a function, use the <code>url_defaults()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li><li><h3 id="url-defaults-f" tabindex="-1">url_defaults(<code>f</code>) <a class="header-anchor" href="#url-defaults-f" aria-label="Permalink to &quot;url_defaults(`f`)&quot;">​</a></h3><p>Callback function for URL defaults for all view functions of the application. It’s called with the endpoint and values and should update the values passed in place.</p><p>This is available on both app and blueprint objects. When used on an app, this is called for every request. When used on a blueprint, this is called for requests that the blueprint handles. To register with a blueprint and affect every request, use <code>Blueprint.app_url_defaults()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_url_defaults)</code> –</li></ul><p><em>Return type</em>: <code>T_url_defaults</code></p></li><li><h3 id="url-value-preprocessor-f" tabindex="-1">url_value_preprocessor(<code>f</code>) <a class="header-anchor" href="#url-value-preprocessor-f" aria-label="Permalink to &quot;url_value_preprocessor(`f`)&quot;">​</a></h3><p>Register a URL value preprocessor function for all view functions in the application. These functions will be called before the <code>before_request()</code> functions.</p><p>The function can modify the values captured from the matched url before they are passed to the view. For example, this can be used to pop a common language code value and place it in g rather than pass it to every view.</p><p>The function is passed the endpoint name and values dict. The return value is ignored.</p><p>This is available on both app and blueprint objects. When used on an app, this is called for every request. When used on a blueprint, this is called for requests that the blueprint handles. To register with a blueprint and affect every request, use <code>Blueprint.app_url_value_preprocessor()</code>.</p><p><em>Parameters</em>:</p><ul><li><code>f (T_url_value_preprocessor)</code> –</li></ul><p><em>Return type</em>: <code>T_url_value_preprocessor</code></p></li><li><h3 id="url-value-preprocessors-dict-ft-apporblueprintkey-list-ft-urlvaluepreprocessorcallable" tabindex="-1">url_value_preprocessors: <code>dict[ft.AppOrBlueprintKey, list[ft.URLValuePreprocessorCallable]]</code> <a class="header-anchor" href="#url-value-preprocessors-dict-ft-apporblueprintkey-list-ft-urlvaluepreprocessorcallable" aria-label="Permalink to &quot;url_value_preprocessors: `dict[ft.AppOrBlueprintKey, list[ft.URLValuePreprocessorCallable]]`&quot;">​</a></h3><p>A data structure of functions to call to modify the keyword arguments passed to the view function, in the format <code>{scope: [functions]}</code>. The <code>scope</code> key is the name of a blueprint the functions are active for, or <code>None</code> for all requests.</p><p>To register a function, use the <code>url_value_preprocessor()</code> decorator.</p><p>This data structure is internal. It should not be modified directly and its format may change at any time.</p></li></ul>',9),s=[l];function n(p,i,c,d,u,h){return t(),o("div",null,s)}const b=e(r,[["render",n]]);export{f as __pageData,b as default};
